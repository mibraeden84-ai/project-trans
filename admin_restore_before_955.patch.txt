*** Begin Patch
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                $db->query("INSERT INTO user_permissions (user_id, can_upload, can_delete, can_manage_brands, can_manage_models) VALUES (?, ?, ?, ?, ?) ON DUPLICATE KEY UPDATE can_upload=VALUES(can_upload), can_delete=VALUES(can_delete), can_manage_brands=VALUES(can_manage_brands), can_manage_models=VALUES(can_manage_models)",
+                $db->query("INSERT INTO user_permissions (user_id, can_upload, can_delete, can_manage_brands, can_manage_models) VALUES (?, ?, ?, ?, ?) ON CONFLICT (user_id) DO UPDATE SET can_upload=EXCLUDED.can_upload, can_delete=EXCLUDED.can_delete, can_manage_brands=EXCLUDED.can_manage_brands, can_manage_models=EXCLUDED.can_manage_models",
                     [$uid, $perms['can_upload'], $perms['can_delete'], $perms['can_manage_brands'], $perms['can_manage_models']]);
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                $db->query("INSERT INTO user_permissions (user_id, can_upload, can_delete, can_manage_brands, can_manage_models) VALUES (?, ?, ?, ?, ?) ON CONFLICT (user_id) DO UPDATE SET can_upload=EXCLUDED.can_upload, can_delete=EXCLUDED.can_delete, can_manage_brands=EXCLUDED.can_manage_brands, can_manage_models=EXCLUDED.can_manage_models",
-                    [$uid, $perms['can_upload'], $perms['can_delete'], $perms['can_manage_brands'], $perms['can_manage_models']]);
+                if (DB_DRIVER === 'pgsql') {
+                    $db->query("INSERT INTO user_permissions (user_id, can_upload, can_delete, can_manage_brands, can_manage_models) VALUES (?, ?, ?, ?, ?) ON CONFLICT (user_id) DO UPDATE SET can_upload=EXCLUDED.can_upload, can_delete=EXCLUDED.can_delete, can_manage_brands=EXCLUDED.can_manage_brands, can_manage_models=EXCLUDED.can_manage_models",
+                        [$uid, $perms['can_upload'], $perms['can_delete'], $perms['can_manage_brands'], $perms['can_manage_models']]);
+                } else {
+                    $db->query("INSERT INTO user_permissions (user_id, can_upload, can_delete, can_manage_brands, can_manage_models) VALUES (?, ?, ?, ?, ?) ON DUPLICATE KEY UPDATE can_upload=VALUES(can_upload), can_delete=VALUES(can_delete), can_manage_brands=VALUES(can_manage_brands), can_manage_models=VALUES(can_manage_models)",
+                        [$uid, $perms['can_upload'], $perms['can_delete'], $perms['can_manage_brands'], $perms['can_manage_models']]);
+                }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if (isset($_POST['rename_file']) && isset($_POST['type']) && isset($_POST['file_id'])) {
@@
     echo json_encode(['success'=>true]);
     exit;
 }
+
+// Handle brand rename/color update
+if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['update_brand'])) {
+    if (!canManageBrands()) { setFlash('You don\'t have permission to edit brands', 'error'); header('Location: dashboard.php#brands-models'); exit; }
+    $brandId = (int)($_POST['brand_id'] ?? 0);
+    $brandName = trim($_POST['brand_name'] ?? '');
+    $brandColor = trim($_POST['brand_color'] ?? '#005aa0');
+
+    if (!$brandId || $brandName === '') {
+        setFlash('Brand name is required', 'error');
+    } else {
+        $existing = $db->fetchOne("SELECT id FROM brands WHERE LOWER(name) = LOWER(?) AND id != ?", [$brandName, $brandId]);
+        if ($existing) {
+            setFlash('Another brand already uses that name', 'error');
+        } else {
+            $slug = slugify($brandName);
+            $slugOwner = $db->fetchOne("SELECT id FROM brands WHERE slug = ? AND id != ?", [$slug, $brandId]);
+            if ($slugOwner) {
+                setFlash('Another brand already uses that URL slug', 'error');
+            } else {
+                $db->query("UPDATE brands SET name = ?, slug = ?, color = ? WHERE id = ?", [$brandName, $slug, $brandColor, $brandId]);
+                setFlash("Brand updated");
+            }
+        }
+    }
+    header('Location: dashboard.php#brands-models');
+    exit;
+}
+
+// Handle brand delete
+if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['delete_brand'])) {
+    if (!canManageBrands()) { setFlash('You don\'t have permission to delete brands', 'error'); header('Location: dashboard.php#brands-models'); exit; }
+    $brandId = (int)($_POST['brand_id'] ?? 0);
+    $brand = $db->fetchOne("SELECT name FROM brands WHERE id = ?", [$brandId]);
+    if ($brand) {
+        $db->query("DELETE FROM brands WHERE id = ?", [$brandId]);
+        setFlash("Deleted brand " . $brand['name']);
+    }
+    header('Location: dashboard.php#brands-models');
+    exit;
+}
+
+// Handle model rename/update
+if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['update_model'])) {
+    if (!canManageModels()) { setFlash('You don\'t have permission to edit models', 'error'); header('Location: dashboard.php#brands-models'); exit; }
+    $modelId = (int)($_POST['model_id'] ?? 0);
+    $modelBrandId = (int)($_POST['model_brand_id'] ?? 0);
+    $modelName = trim($_POST['model_name'] ?? '');
+    $modelSystemType = $_POST['model_system_type'] ?? null;
+    if ($modelSystemType === '') $modelSystemType = null;
+
+    if (!$modelId || !$modelBrandId || $modelName === '') {
+        setFlash('Model name and brand are required', 'error');
+    } else {
+        $existing = $db->fetchOne(
+            "SELECT id FROM device_models WHERE brand_id = ? AND LOWER(name) = LOWER(?) AND id != ?",
+            [$modelBrandId, $modelName, $modelId]
+        );
+        if ($existing) {
+            setFlash('That brand already has a model with this name', 'error');
+        } else {
+            $slug = slugify($modelName);
+            $slugOwner = $db->fetchOne("SELECT id FROM device_models WHERE brand_id = ? AND slug = ? AND id != ?", [$modelBrandId, $slug, $modelId]);
+            if ($slugOwner) {
+                $slug = slugify($modelName . '-' . $modelBrandId);
+            }
+            $db->query("UPDATE device_models SET brand_id = ?, name = ?, slug = ?, system_type = ? WHERE id = ?",
+                [$modelBrandId, $modelName, $slug, $modelSystemType, $modelId]);
+            setFlash("Model updated");
+        }
+    }
+    header('Location: dashboard.php#brands-models');
+    exit;
+}
+
+// Handle model delete
+if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['delete_model'])) {
+    if (!canManageModels()) { setFlash('You don\'t have permission to delete models', 'error'); header('Location: dashboard.php#brands-models'); exit; }
+    $modelId = (int)($_POST['model_id'] ?? 0);
+    $model = $db->fetchOne("SELECT name FROM device_models WHERE id = ?", [$modelId]);
+    if ($model) {
+        $db->query("DELETE FROM device_models WHERE id = ?", [$modelId]);
+        setFlash("Deleted model " . $model['name']);
+    }
+    header('Location: dashboard.php#brands-models');
+    exit;
+}
 
 // Handle inline brand creation
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['create_brand']) && isset($_POST['brand_name']) && $_POST['brand_name'] !== '') {
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 $brands = $db->getBrands();
+$managedBrands = $db->fetchAll(
+    "SELECT b.*,
+        (SELECT COUNT(*) FROM device_models dm WHERE dm.brand_id = b.id) as model_count,
+        (
+            (SELECT COUNT(*) FROM firmware_files f WHERE f.brand_id = b.id AND f.status = 'active') +
+            (SELECT COUNT(*) FROM manuals m WHERE m.brand_id = b.id AND m.status = 'active') +
+            (SELECT COUNT(*) FROM software_files s WHERE s.brand_id = b.id AND s.status = 'active')
+        ) as file_count
+     FROM brands b
+     ORDER BY b.name"
+);
+$managedModels = $db->fetchAll(
+    "SELECT dm.*, b.name as brand_name,
+        (SELECT COUNT(*) FROM config_files c WHERE c.device_model_id = dm.id AND c.status = 'active') as config_count,
+        (
+            (SELECT COUNT(*) FROM firmware_files f WHERE f.device_model_id = dm.id AND f.status = 'active') +
+            (SELECT COUNT(*) FROM manuals m WHERE m.device_model_id = dm.id AND m.status = 'active') +
+            (SELECT COUNT(*) FROM software_files s WHERE s.device_model_id = dm.id AND s.status = 'active')
+        ) as file_count
+     FROM device_models dm
+     JOIN brands b ON dm.brand_id = b.id
+     ORDER BY b.name, dm.name"
+);
 $stats = [
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         <a href="#" onclick="return showTab('firmware')"><i class="fas fa-microchip"></i> Firmware</a>
         <a href="#" onclick="return showTab('manuals')"><i class="fas fa-book"></i> Manuals</a>
         <a href="#" onclick="return showTab('software')"><i class="fas fa-gear"></i> Software</a>
+        <?php if (canManageBrands() || canManageModels()): ?><a href="#" onclick="return showTab('brands-models')"><i class="fas fa-sitemap"></i> Brands &amp; Models</a><?php endif; ?>
         <?php if (isAdmin()): ?><a href="#" onclick="return showTab('users')"><i class="fas fa-users"></i> Users</a><?php endif; ?>
@@
                 </div>
             </div>
+            <?php flash(); ?>
 
         <?php if (isAdmin()): ?>
@@
             <button class="tab-btn" onclick="showTab('firmware')">Firmware</button>
             <button class="tab-btn" onclick="showTab('manuals')">Manuals</button>
             <button class="tab-btn" onclick="showTab('software')">Software</button>
+            <?php if (canManageBrands() || canManageModels()): ?><button class="tab-btn" onclick="showTab('brands-models')">Brands &amp; Models</button><?php endif; ?>
             <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')">Users</button><?php endif; ?>
         </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         <div id="tab-software" class="tab-content">
             <div class="section">
                 <h2><i class="fas fa-gear"></i> Configurator Software</h2>
@@
                 <?php endif; ?>
             </div>
         </div>
 
+        <?php if (canManageBrands() || canManageModels()): ?>
+        <div id="tab-brands-models" class="tab-content">
+            <?php if (canManageBrands()): ?>
+            <div class="section">
+                <h2><i class="fas fa-sitemap"></i> Brands</h2>
+                <?php if (empty($managedBrands)): echo '<div class="empty">No brands found.</div>'; else: ?>
+                <table>
+                    <thead><tr><th>Name</th><th>Color</th><th>Models</th><th>Files</th><th>Actions</th></tr></thead>
+                    <tbody>
+                    <?php foreach ($managedBrands as $brandRow): ?>
+                        <?php
+                        $brandFormId = 'brandForm' . (int)$brandRow['id'];
+                        $brandColor = preg_match('/^#[0-9a-fA-F]{6}$/', $brandRow['color'] ?? '') ? $brandRow['color'] : '#005aa0';
+                        $brandConfirm = htmlspecialchars(json_encode('Delete brand "' . $brandRow['name'] . '"? This also removes related models and linked files.'), ENT_QUOTES, 'UTF-8');
+                        ?>
+                        <tr>
+                            <td>
+                                <input type="text" name="brand_name" value="<?= escape($brandRow['name']) ?>" form="<?= $brandFormId ?>" required>
+                            </td>
+                            <td>
+                                <input type="color" name="brand_color" value="<?= escape($brandColor) ?>" form="<?= $brandFormId ?>" style="width:44px;height:34px;padding:2px">
+                            </td>
+                            <td><?= (int)$brandRow['model_count'] ?></td>
+                            <td><?= (int)$brandRow['file_count'] ?></td>
+                            <td>
+                                <form id="<?= $brandFormId ?>" method="POST" style="display:inline">
+                                    <input type="hidden" name="update_brand" value="1">
+                                    <input type="hidden" name="brand_id" value="<?= (int)$brandRow['id'] ?>">
+                                </form>
+                                <button type="submit" form="<?= $brandFormId ?>" class="btn btn-primary btn-sm"><i class="fas fa-save"></i> Save</button>
+                                <form method="POST" style="display:inline" onsubmit='return confirm(<?= $brandConfirm ?>)'>
+                                    <input type="hidden" name="delete_brand" value="1">
+                                    <input type="hidden" name="brand_id" value="<?= (int)$brandRow['id'] ?>">
+                                    <button type="submit" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i> Delete</button>
+                                </form>
+                            </td>
+                        </tr>
+                    <?php endforeach; ?>
+                    </tbody>
+                </table>
+                <?php endif; ?>
+            </div>
+            <?php endif; ?>
+
+            <?php if (canManageModels()): ?>
+            <div class="section">
+                <h2><i class="fas fa-microchip"></i> Models</h2>
+                <?php if (empty($managedModels)): echo '<div class="empty">No models found.</div>'; else: ?>
+                <table>
+                    <thead><tr><th>Name</th><th>Brand</th><th>System</th><th>Configs</th><th>Files</th><th>Actions</th></tr></thead>
+                    <tbody>
+                    <?php foreach ($managedModels as $modelRow): ?>
+                        <?php
+                        $modelFormId = 'modelForm' . (int)$modelRow['id'];
+                        $modelConfirm = htmlspecialchars(json_encode('Delete model "' . $modelRow['name'] . '"? This also removes linked configuration files.'), ENT_QUOTES, 'UTF-8');
+                        ?>
+                        <tr>
+                            <td>
+                                <input type="text" name="model_name" value="<?= escape($modelRow['name']) ?>" form="<?= $modelFormId ?>" required>
+                            </td>
+                            <td>
+                                <select name="model_brand_id" form="<?= $modelFormId ?>" required>
+                                    <?php foreach ($brands as $brandOption): ?>
+                                    <option value="<?= (int)$brandOption['id'] ?>" <?= (int)$brandOption['id'] === (int)$modelRow['brand_id'] ? 'selected' : '' ?>><?= escape($brandOption['name']) ?></option>
+                                    <?php endforeach; ?>
+                                </select>
+                            </td>
+                            <td>
+                                <select name="model_system_type" form="<?= $modelFormId ?>">
+                                    <option value="" <?= empty($modelRow['system_type']) ? 'selected' : '' ?>>None</option>
+                                    <option value="advanced" <?= ($modelRow['system_type'] ?? '') === 'advanced' ? 'selected' : '' ?>>Advanced</option>
+                                    <option value="standard" <?= ($modelRow['system_type'] ?? '') === 'standard' ? 'selected' : '' ?>>Standard</option>
+                                </select>
+                            </td>
+                            <td><?= (int)$modelRow['config_count'] ?></td>
+                            <td><?= (int)$modelRow['file_count'] ?></td>
+                            <td>
+                                <form id="<?= $modelFormId ?>" method="POST" style="display:inline">
+                                    <input type="hidden" name="update_model" value="1">
+                                    <input type="hidden" name="model_id" value="<?= (int)$modelRow['id'] ?>">
+                                </form>
+                                <button type="submit" form="<?= $modelFormId ?>" class="btn btn-primary btn-sm"><i class="fas fa-save"></i> Save</button>
+                                <form method="POST" style="display:inline" onsubmit='return confirm(<?= $modelConfirm ?>)'>
+                                    <input type="hidden" name="delete_model" value="1">
+                                    <input type="hidden" name="model_id" value="<?= (int)$modelRow['id'] ?>">
+                                    <button type="submit" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i> Delete</button>
+                                </form>
+                            </td>
+                        </tr>
+                    <?php endforeach; ?>
+                    </tbody>
+                </table>
+                <?php endif; ?>
+            </div>
+            <?php endif; ?>
+        </div>
+        <?php endif; ?>
+
     <?php if (isAdmin()): ?>
         <div id="tab-users" class="tab-content">
             <div class="section">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         table { width: 100%; border-collapse: collapse; }
         th, td { padding: 10px 12px; text-align: left; border-bottom: 1px solid #eee; font-size: 0.85rem; }
         th { font-weight: 600; color: #6c757d; }
+        table input[type="text"], table select { width: 100%; min-width: 120px; padding: 7px 9px; border: 2px solid #e0e0e0; border-radius: 6px; font-size: 0.85rem; outline: none; }
+        table input[type="text"]:focus, table select:focus { border-color: #005aa0; }
         .btn { display: inline-flex; align-items: center; gap: 6px; padding: 8px 16px; border-radius: 6px; font-size: 0.85rem; font-weight: 600; text-decoration: none; border: none; cursor: pointer; transition: all 0.3s; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
+        .admin-search { display: flex; align-items: center; gap: 10px; background: #fff; border-radius: 10px; padding: 12px 16px; margin-bottom: 16px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
+        .admin-search i { color: #6c757d; }
+        .admin-search input { flex: 1; min-width: 180px; border: none; outline: none; font-size: 0.95rem; }
+        .admin-search button { border: none; background: transparent; color: #6c757d; cursor: pointer; width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 6px; }
+        .admin-search button:hover { background: #f0f2f5; color: #333; }
+        .admin-search-meta { color: #6c757d; font-size: 0.8rem; white-space: nowrap; }
         .tab-bar { display: flex; gap: 4px; margin-bottom: 16px; flex-wrap: wrap; }
@@
         <?php if (isAdmin()): ?>
         <div class="stats-grid">
@@
         </div>
         <?php endif; ?>
 
+        <div class="admin-search">
+            <i class="fas fa-search"></i>
+            <input type="search" id="adminSearchInput" placeholder="Search admin records..." autocomplete="off">
+            <span id="adminSearchMeta" class="admin-search-meta"></span>
+            <button type="button" onclick="clearAdminSearch()" title="Clear search"><i class="fas fa-times"></i></button>
+        </div>
+
         <div class="tab-bar">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
     </script>
     <script>
+        function adminRowSearchText(row) {
+            var parts = [row.innerText || row.textContent || ''];
+            row.querySelectorAll('input, textarea').forEach(function(el) {
+                parts.push(el.value || '');
+            });
+            row.querySelectorAll('select').forEach(function(el) {
+                parts.push(el.value || '');
+                if (el.selectedIndex >= 0 && el.options[el.selectedIndex]) {
+                    parts.push(el.options[el.selectedIndex].text || '');
+                }
+            });
+            return parts.join(' ').toLowerCase();
+        }
+
+        function ensureAdminSearchEmptyRow(table) {
+            var tbody = table.tBodies[0];
+            if (!tbody) return null;
+            var row = tbody.querySelector('.admin-search-empty-row');
+            if (!row) {
+                row = document.createElement('tr');
+                row.className = 'admin-search-empty-row';
+                var cell = document.createElement('td');
+                cell.colSpan = table.tHead && table.tHead.rows[0] ? table.tHead.rows[0].cells.length : 1;
+                cell.className = 'empty';
+                cell.textContent = 'No matching records';
+                row.appendChild(cell);
+                tbody.appendChild(row);
+            }
+            return row;
+        }
+
+        function applyAdminSearch() {
+            var input = document.getElementById('adminSearchInput');
+            var meta = document.getElementById('adminSearchMeta');
+            var query = input ? input.value.trim().toLowerCase() : '';
+            var activeTab = document.querySelector('.tab-content.active');
+            var activeVisible = 0;
+            var activeTotal = 0;
+
+            document.querySelectorAll('.tab-content table').forEach(function(table) {
+                var tbody = table.tBodies[0];
+                if (!tbody) return;
+                var rows = Array.prototype.filter.call(tbody.rows, function(row) {
+                    return !row.classList.contains('admin-search-empty-row');
+                });
+                var visible = 0;
+
+                rows.forEach(function(row) {
+                    var match = query === '' || adminRowSearchText(row).indexOf(query) !== -1;
+                    row.style.display = match ? '' : 'none';
+                    if (match) visible++;
+                });
+
+                var emptyRow = ensureAdminSearchEmptyRow(table);
+                if (emptyRow) emptyRow.style.display = query !== '' && visible === 0 ? '' : 'none';
+
+                if (activeTab && activeTab.contains(table)) {
+                    activeVisible += visible;
+                    activeTotal += rows.length;
+                }
+            });
+
+            if (meta) {
+                meta.textContent = query && activeTotal ? activeVisible + '/' + activeTotal : '';
+            }
+        }
+
+        function clearAdminSearch() {
+            var input = document.getElementById('adminSearchInput');
+            if (input) input.value = '';
+            applyAdminSearch();
+            if (input) input.focus();
+        }
+
         function showTab(tabName) {
             document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
             document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
             document.querySelectorAll('.sidebar a').forEach(a => a.classList.remove('active'));
             document.getElementById('tab-' + tabName).classList.add('active');
@@
             const nav = document.querySelector(`.sidebar a[onclick*="'${tabName}'"]`);
             if (nav) nav.classList.add('active');
             location.hash = tabName;
+            applyAdminSearch();
             return false;
         }
@@
         // Hash-based initial tab on page load
         (function() {
+            var adminSearchInput = document.getElementById('adminSearchInput');
+            if (adminSearchInput) {
+                adminSearchInput.addEventListener('input', applyAdminSearch);
+            }
             if (location.hash) {
                 const tab = location.hash.replace('#', '');
                 if (document.getElementById('tab-' + tab)) {
                     showTab(tab);
                 }
             }
+            applyAdminSearch();
         })();
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .tab-bar { display: flex; gap: 4px; margin-bottom: 16px; flex-wrap: wrap; }
         .tab-btn { padding: 8px 20px; border: none; background: #e0e0e0; border-radius: 6px; cursor: pointer; font-size: 0.85rem; font-weight: 600; transition: all 0.3s; }
         .tab-btn.active { background: #005aa0; color: #fff; }
+        .tab-search-count { display: inline-flex; align-items: center; justify-content: center; min-width: 20px; height: 20px; margin-left: 6px; padding: 0 6px; border-radius: 999px; background: rgba(0,0,0,0.12); font-size: 0.75rem; line-height: 1; }
+        .tab-btn.active .tab-search-count { background: rgba(255,255,255,0.22); color: #fff; }
         .tab-content { display: none; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function ensureAdminSearchEmptyRow(table) {
             var tbody = table.tBodies[0];
             if (!tbody) return null;
@@
             return row;
         }
 
+        function adminTabNameFromControl(control) {
+            var onclick = control.getAttribute('onclick') || '';
+            var match = onclick.match(/showTab\('([^']+)'\)/);
+            return match ? match[1] : '';
+        }
+
+        function updateAdminSearchBadges(tabStats, query) {
+            document.querySelectorAll('.tab-btn').forEach(function(btn) {
+                var tabName = adminTabNameFromControl(btn);
+                var badge = btn.querySelector('.tab-search-count');
+                if (!query || !tabName || !tabStats[tabName] || tabStats[tabName].total === 0) {
+                    if (badge) badge.remove();
+                    return;
+                }
+
+                if (!badge) {
+                    badge = document.createElement('span');
+                    badge.className = 'tab-search-count';
+                    btn.appendChild(badge);
+                }
+                badge.textContent = tabStats[tabName].visible;
+            });
+        }
+
         function applyAdminSearch() {
             var input = document.getElementById('adminSearchInput');
             var meta = document.getElementById('adminSearchMeta');
             var query = input ? input.value.trim().toLowerCase() : '';
             var activeTab = document.querySelector('.tab-content.active');
             var activeVisible = 0;
             var activeTotal = 0;
+            var tabStats = {};
 
-            document.querySelectorAll('.tab-content table').forEach(function(table) {
-                var tbody = table.tBodies[0];
-                if (!tbody) return;
-                var rows = Array.prototype.filter.call(tbody.rows, function(row) {
-                    return !row.classList.contains('admin-search-empty-row');
-                });
-                var visible = 0;
-
-                rows.forEach(function(row) {
-                    var match = query === '' || adminRowSearchText(row).indexOf(query) !== -1;
-                    row.style.display = match ? '' : 'none';
-                    if (match) visible++;
-                });
-
-                var emptyRow = ensureAdminSearchEmptyRow(table);
-                if (emptyRow) emptyRow.style.display = query !== '' && visible === 0 ? '' : 'none';
-
-                if (activeTab && activeTab.contains(table)) {
-                    activeVisible += visible;
-                    activeTotal += rows.length;
-                }
+            document.querySelectorAll('.tab-content').forEach(function(tabContent) {
+                var tabName = tabContent.id.replace(/^tab-/, '');
+                tabStats[tabName] = { visible: 0, total: 0 };
+
+                tabContent.querySelectorAll('table').forEach(function(table) {
+                    var tbody = table.tBodies[0];
+                    if (!tbody) return;
+                    var rows = Array.prototype.filter.call(tbody.rows, function(row) {
+                        return !row.classList.contains('admin-search-empty-row');
+                    });
+                    var visible = 0;
+
+                    rows.forEach(function(row) {
+                        var match = query === '' || adminRowSearchText(row).indexOf(query) !== -1;
+                        row.style.display = match ? '' : 'none';
+                        if (match) visible++;
+                    });
+
+                    var emptyRow = ensureAdminSearchEmptyRow(table);
+                    if (emptyRow) emptyRow.style.display = query !== '' && visible === 0 ? '' : 'none';
+
+                    tabStats[tabName].visible += visible;
+                    tabStats[tabName].total += rows.length;
+                });
+
+                if (activeTab && activeTab === tabContent) {
+                    activeVisible = tabStats[tabName].visible;
+                    activeTotal = tabStats[tabName].total;
+                }
             });
 
+            updateAdminSearchBadges(tabStats, query);
+
             if (meta) {
-                meta.textContent = query && activeTotal ? activeVisible + '/' + activeTotal : '';
+                meta.textContent = query ? activeVisible + '/' + activeTotal : '';
             }
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-    $sql = "UPDATE $table SET name = ?, version = ?, description = ? WHERE id = ?";
     if ($type === 'firmware') {
         $changelog = trim($_POST['changelog'] ?? '');
-        $sql = "UPDATE $table SET name = ?, version = ?, changelog = ? WHERE id = ?";
-        $db->query($sql, [$newName, $newVersion, $changelog, $id]);
+        $db->query("UPDATE $table SET name = ?, version = ?, changelog = ? WHERE id = ?", [$newName, $newVersion, $changelog, $id]);
+    } elseif ($type === 'manual') {
+        $db->query("UPDATE $table SET name = ?, description = ? WHERE id = ?", [$newName, $newDescription, $id]);
     } else {
-        $db->query($sql, [$newName, $newVersion, $newDescription, $id]);
+        $db->query("UPDATE $table SET name = ?, version = ?, description = ? WHERE id = ?", [$newName, $newVersion, $newDescription, $id]);
     }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        * { margin: 0; padding: 0; box-sizing: border-box; }
-        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #f0f2f5; color: #333; }
+        :root {
+            --ink: #182230;
+            --muted: #667085;
+            --line: #d9e2ec;
+            --panel: #fff;
+            --page: #f5f7fb;
+            --nav: #111827;
+            --primary: #005aa0;
+            --primary-dark: #003d73;
+            --success: #008a5b;
+            --danger: #d92d20;
+            --shadow: 0 8px 24px rgba(15, 23, 42, 0.07);
+        }
+        * { margin: 0; padding: 0; box-sizing: border-box; }
+        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: var(--page); color: var(--ink); }
         .sidebar {
             position: fixed; left: 0; top: 0; bottom: 0; width: 240px;
-            background: #1a1a2e; color: #fff; padding: 24px 0; overflow-y: auto;
+            background: var(--nav); color: #fff; padding: 22px 0; overflow-y: auto;
+            box-shadow: 8px 0 28px rgba(15, 23, 42, 0.18);
         }
-        .sidebar h2 { padding: 0 20px; font-size: 1rem; opacity: 0.7; margin-bottom: 16px; }
+        .sidebar h2 { padding: 0 20px; font-size: 0.72rem; letter-spacing: 0.08em; text-transform: uppercase; opacity: 0.62; margin-bottom: 10px; }
         .sidebar a {
-            display: block; padding: 12px 20px; color: rgba(255,255,255,0.8);
-            text-decoration: none; font-size: 0.9rem; transition: all 0.3s;
+            display: flex; align-items: center; margin: 2px 10px; padding: 10px 12px; color: rgba(255,255,255,0.78);
+            text-decoration: none; font-size: 0.9rem; border-radius: 8px; transition: background 0.18s, color 0.18s, transform 0.18s;
         }
-        .sidebar a:hover, .sidebar a.active { background: rgba(255,255,255,0.1); color: #fff; }
+        .sidebar a:hover, .sidebar a.active { background: rgba(255,255,255,0.12); color: #fff; transform: translateX(2px); }
         .sidebar a i { width: 20px; margin-right: 10px; }
-        .sidebar .logout { margin-top: 32px; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 16px; color: #e74c3c; }
-        .main { margin-left: 240px; padding: 24px 32px; }
+        .sidebar .logout { margin-top: 28px; color: #ffb4ab; }
+        .main { margin-left: 240px; padding: 28px 32px 40px; }
         .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
-        .header h1 { font-size: 1.5rem; }
-        .header .user { font-size: 0.9rem; color: #6c757d; }
-        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 16px; margin-bottom: 32px; }
-        .stat-card { background: #fff; border-radius: 12px; padding: 20px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); text-align: center; }
-        .stat-card .number { font-size: 2rem; font-weight: 700; color: #005aa0; }
-        .stat-card .label { font-size: 0.85rem; color: #6c757d; margin-top: 4px; }
-        .section { background: #fff; border-radius: 12px; padding: 24px; margin-bottom: 24px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
-        .section h2 { font-size: 1.1rem; margin-bottom: 16px; display: flex; align-items: center; gap: 8px; }
-        table { width: 100%; border-collapse: collapse; }
-        th, td { padding: 10px 12px; text-align: left; border-bottom: 1px solid #eee; font-size: 0.85rem; }
-        th { font-weight: 600; color: #6c757d; }
-        table input[type="text"], table select { width: 100%; min-width: 120px; padding: 7px 9px; border: 2px solid #e0e0e0; border-radius: 6px; font-size: 0.85rem; outline: none; }
-        table input[type="text"]:focus, table select:focus { border-color: #005aa0; }
-        .btn { display: inline-flex; align-items: center; gap: 6px; padding: 8px 16px; border-radius: 6px; font-size: 0.85rem; font-weight: 600; text-decoration: none; border: none; cursor: pointer; transition: all 0.3s; }
-        .btn-primary { background: #005aa0; color: #fff; }
-        .btn-primary:hover { background: #003d73; }
-        .btn-danger { background: #e74c3c; color: #fff; }
-        .btn-danger:hover { background: #c0392b; }
+        .header h1 { font-size: 1.45rem; font-weight: 700; letter-spacing: 0; }
+        .header .user { font-size: 0.9rem; color: var(--muted); }
+        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 14px; margin-bottom: 22px; }
+        .stat-card { background: var(--panel); border: 1px solid rgba(0,90,160,0.08); border-radius: 8px; padding: 18px; box-shadow: var(--shadow); }
+        .stat-card .number { font-size: 1.85rem; font-weight: 750; color: var(--primary); line-height: 1; }
+        .stat-card .label { font-size: 0.78rem; color: var(--muted); margin-top: 8px; text-transform: uppercase; letter-spacing: 0.06em; }
+        .section { background: var(--panel); border: 1px solid rgba(15, 23, 42, 0.06); border-radius: 8px; padding: 22px; margin-bottom: 22px; box-shadow: var(--shadow); overflow-x: auto; }
+        .section h2 { font-size: 1.02rem; margin-bottom: 16px; display: flex; align-items: center; gap: 8px; color: var(--ink); }
+        table { width: 100%; border-collapse: separate; border-spacing: 0; min-width: 780px; }
+        th, td { padding: 12px 12px; text-align: left; border-bottom: 1px solid #edf1f5; font-size: 0.85rem; vertical-align: middle; }
+        th { font-weight: 700; color: var(--muted); background: #f8fafc; position: sticky; top: 0; z-index: 1; }
+        tbody tr { transition: background 0.15s; }
+        tbody tr:hover { background: #f8fbff; }
+        table input[type="text"], table select { width: 100%; min-width: 120px; padding: 8px 10px; border: 1px solid var(--line); border-radius: 6px; font-size: 0.85rem; outline: none; background: #fff; }
+        table input[type="text"]:focus, table select:focus { border-color: var(--primary); box-shadow: 0 0 0 3px rgba(0,90,160,0.12); }
+        .btn { display: inline-flex; align-items: center; justify-content: center; gap: 6px; padding: 8px 14px; border-radius: 6px; font-size: 0.85rem; font-weight: 650; text-decoration: none; border: none; cursor: pointer; transition: background 0.18s, box-shadow 0.18s, transform 0.18s; white-space: nowrap; }
+        .btn:hover { transform: translateY(-1px); }
+        .btn-primary { background: var(--primary); color: #fff; box-shadow: 0 4px 12px rgba(0,90,160,0.18); }
+        .btn-primary:hover { background: var(--primary-dark); }
+        .btn-danger { background: var(--danger); color: #fff; box-shadow: 0 4px 12px rgba(217,45,32,0.16); }
+        .btn-danger:hover { background: #b42318; }
         .btn-sm { padding: 4px 10px; font-size: 0.8rem; }
         .form-group { margin-bottom: 16px; }
-        .form-group label { display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 4px; }
+        .form-group label { display: block; font-size: 0.82rem; font-weight: 700; margin-bottom: 6px; color: #344054; }
         .form-group input, .form-group select, .form-group textarea {
-            width: 100%; padding: 10px 12px; border: 2px solid #e0e0e0;
-            border-radius: 6px; font-size: 0.9rem; outline: none;
+            width: 100%; padding: 10px 12px; border: 1px solid var(--line);
+            border-radius: 6px; font-size: 0.9rem; outline: none; background: #fff;
         }
-        .form-group input:focus, .form-group select:focus { border-color: #005aa0; }
+        .form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--primary); box-shadow: 0 0 0 3px rgba(0,90,160,0.12); }
         .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
-        .admin-search { display: flex; align-items: center; gap: 10px; background: #fff; border-radius: 10px; padding: 12px 16px; margin-bottom: 16px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
-        .admin-search i { color: #6c757d; }
+        .admin-search { display: flex; align-items: center; gap: 10px; background: #fff; border: 1px solid rgba(0,90,160,0.1); border-radius: 8px; padding: 12px 16px; margin-bottom: 14px; box-shadow: var(--shadow); position: sticky; top: 0; z-index: 5; }
+        .admin-search:focus-within { border-color: var(--primary); box-shadow: 0 0 0 3px rgba(0,90,160,0.10), var(--shadow); }
+        .admin-search i { color: var(--muted); }
         .admin-search input { flex: 1; min-width: 180px; border: none; outline: none; font-size: 0.95rem; }
-        .admin-search button { border: none; background: transparent; color: #6c757d; cursor: pointer; width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 6px; }
-        .admin-search button:hover { background: #f0f2f5; color: #333; }
-        .admin-search-meta { color: #6c757d; font-size: 0.8rem; white-space: nowrap; }
-        .tab-bar { display: flex; gap: 4px; margin-bottom: 16px; flex-wrap: wrap; }
-        .tab-btn { padding: 8px 20px; border: none; background: #e0e0e0; border-radius: 6px; cursor: pointer; font-size: 0.85rem; font-weight: 600; transition: all 0.3s; }
-        .tab-btn.active { background: #005aa0; color: #fff; }
+        .admin-search button { border: none; background: transparent; color: var(--muted); cursor: pointer; width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 6px; }
+        .admin-search button:hover { background: #eef4fa; color: var(--ink); }
+        .admin-search-meta { color: var(--muted); font-size: 0.8rem; white-space: nowrap; }
+        .tab-bar { display: flex; gap: 6px; margin-bottom: 16px; flex-wrap: wrap; }
+        .tab-btn { padding: 9px 16px; border: 1px solid var(--line); background: #fff; color: #344054; border-radius: 8px; cursor: pointer; font-size: 0.85rem; font-weight: 700; transition: all 0.18s; }
+        .tab-btn:hover { border-color: rgba(0,90,160,0.35); background: #f8fbff; }
+        .tab-btn.active { background: var(--primary); border-color: var(--primary); color: #fff; box-shadow: 0 8px 18px rgba(0,90,160,0.18); }
         .tab-search-count { display: inline-flex; align-items: center; justify-content: center; min-width: 20px; height: 20px; margin-left: 6px; padding: 0 6px; border-radius: 999px; background: rgba(0,0,0,0.12); font-size: 0.75rem; line-height: 1; }
         .tab-btn.active .tab-search-count { background: rgba(255,255,255,0.22); color: #fff; }
         .tab-content { display: none; }
         .tab-content.active { display: block; }
-        .empty { text-align: center; padding: 32px; color: #6c757d; font-size: 0.9rem; }
-        .back-link { margin-bottom: 16px; display: inline-block; color: #005aa0; text-decoration: none; font-size: 0.9rem; }
-        .required { color: #e74c3c; margin-left: 2px; }
+        .empty { text-align: center; padding: 32px; color: var(--muted); font-size: 0.9rem; }
+        .back-link { margin-bottom: 16px; display: inline-block; color: var(--primary); text-decoration: none; font-size: 0.9rem; }
+        .required { color: var(--danger); margin-left: 2px; }
+        @media (max-width: 900px) {
+            .sidebar { position: static; width: auto; display: block; padding: 14px 0; }
+            .main { margin-left: 0; padding: 18px; }
+            .form-row { grid-template-columns: 1fr; gap: 0; }
+            .header { align-items: flex-start; gap: 12px; flex-direction: column; }
+            .admin-search { position: static; flex-wrap: wrap; }
+            .admin-search input { flex-basis: calc(100% - 44px); }
+        }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function deleteFile(id, type) {
             if (!confirm('Delete this file?')) return false;
             fetch('dashboard.php?delete=' + id + '&type=' + type + '&ajax=1')
-                .then(function(r) { return r.json(); })
+                .then(function(r) {
+                    if (!r.ok) throw new Error('Request failed');
+                    return r.json();
+                })
                 .then(function(d) {
                     if (d.success) {
                         var btn = document.querySelector('[data-del-id="' + id + '"][data-del-type="' + type + '"]');
                         if (btn) { var tr = btn.closest('tr'); if (tr) tr.remove(); }
+                        applyAdminSearch();
+                    } else {
+                        alert('Delete failed');
                     }
-                });
+                })
+                .catch(function() { alert('Delete failed. Please refresh and try again.'); });
             return false;
         }
@@
             fetch('dashboard.php', { method: 'POST', body: form })
-                .then(function(r) { return r.json(); })
+                .then(function(r) {
+                    if (!r.ok) throw new Error('Request failed');
+                    return r.json();
+                })
                 .then(function(d) {
                     if (d.success) {
                         checked.forEach(function(cb) { var tr = cb.closest('tr'); if (tr) tr.remove(); });
+                        applyAdminSearch();
+                    } else {
+                        alert('Delete failed: ' + (d.message || 'Unknown error'));
                     }
-                });
+                })
+                .catch(function() { alert('Bulk delete failed. Please refresh and try again.'); });
         }
@@
         function showTab(tabName) {
+            var nextTab = document.getElementById('tab-' + tabName);
+            if (!nextTab) return false;
             document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
             document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
             document.querySelectorAll('.sidebar a').forEach(a => a.classList.remove('active'));
-            document.getElementById('tab-' + tabName).classList.add('active');
+            nextTab.classList.add('active');
@@
             var adminSearchInput = document.getElementById('adminSearchInput');
             if (adminSearchInput) {
                 adminSearchInput.addEventListener('input', applyAdminSearch);
+                adminSearchInput.addEventListener('keydown', function(e) {
+                    if (e.key === 'Escape') clearAdminSearch();
+                });
             }
@@
             fetch('dashboard.php', { method: 'POST', body: form })
-                .then(function(r) { return r.json(); })
+                .then(function(r) {
+                    if (!r.ok) throw new Error('Request failed');
+                    return r.json();
+                })
                 .then(function(d) {
                     if (d.success) {
                         location.reload();
                     } else {
                         alert('Error: ' + (d.message || 'Unknown error'));
                     }
-                });
+                })
+                .catch(function() { alert('Save failed. Please refresh and try again.'); });
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        .admin-search input { flex: 1; min-width: 180px; border: none; outline: none; font-size: 0.95rem; }
+        .admin-search input { flex: 1 1 auto; min-width: 0; border: none; outline: none; font-size: 0.95rem; }
         .admin-search button { border: none; background: transparent; color: var(--muted); cursor: pointer; width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 6px; }
+        .admin-search button[hidden] { display: none; }
         .admin-search button:hover { background: #eef4fa; color: var(--ink); }
@@
-            .admin-search { position: static; flex-wrap: wrap; }
-            .admin-search input { flex-basis: calc(100% - 44px); }
+            .admin-search { position: static; flex-wrap: nowrap; padding: 10px 12px; }
+            .admin-search-meta { display: none; }
@@
-            <button type="button" onclick="clearAdminSearch()" title="Clear search"><i class="fas fa-times"></i></button>
+            <button type="button" id="adminSearchClear" onclick="clearAdminSearch()" title="Clear search" hidden><i class="fas fa-times"></i></button>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function applyAdminSearch() {
             var input = document.getElementById('adminSearchInput');
             var meta = document.getElementById('adminSearchMeta');
+            var clear = document.getElementById('adminSearchClear');
             var query = input ? input.value.trim().toLowerCase() : '';
             var activeTab = document.querySelector('.tab-content.active');
             var activeVisible = 0;
@@
             if (meta) {
                 meta.textContent = query ? activeVisible + '/' + activeTotal : '';
             }
+            if (clear) {
+                clear.hidden = query === '';
+            }
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['upload'])) {
     if (!canUpload()) { setFlash('You don\'t have upload permission', 'error'); header('Location: dashboard.php#add-file'); exit; }
     $uploadType  = $_POST['upload_type'] ?? '';
     $displayName = trim($_POST['display_name'] ?? '');
-    $brandId     = $_POST['brand_id'] ?? null;
-    $modelId     = $_POST['model_id'] ?? null;
+    $brandId     = (int)($_POST['brand_id'] ?? 0);
+    $modelIdRaw  = $_POST['model_id'] ?? '';
+    $modelId     = $modelIdRaw === '' ? null : (int)$modelIdRaw;
     $version     = $_POST['version'] ?? '1.0';
     $description = $_POST['description'] ?? '';
     $changelog   = $_POST['changelog'] ?? '';
     $systemType  = $_POST['system_type'] ?? null;
     if ($systemType === '') $systemType = null;
@@
-    $uploadSubdirs = ['config' => 'configs', 'firmware' => 'firmware', 'manual' => 'manuals', 'software' => 'software'];
+    $uploadSubdirs = ['config' => 'configs', 'firmware' => 'firmware', 'manual' => 'manuals', 'software' => 'software'];
     $tableMap = ['config' => 'config_files', 'firmware' => 'firmware_files', 'manual' => 'manuals', 'software' => 'software_files'];
+    $allowedExtsByType = [
+        'config' => ALLOWED_CONFIG_EXT,
+        'firmware' => ALLOWED_FIRMWARE_EXT,
+        'manual' => ALLOWED_MANUAL_EXT,
+        'software' => ALLOWED_SOFTWARE_EXT,
+    ];
 
     if (isset($uploadSubdirs[$uploadType]) && isset($_FILES['file']) && $_FILES['file']['error'] === UPLOAD_ERR_OK) {
         $file = $_FILES['file'];
         $ext = strtolower(pathinfo($file['name'], PATHINFO_EXTENSION));
-        $allowedExts = array_merge(ALLOWED_CONFIG_EXT, ALLOWED_FIRMWARE_EXT, ALLOWED_MANUAL_EXT, ALLOWED_SOFTWARE_EXT);
+        $allowedExts = $allowedExtsByType[$uploadType] ?? [];
 
-        if (!in_array($ext, $allowedExts)) { setFlash("File type .$ext not allowed", 'error'); header('Location: dashboard.php#add-file'); exit; }
+        if (!in_array($ext, $allowedExts, true)) {
+            setFlash("File type .$ext not allowed for " . $uploadType, 'error');
+            header('Location: dashboard.php#add-file');
+            exit;
+        }
         if ($file['size'] > MAX_FILE_SIZE) { setFlash('File exceeds max size', 'error'); header('Location: dashboard.php#add-file'); exit; }
 
         $subdir = $uploadSubdirs[$uploadType];
 
         // Validate required fields per type
-        if (!$brandId) { setFlash('Brand is required', 'error'); header('Location: dashboard.php#add-file'); exit; }
+        if (!$brandId) { setFlash('Brand is required', 'error'); header('Location: dashboard.php#add-file'); exit; }
         if ($uploadType === 'config' && !$modelId) { setFlash('Model is required for config files', 'error'); header('Location: dashboard.php#add-file'); exit; }
+        if ($uploadType !== 'config' && !$modelId) {
+            $modelId = null;
+        }
+        if ($modelId) {
+            $modelRow = $db->fetchOne("SELECT id, brand_id FROM device_models WHERE id = ?", [$modelId]);
+            if (!$modelRow) {
+                setFlash('Selected model does not exist', 'error');
+                header('Location: dashboard.php#add-file');
+                exit;
+            }
+            if ((int)$modelRow['brand_id'] !== (int)$brandId) {
+                setFlash('Selected model does not belong to selected brand', 'error');
+                header('Location: dashboard.php#add-file');
+                exit;
+            }
+        }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
+
+        /* ===== UPLOAD EXPERIENCE ===== */
+        .upload-hero {
+            position: relative;
+            border-radius: 12px;
+            overflow: hidden;
+            margin-bottom: 16px;
+            border: 1px solid rgba(0,90,160,0.12);
+            background: linear-gradient(135deg, #0b233f, #005aa0);
+            color: #fff;
+        }
+        .upload-hero-bg {
+            position: absolute;
+            inset: 0;
+            background: radial-gradient(circle at 80% 20%, rgba(255,255,255,0.2), transparent 45%);
+            pointer-events: none;
+        }
+        .upload-hero-content {
+            position: relative;
+            padding: 18px 20px;
+            display: flex;
+            align-items: center;
+            gap: 12px;
+        }
+        .upload-icon-wrap {
+            width: 40px;
+            height: 40px;
+            border-radius: 10px;
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            background: rgba(255,255,255,0.18);
+            font-size: 1.1rem;
+        }
+        .upload-hero h2 {
+            margin: 0;
+            font-size: 1.05rem;
+            font-weight: 700;
+        }
+        .upload-hero p {
+            margin: 3px 0 0;
+            font-size: 0.84rem;
+            opacity: 0.9;
+        }
+        .upload-layout {
+            display: grid;
+            grid-template-columns: minmax(260px, 0.9fr) minmax(320px, 1.1fr);
+            gap: 16px;
+        }
+        .upload-dropzone {
+            position: relative;
+            border: 2px dashed #c9d8ea;
+            border-radius: 12px;
+            background: linear-gradient(180deg, #f9fbff, #f4f8fd);
+            min-height: 230px;
+            display: flex;
+            align-items: center;
+            justify-content: center;
+            text-align: center;
+            cursor: pointer;
+            transition: border-color 0.2s, background 0.2s, box-shadow 0.2s;
+            overflow: hidden;
+        }
+        .upload-dropzone input[type="file"] {
+            position: absolute;
+            opacity: 0;
+            pointer-events: none;
+            width: 0;
+            height: 0;
+        }
+        .dropzone-glow {
+            position: absolute;
+            width: 220px;
+            height: 220px;
+            border-radius: 999px;
+            background: radial-gradient(circle, rgba(0,90,160,0.18), transparent 65%);
+            filter: blur(14px);
+            pointer-events: none;
+        }
+        .dropzone-content {
+            position: relative;
+            z-index: 1;
+            display: flex;
+            flex-direction: column;
+            align-items: center;
+            gap: 10px;
+            padding: 18px;
+        }
+        .dropzone-icon {
+            width: 56px;
+            height: 56px;
+            border-radius: 14px;
+            background: #e8f1fb;
+            color: var(--primary);
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            font-size: 1.3rem;
+        }
+        .dropzone-text {
+            display: flex;
+            flex-direction: column;
+            gap: 5px;
+        }
+        .dropzone-title {
+            font-size: 0.95rem;
+            font-weight: 700;
+            color: #1f2937;
+        }
+        .dropzone-link {
+            color: var(--primary);
+            text-decoration: underline;
+        }
+        .dropzone-hint {
+            color: var(--muted);
+            font-size: 0.78rem;
+        }
+        .upload-dropzone.dragover {
+            border-color: var(--primary);
+            background: #eef5fd;
+            box-shadow: 0 0 0 4px rgba(0,90,160,0.1);
+        }
+        .upload-dropzone.has-file {
+            border-color: #94a3b8;
+            background: #f8fafc;
+        }
+        .dropzone-preview {
+            position: relative;
+            z-index: 1;
+            display: flex;
+            align-items: center;
+            gap: 10px;
+            width: calc(100% - 20px);
+            max-width: 100%;
+            background: #fff;
+            border: 1px solid #dbe5f0;
+            border-radius: 10px;
+            padding: 10px 12px;
+            box-shadow: var(--shadow-sm);
+            text-align: left;
+        }
+        .file-preview-icon {
+            width: 40px;
+            height: 40px;
+            border-radius: 8px;
+            background: #edf4fb;
+            color: var(--primary);
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            font-size: 1.05rem;
+            flex: 0 0 auto;
+        }
+        .file-preview-info {
+            min-width: 0;
+            display: flex;
+            flex-direction: column;
+            gap: 4px;
+        }
+        .file-preview-name {
+            font-size: 0.84rem;
+            font-weight: 600;
+            color: #1f2937;
+            overflow: hidden;
+            text-overflow: ellipsis;
+            white-space: nowrap;
+        }
+        .file-preview-size {
+            font-size: 0.76rem;
+            color: var(--muted);
+        }
+        .file-preview-remove {
+            margin-left: auto;
+            width: 30px;
+            height: 30px;
+            border-radius: 8px;
+            border: none;
+            background: #fee2e2;
+            color: #b91c1c;
+            cursor: pointer;
+        }
+        .file-preview-remove:hover { background: #fecaca; }
+        .upload-fields {
+            display: flex;
+            flex-direction: column;
+            gap: 10px;
+        }
+        .upload-field {
+            display: flex;
+            flex-direction: column;
+            gap: 5px;
+        }
+        .upload-field label {
+            font-size: 0.81rem;
+            font-weight: 700;
+            color: #334155;
+            display: inline-flex;
+            align-items: center;
+            gap: 6px;
+        }
+        .upload-field-row {
+            display: grid;
+            grid-template-columns: 1fr 1fr;
+            gap: 10px;
+        }
+        .upload-field-action {
+            display: flex;
+            align-items: center;
+            gap: 6px;
+        }
+        .upload-field-action select {
+            flex: 1;
+            min-width: 0;
+        }
+        .btn-field-action {
+            width: 34px;
+            height: 34px;
+            border-radius: 8px;
+            border: 1px solid #cbd5e1;
+            background: #f8fafc;
+            color: #334155;
+            cursor: pointer;
+            flex: 0 0 auto;
+        }
+        .btn-field-action:hover {
+            border-color: var(--primary);
+            color: var(--primary);
+            background: #eff6ff;
+        }
+        .btn-upload-submit {
+            margin-top: 2px;
+            width: 100%;
+            padding: 11px 14px;
+            border: none;
+            border-radius: 10px;
+            cursor: pointer;
+            color: #fff;
+            font-weight: 700;
+            font-size: 0.9rem;
+            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
+            box-shadow: 0 8px 20px rgba(0,90,160,0.24);
+        }
+        .btn-upload-submit:disabled {
+            opacity: 0.7;
+            cursor: not-allowed;
+        }
+        .upload-inline-form {
+            margin-top: 12px;
+            border: 1px solid #dbe5f0;
+            background: #f8fbff;
+            border-radius: 10px;
+            padding: 12px;
+        }
+        .inline-form-row {
+            display: grid;
+            grid-template-columns: 1.2fr auto auto auto;
+            gap: 8px;
+            align-items: center;
+        }
+        .inline-form-row select,
+        .inline-form-row input[type="text"],
+        .inline-form-row input[type="color"] {
+            width: 100%;
+            min-width: 0;
+        }
 
         /* ===== ADMIN SEARCH ===== */
         .admin-search {
@@
             .admin-search { position: static; flex-wrap: nowrap; padding: 10px 14px; }
             .admin-search-meta { display: none; }
             .stat-card { padding: 14px; }
             .stat-card .number { font-size: 1.4rem; }
+            .upload-layout { grid-template-columns: 1fr; }
+            .upload-dropzone { min-height: 180px; }
+            .upload-field-row { grid-template-columns: 1fr; }
+            .inline-form-row { grid-template-columns: 1fr; }
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                        <input type="file" name="file" id="fileInput" required onchange="onFileSelect(this)">
+                        <input type="file" name="file" id="fileInput" accept=".cfg,.txt,.conf,.ini" required onchange="onFileSelect(this)">
@@
-                                <span class="dropzone-hint">Supports .cfg, .txt, .pdf, .bin, .fw, .zip, .exe and more</span>
+                                <span class="dropzone-hint" id="dropzoneHint">Allowed for Configuration File: .cfg, .txt, .conf, .ini</span>
@@
-                                <select name="system_type">
-                                    <option value="">â€” None â€”</option>
+                                <select name="system_type">
+                                    <option value="">- None -</option>
                                     <option value="advanced">Advanced Tracking</option>
                                     <option value="standard">Standard Tracking</option>
                                 </select>
@@
-                                <label><i class="fas fa-building"></i> Brand</label>
+                                <label><i class="fas fa-building"></i> Brand <span class="required">*</span></label>
                                 <div class="upload-field-action">
                                     <select name="brand_id" id="brandSelect" required>
-                                        <option value="">â€” Select â€”</option>
+                                        <option value="">- Select -</option>
                                         <?php foreach ($brands as $b): ?>
                                         <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                         <?php endforeach; ?>
@@
-                                <label><i class="fas fa-microchip"></i> Device Model</label>
+                                <label id="modelFieldLabel"><i class="fas fa-microchip"></i> Device Model <span class="required">*</span></label>
                                 <div class="upload-field-action">
-                                    <select name="model_id" id="modelSelect" required>
-                                        <option value="">â€” Select â€”</option>
+                                    <select name="model_id" id="modelSelect">
+                                        <option value="">- Select model -</option>
                                         <?php foreach ($brands as $b):
                                             $models = $db->getModelsByBrand($b['id']);
                                             foreach ($models as $m): ?>
@@
-                <?php if (canManageBrands()): ?>
+            </form>
+
+                <?php if (canManageBrands()): ?>
                 <div id="brandInlineForm" class="upload-inline-form" style="display:none">
                     <form method="POST">
                         <input type="hidden" name="create_brand" value="1">
                         <input type="hidden" name="return_to" value="dashboard.php#add-file">
                         <div class="inline-form-row">
                             <input type="text" name="brand_name" placeholder="Brand name" required>
@@
                 <div id="modelInlineForm" class="upload-inline-form" style="display:none">
                     <form method="POST">
                         <input type="hidden" name="create_model" value="1">
                         <input type="hidden" name="return_to" value="dashboard.php#add-file">
                         <div class="inline-form-row">
                             <select name="model_brand_id" required>
-                                <option value="">â€” Brand â€”</option>
+                                <option value="">- Brand -</option>
                                 <?php foreach ($brands as $b): ?>
                                 <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                 <?php endforeach; ?>
@@
                 </div>
                 <?php endif; ?>
-            </form>
             <script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                        <input type="file" name="file" id="fileInput" required onchange="onFileSelect(this)">
+                        <input type="file" name="file" id="fileInput" accept=".cfg,.txt,.conf,.ini" required onchange="onFileSelect(this)">
@@
-                                <span class="dropzone-hint">Supports .cfg, .txt, .pdf, .bin, .fw, .zip, .exe and more</span>
+                                <span class="dropzone-hint" id="dropzoneHint">Allowed for Configuration File: .cfg, .txt, .conf, .ini</span>
@@
-                                <select name="system_type">
-                                    <option value="">â€” None â€”</option>
+                                <select name="system_type">
+                                    <option value="">- None -</option>
                                     <option value="advanced">Advanced Tracking</option>
                                     <option value="standard">Standard Tracking</option>
                                 </select>
@@
-                                <label><i class="fas fa-building"></i> Brand</label>
+                                <label><i class="fas fa-building"></i> Brand <span class="required">*</span></label>
                                 <div class="upload-field-action">
                                     <select name="brand_id" id="brandSelect" required>
-                                        <option value="">â€” Select â€”</option>
+                                        <option value="">- Select -</option>
                                         <?php foreach ($brands as $b): ?>
                                         <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                         <?php endforeach; ?>
@@
-                                <label><i class="fas fa-microchip"></i> Device Model</label>
+                                <label id="modelFieldLabel"><i class="fas fa-microchip"></i> Device Model <span class="required">*</span></label>
                                 <div class="upload-field-action">
-                                    <select name="model_id" id="modelSelect" required>
-                                        <option value="">â€” Select â€”</option>
+                                    <select name="model_id" id="modelSelect">
+                                        <option value="">- Select model -</option>
                                         <?php foreach ($brands as $b):
                                             $models = $db->getModelsByBrand($b['id']);
                                             foreach ($models as $m): ?>
@@
                             <select name="model_brand_id" required>
-                                <option value="">â€” Brand â€”</option>
+                                <option value="">- Brand -</option>
                                 <?php foreach ($brands as $b): ?>
                                 <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                 <?php endforeach; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                        <input type="file" name="file" id="fileInput" required onchange="onFileSelect(this)">
+                        <input type="file" name="file" id="fileInput" accept=".cfg,.txt,.conf,.ini" required onchange="onFileSelect(this)">
@@
-                                <span class="dropzone-hint">Supports .cfg, .txt, .pdf, .bin, .fw, .zip, .exe and more</span>
+                                <span class="dropzone-hint" id="dropzoneHint">Allowed for Configuration File: .cfg, .txt, .conf, .ini</span>
@@
-                                <label><i class="fas fa-building"></i> Brand</label>
+                                <label><i class="fas fa-building"></i> Brand <span class="required">*</span></label>
@@
-                                <label><i class="fas fa-microchip"></i> Device Model</label>
+                                <label id="modelFieldLabel"><i class="fas fa-microchip"></i> Device Model <span class="required">*</span></label>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                    <select name="model_id" id="modelSelect" required>
+                                    <select name="model_id" id="modelSelect">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 <div class="upload-layout">
@@
                     </div>
                 </div>
+            </form>
 
-                <?php if (canManageBrands()): ?>
+                <?php if (canManageBrands()): ?>
                 <div id="brandInlineForm" class="upload-inline-form" style="display:none">
                     <form method="POST">
@@
                 </div>
                 <?php endif; ?>
-            </form>
             <script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-            <script>
-                function toggleUploadFields() {
-                    var type = document.getElementById('uploadType').value;
-                    document.getElementById('changelogField').style.display = type === 'firmware' ? 'block' : 'none';
-                }
-                toggleUploadFields();
-
-                // Drop zone
-                var dropzone = document.getElementById('uploadDropzone');
-                var fileInput = document.getElementById('fileInput');
-                ['dragenter','dragover'].forEach(function(e) {
-                    dropzone.addEventListener(e, function(ev) { ev.preventDefault(); dropzone.classList.add('dragover'); });
-                });
-                ['dragleave','drop'].forEach(function(e) {
-                    dropzone.addEventListener(e, function(ev) { ev.preventDefault(); dropzone.classList.remove('dragover'); });
-                });
-                dropzone.addEventListener('drop', function(ev) {
-                    if (ev.dataTransfer.files.length) {
-                        fileInput.files = ev.dataTransfer.files;
-                        onFileSelect(fileInput);
-                    }
-                });
-                dropzone.addEventListener('click', function() { fileInput.click(); });
-
-                function onFileSelect(input) {
-                    if (input.files && input.files[0]) {
-                        var f = input.files[0];
-                        document.getElementById('previewName').textContent = f.name;
-                        document.getElementById('previewSize').textContent = (f.size / 1024).toFixed(1) + ' KB';
-                        var icon = document.getElementById('previewIcon');
-                        var ext = f.name.split('.').pop().toLowerCase();
-                        var icons = {cfg:'fa-file-code',conf:'fa-file-code',txt:'fa-file-lines',pdf:'fa-file-pdf',bin:'fa-microchip',fw:'fa-microchip',hex:'fa-microchip',zip:'fa-file-zipper',exe:'fa-gear',msi:'fa-gear'};
-                        icon.innerHTML = '<i class="fas ' + (icons[ext]||'fa-file') + '"></i>';
-                        document.getElementById('dropzoneContent').style.display = 'none';
-                        document.getElementById('dropzonePreview').style.display = 'flex';
-                        dropzone.classList.add('has-file');
-                    }
-                }
-                function clearFileInput() {
-                    fileInput.value = '';
-                    document.getElementById('dropzoneContent').style.display = '';
-                    document.getElementById('dropzonePreview').style.display = 'none';
-                    dropzone.classList.remove('has-file');
-                }
-
-                // Upload spinner
-                document.getElementById('uploadForm').addEventListener('submit', function() {
-                    document.getElementById('uploadSubmitBtn').disabled = true;
-                    document.querySelector('.btn-upload-text').style.display = 'none';
-                    document.querySelector('.btn-upload-loader').style.display = 'inline';
-                });
-            </script>
+            <script>
+                var uploadTypeExtMap = {
+                    config: ['cfg', 'txt', 'conf', 'ini'],
+                    firmware: ['fw', 'bin', 'hex', 'dfu'],
+                    manual: ['pdf', 'doc', 'docx', 'txt'],
+                    software: ['exe', 'msi', 'zip', 'rar', '7z', 'gz', 'xim', 'cif']
+                };
+
+                function formatAcceptList(exts) {
+                    return exts.map(function(ext) { return '.' + ext; }).join(', ');
+                }
+
+                function syncModelRequirement() {
+                    var type = document.getElementById('uploadType').value;
+                    var modelSelect = document.getElementById('modelSelect');
+                    var modelLabel = document.getElementById('modelFieldLabel');
+                    var modelRequired = type === 'config';
+                    modelSelect.required = modelRequired;
+                    modelLabel.innerHTML = modelRequired
+                        ? '<i class="fas fa-microchip"></i> Device Model <span class="required">*</span>'
+                        : '<i class="fas fa-microchip"></i> Device Model (Optional)';
+                }
+
+                function syncUploadBrandModel() {
+                    var brandSelect = document.getElementById('brandSelect');
+                    var modelSelect = document.getElementById('modelSelect');
+                    if (!brandSelect || !modelSelect) return;
+
+                    var brandId = brandSelect.value;
+                    var selectedValid = false;
+                    Array.prototype.forEach.call(modelSelect.options, function(opt, idx) {
+                        if (idx === 0) return;
+                        var visible = !brandId || opt.getAttribute('data-brand') === brandId;
+                        opt.hidden = !visible;
+                        opt.disabled = !visible;
+                        if (visible && opt.selected) selectedValid = true;
+                    });
+                    if (!selectedValid) {
+                        modelSelect.value = '';
+                    }
+                }
+
+                function syncUploadAccept() {
+                    var type = document.getElementById('uploadType').value;
+                    var fileInput = document.getElementById('fileInput');
+                    var hint = document.getElementById('dropzoneHint');
+                    var exts = uploadTypeExtMap[type] || [];
+                    if (fileInput) fileInput.setAttribute('accept', formatAcceptList(exts));
+                    if (hint) {
+                        var typeLabel = type.charAt(0).toUpperCase() + type.slice(1);
+                        hint.textContent = exts.length
+                            ? 'Allowed for ' + typeLabel + ': ' + formatAcceptList(exts)
+                            : 'Allowed extensions will appear after selecting file type.';
+                    }
+                }
+
+                function toggleUploadFields() {
+                    var type = document.getElementById('uploadType').value;
+                    document.getElementById('changelogField').style.display = type === 'firmware' ? 'block' : 'none';
+                    syncModelRequirement();
+                    syncUploadBrandModel();
+                    syncUploadAccept();
+                }
+
+                toggleUploadFields();
+
+                document.getElementById('brandSelect').addEventListener('change', syncUploadBrandModel);
+
+                // Drop zone
+                var dropzone = document.getElementById('uploadDropzone');
+                var fileInput = document.getElementById('fileInput');
+                ['dragenter','dragover'].forEach(function(e) {
+                    dropzone.addEventListener(e, function(ev) { ev.preventDefault(); dropzone.classList.add('dragover'); });
+                });
+                ['dragleave','drop'].forEach(function(e) {
+                    dropzone.addEventListener(e, function(ev) { ev.preventDefault(); dropzone.classList.remove('dragover'); });
+                });
+                dropzone.addEventListener('drop', function(ev) {
+                    if (ev.dataTransfer.files.length) {
+                        fileInput.files = ev.dataTransfer.files;
+                        onFileSelect(fileInput);
+                    }
+                });
+                dropzone.addEventListener('click', function() { fileInput.click(); });
+
+                function onFileSelect(input) {
+                    if (input.files && input.files[0]) {
+                        var f = input.files[0];
+                        document.getElementById('previewName').textContent = f.name;
+                        document.getElementById('previewSize').textContent = (f.size / 1024).toFixed(1) + ' KB';
+                        var icon = document.getElementById('previewIcon');
+                        var ext = f.name.split('.').pop().toLowerCase();
+                        var icons = {cfg:'fa-file-code',conf:'fa-file-code',txt:'fa-file-lines',pdf:'fa-file-pdf',bin:'fa-microchip',fw:'fa-microchip',hex:'fa-microchip',zip:'fa-file-zipper',exe:'fa-gear',msi:'fa-gear'};
+                        icon.innerHTML = '<i class="fas ' + (icons[ext]||'fa-file') + '"></i>';
+                        document.getElementById('dropzoneContent').style.display = 'none';
+                        document.getElementById('dropzonePreview').style.display = 'flex';
+                        dropzone.classList.add('has-file');
+                    }
+                }
+                function clearFileInput() {
+                    fileInput.value = '';
+                    document.getElementById('dropzoneContent').style.display = '';
+                    document.getElementById('dropzonePreview').style.display = 'none';
+                    dropzone.classList.remove('has-file');
+                }
+
+                // Upload spinner + pre-submit validation
+                document.getElementById('uploadForm').addEventListener('submit', function(e) {
+                    var type = document.getElementById('uploadType').value;
+                    var brand = document.getElementById('brandSelect').value;
+                    var model = document.getElementById('modelSelect').value;
+                    if (!brand) {
+                        e.preventDefault();
+                        alert('Please select a brand.');
+                        return;
+                    }
+                    if (type === 'config' && !model) {
+                        e.preventDefault();
+                        alert('Please select a device model for configuration files.');
+                        return;
+                    }
+
+                    document.getElementById('uploadSubmitBtn').disabled = true;
+                    document.querySelector('.btn-upload-text').style.display = 'none';
+                    document.querySelector('.btn-upload-loader').style.display = 'inline';
+                });
+            </script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .stat-card .label {
             font-size: 0.76rem; color: var(--muted);
             margin-top: 6px; text-transform: uppercase;
             letter-spacing: 0.05em; font-weight: 600;
         }
+        .recent-grid {
+            display: grid;
+            grid-template-columns: 1fr 1fr;
+            gap: 14px;
+            margin-bottom: 18px;
+        }
+        .recent-list {
+            list-style: none;
+            margin: 0;
+            padding: 0;
+            display: flex;
+            flex-direction: column;
+            gap: 8px;
+        }
+        .recent-item {
+            border: 1px solid #e5ebf3;
+            border-radius: 8px;
+            padding: 8px 10px;
+            background: #fbfdff;
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            gap: 10px;
+        }
+        .recent-item-main {
+            min-width: 0;
+            display: flex;
+            flex-direction: column;
+            gap: 2px;
+        }
+        .recent-item-name {
+            font-size: 0.84rem;
+            font-weight: 600;
+            color: #1f2937;
+            white-space: nowrap;
+            overflow: hidden;
+            text-overflow: ellipsis;
+        }
+        .recent-item-meta {
+            font-size: 0.75rem;
+            color: var(--muted);
+            white-space: nowrap;
+            overflow: hidden;
+            text-overflow: ellipsis;
+        }
+        .recent-item-date {
+            font-size: 0.74rem;
+            color: #64748b;
+            white-space: nowrap;
+            flex: 0 0 auto;
+        }
@@
             .stat-card { padding: 14px; }
             .stat-card .number { font-size: 1.4rem; }
+            .recent-grid { grid-template-columns: 1fr; }
             .upload-layout { grid-template-columns: 1fr; }
             .upload-dropzone { min-height: 180px; }
             .upload-field-row { grid-template-columns: 1fr; }
             .inline-form-row { grid-template-columns: 1fr; }
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         <div class="stats-grid">
             <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
             <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
             <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
             <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
@@
             <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
             <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
         </div>
+        <div class="recent-grid">
+            <div class="section" style="margin-bottom:0">
+                <h2><i class="fas fa-clock"></i> Recent Config Uploads</h2>
+                <?php if (empty($recentConfigs)): ?>
+                    <div class="empty" style="padding:18px 10px">No recent config uploads.</div>
+                <?php else: ?>
+                    <ul class="recent-list">
+                        <?php foreach ($recentConfigs as $c): ?>
+                        <li class="recent-item">
+                            <div class="recent-item-main">
+                                <span class="recent-item-name"><?= escape($c['name']) ?></span>
+                                <span class="recent-item-meta"><?= escape($c['brand_name']) ?> / <?= escape($c['model_name']) ?> / v<?= escape($c['version']) ?></span>
+                            </div>
+                            <span class="recent-item-date"><?= date('Y-m-d', strtotime($c['created_at'])) ?></span>
+                        </li>
+                        <?php endforeach; ?>
+                    </ul>
+                <?php endif; ?>
+            </div>
+            <div class="section" style="margin-bottom:0">
+                <h2><i class="fas fa-clock"></i> Recent Firmware Uploads</h2>
+                <?php if (empty($recentFirmware)): ?>
+                    <div class="empty" style="padding:18px 10px">No recent firmware uploads.</div>
+                <?php else: ?>
+                    <ul class="recent-list">
+                        <?php foreach ($recentFirmware as $f): ?>
+                        <li class="recent-item">
+                            <div class="recent-item-main">
+                                <span class="recent-item-name"><?= escape($f['name']) ?></span>
+                                <span class="recent-item-meta"><?= escape($f['brand_name']) ?> / v<?= escape($f['version']) ?></span>
+                            </div>
+                            <span class="recent-item-date"><?= date('Y-m-d', strtotime($f['created_at'])) ?></span>
+                        </li>
+                        <?php endforeach; ?>
+                    </ul>
+                <?php endif; ?>
+            </div>
+        </div>
         <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function showInlineForm(id) {
-            document.getElementById(id).style.display = 'block';
+            var el = document.getElementById(id);
+            if (!el) return;
+            el.style.display = 'block';
+            if (id === 'modelInlineForm') {
+                var brandSelect = document.getElementById('brandSelect');
+                var inlineBrand = el.querySelector('select[name="model_brand_id"]');
+                if (brandSelect && inlineBrand && brandSelect.value) {
+                    inlineBrand.value = brandSelect.value;
+                }
+            }
         }
@@
             if (cb) {
                 const sel = document.getElementById('brandSelect');
                 if (sel) { sel.value = cb; showTab('add-file'); }
+                if (typeof syncUploadBrandModel === 'function') syncUploadBrandModel();
                 const url = new URL(location);
                 url.searchParams.delete('created_brand');
                 history.replaceState(null, '', url);
             }
             if (cm) {
                 const sel = document.getElementById('modelSelect');
                 if (sel) { sel.value = cm; showTab('add-file'); }
+                if (typeof syncUploadBrandModel === 'function') syncUploadBrandModel();
                 const url = new URL(location);
                 url.searchParams.delete('created_model');
                 history.replaceState(null, '', url);
             }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                    <option value="">â€” None â€”</option>
+                                    <option value="">- None -</option>
@@
-                                        <option value="">â€” Select â€”</option>
+                                        <option value="">- Select -</option>
@@
-                                        <option value="">â€” Select â€”</option>
+                                        <option value="">- Select model -</option>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             if (cm) {
                 const sel = document.getElementById('modelSelect');
-                if (sel) { sel.value = cm; showTab('add-file'); }
+                if (sel) {
+                    sel.value = cm;
+                    const selectedModel = sel.options[sel.selectedIndex];
+                    const modelBrand = selectedModel ? selectedModel.getAttribute('data-brand') : '';
+                    const brandSel = document.getElementById('brandSelect');
+                    if (brandSel && modelBrand) brandSel.value = modelBrand;
+                    showTab('add-file');
+                }
                 if (typeof syncUploadBrandModel === 'function') syncUploadBrandModel();
                 const url = new URL(location);
                 url.searchParams.delete('created_model');
                 history.replaceState(null, '', url);
             }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        <a href="dashboard.php" <?= isAdmin() ? 'class="active" id="nav-dashboard"' : '' ?>><i class="fas fa-chart-bar"></i> <?= isAdmin() ? 'Dashboard' : 'Overview' ?></a>
+        <a href="#" class="active" id="nav-dashboard" onclick="return showTab('overview')"><i class="fas fa-chart-bar"></i> <?= isAdmin() ? 'Dashboard' : 'Overview' ?></a>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        <?php if (isAdmin()): ?>
-        <div class="stats-grid">
-            <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
-            <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
-            <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
-            <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
-            <div class="stat-card"><div class="number"><?= $stats['manuals'] ?></div><div class="label">Manuals</div></div>
-            <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
-            <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
-        </div>
-        <div class="recent-grid">
-            <div class="section" style="margin-bottom:0">
-                <h2><i class="fas fa-clock"></i> Recent Config Uploads</h2>
-                <?php if (empty($recentConfigs)): ?>
-                    <div class="empty" style="padding:18px 10px">No recent config uploads.</div>
-                <?php else: ?>
-                    <ul class="recent-list">
-                        <?php foreach ($recentConfigs as $c): ?>
-                        <li class="recent-item">
-                            <div class="recent-item-main">
-                                <span class="recent-item-name"><?= escape($c['name']) ?></span>
-                                <span class="recent-item-meta"><?= escape($c['brand_name']) ?> / <?= escape($c['model_name']) ?> / v<?= escape($c['version']) ?></span>
-                            </div>
-                            <span class="recent-item-date"><?= date('Y-m-d', strtotime($c['created_at'])) ?></span>
-                        </li>
-                        <?php endforeach; ?>
-                    </ul>
-                <?php endif; ?>
-            </div>
-            <div class="section" style="margin-bottom:0">
-                <h2><i class="fas fa-clock"></i> Recent Firmware Uploads</h2>
-                <?php if (empty($recentFirmware)): ?>
-                    <div class="empty" style="padding:18px 10px">No recent firmware uploads.</div>
-                <?php else: ?>
-                    <ul class="recent-list">
-                        <?php foreach ($recentFirmware as $f): ?>
-                        <li class="recent-item">
-                            <div class="recent-item-main">
-                                <span class="recent-item-name"><?= escape($f['name']) ?></span>
-                                <span class="recent-item-meta"><?= escape($f['brand_name']) ?> / v<?= escape($f['version']) ?></span>
-                            </div>
-                            <span class="recent-item-date"><?= date('Y-m-d', strtotime($f['created_at'])) ?></span>
-                        </li>
-                        <?php endforeach; ?>
-                    </ul>
-                <?php endif; ?>
-            </div>
-        </div>
-        <?php endif; ?>
-
         <div class="admin-search">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         <div class="tab-bar">
-            <?php if (canUpload()): ?><button class="tab-btn active" onclick="showTab('add-file')">Upload File</button><?php endif; ?>
+            <button class="tab-btn active" onclick="showTab('overview')">Dashboard</button>
+            <?php if (canUpload()): ?><button class="tab-btn" onclick="showTab('add-file')">Upload File</button><?php endif; ?>
             <?php if (canViewTab('configs')): ?><button class="tab-btn" onclick="showTab('configs')">Configs</button><?php endif; ?>
             <?php if (canViewTab('firmware')): ?><button class="tab-btn" onclick="showTab('firmware')">Firmware</button><?php endif; ?>
             <?php if (canViewTab('manuals')): ?><button class="tab-btn" onclick="showTab('manuals')">Manuals</button><?php endif; ?>
             <?php if (canViewTab('software')): ?><button class="tab-btn" onclick="showTab('software')">Software</button><?php endif; ?>
             <?php if (canViewTab('brands-models')): ?><button class="tab-btn" onclick="showTab('brands-models')">Brands &amp; Models</button><?php endif; ?>
             <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')">Users</button><?php endif; ?>
         </div>
+
+        <div id="tab-overview" class="tab-content active">
+            <?php if (isAdmin()): ?>
+            <div class="stats-grid">
+                <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
+                <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
+                <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
+                <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
+                <div class="stat-card"><div class="number"><?= $stats['manuals'] ?></div><div class="label">Manuals</div></div>
+                <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
+                <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
+            </div>
+            <div class="recent-grid">
+                <div class="section" style="margin-bottom:0">
+                    <h2><i class="fas fa-clock"></i> Recent Config Uploads</h2>
+                    <?php if (empty($recentConfigs)): ?>
+                        <div class="empty" style="padding:18px 10px">No recent config uploads.</div>
+                    <?php else: ?>
+                        <ul class="recent-list">
+                            <?php foreach ($recentConfigs as $c): ?>
+                            <li class="recent-item">
+                                <div class="recent-item-main">
+                                    <span class="recent-item-name"><?= escape($c['name']) ?></span>
+                                    <span class="recent-item-meta"><?= escape($c['brand_name']) ?> / <?= escape($c['model_name']) ?> / v<?= escape($c['version']) ?></span>
+                                </div>
+                                <span class="recent-item-date"><?= date('Y-m-d', strtotime($c['created_at'])) ?></span>
+                            </li>
+                            <?php endforeach; ?>
+                        </ul>
+                    <?php endif; ?>
+                </div>
+                <div class="section" style="margin-bottom:0">
+                    <h2><i class="fas fa-clock"></i> Recent Firmware Uploads</h2>
+                    <?php if (empty($recentFirmware)): ?>
+                        <div class="empty" style="padding:18px 10px">No recent firmware uploads.</div>
+                    <?php else: ?>
+                        <ul class="recent-list">
+                            <?php foreach ($recentFirmware as $f): ?>
+                            <li class="recent-item">
+                                <div class="recent-item-main">
+                                    <span class="recent-item-name"><?= escape($f['name']) ?></span>
+                                    <span class="recent-item-meta"><?= escape($f['brand_name']) ?> / v<?= escape($f['version']) ?></span>
+                                </div>
+                                <span class="recent-item-date"><?= date('Y-m-d', strtotime($f['created_at'])) ?></span>
+                            </li>
+                            <?php endforeach; ?>
+                        </ul>
+                    <?php endif; ?>
+                </div>
+            </div>
+            <?php else: ?>
+            <div class="section">
+                <h2><i class="fas fa-chart-line"></i> Overview</h2>
+                <p style="font-size:0.9rem;color:var(--muted);line-height:1.5">Use the Upload tab to add files, then use the other tabs to manage, search, and edit them.</p>
+            </div>
+            <?php endif; ?>
+        </div>
 
         <?php if (canUpload()): ?>
-        <div id="tab-add-file" class="tab-content active">
+        <div id="tab-add-file" class="tab-content">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 	            <?php flash(); ?>
+
+	        <?php if (isAdmin()): ?>
+	        <div class="stats-grid">
+	            <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['manuals'] ?></div><div class="label">Manuals</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
+	        </div>
+	        <?php endif; ?>
 
 	        <div class="admin-search">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         <div id="tab-overview" class="tab-content active">
             <?php if (isAdmin()): ?>
-            <div class="stats-grid">
-                <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
-                <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
-                <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
-                <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
-                <div class="stat-card"><div class="number"><?= $stats['manuals'] ?></div><div class="label">Manuals</div></div>
-                <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
-                <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
-            </div>
             <div class="recent-grid">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             // If editing own profile, update session
             if ($uid === (int)($_SESSION['user_id'] ?? 0)) {
                 $_SESSION['user_username'] = $editUsername;
                 $_SESSION['user_image'] = $imagePath;
+                $_SESSION['user_role'] = $editRole;
+                unset($_SESSION['_perms']);
             }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 function flash() {
     if (!empty($_SESSION['flash'])) {
         $f = $_SESSION['flash'];
         unset($_SESSION['flash']);
         $cls = $f['type'] === 'success' ? 'flash-success' : 'flash-error';
         $icn = $f['type'] === 'success' ? 'check-circle' : 'exclamation-circle';
         echo '<div class="flash-msg ' . $cls . '"><i class="fas fa-' . $icn . '"></i> ' . escape($f['msg']) . '</div>';
     }
 }
+
+function removeUploadedFileByRelativePath($relativePath) {
+    if (!$relativePath) return;
+
+    $uploadsRoot = realpath(__DIR__ . '/../uploads');
+    if ($uploadsRoot === false) return;
+
+    $relative = ltrim(str_replace(['\\', '/'], DIRECTORY_SEPARATOR, $relativePath), DIRECTORY_SEPARATOR);
+    $absolute = __DIR__ . '/../' . $relative;
+    $absolute = str_replace(['\\', '/'], DIRECTORY_SEPARATOR, $absolute);
+
+    $uploadsRootNorm = rtrim(strtolower(str_replace('\\', '/', $uploadsRoot)), '/');
+    $absoluteNorm = strtolower(str_replace('\\', '/', $absolute));
+    if (strpos($absoluteNorm, $uploadsRootNorm . '/') !== 0) return;
+
+    if (file_exists($absolute) && is_file($absolute)) {
+        @unlink($absolute);
+    }
+}
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if (isset($_POST['bulk_delete']) && isset($_POST['type']) && isset($_POST['ids'])) {
@@
     foreach ($ids as $id) {
-        $file = $db->fetchOne("SELECT id FROM $table WHERE id = ?", [$id]);
+        $file = $db->fetchOne("SELECT id, file_path FROM $table WHERE id = ?", [$id]);
         if ($file) {
             $db->query("DELETE FROM $table WHERE id = ?", [$id]);
+            removeUploadedFileByRelativePath($file['file_path'] ?? null);
             $deleted++;
         }
     }
@@
 if (isset($_GET['delete']) && isset($_GET['type'])) {
@@
     if (isset($tableMap[$type])) {
-        $file = $db->fetchOne("SELECT name FROM {$tableMap[$type]} WHERE id = ?", [$id]);
+        $file = $db->fetchOne("SELECT name, file_path FROM {$tableMap[$type]} WHERE id = ?", [$id]);
         if ($file) {
             $db->query("DELETE FROM {$tableMap[$type]} WHERE id = ?", [$id]);
+            removeUploadedFileByRelativePath($file['file_path'] ?? null);
             $deleted = true;
             setFlash("Deleted " . $file['name']);
         }
@@
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['delete_brand'])) {
     if (!canManageBrands() || !canDelete()) { setFlash('You don\'t have permission to delete brands', 'error'); header('Location: dashboard.php#brands-models'); exit; }
     $brandId = (int)($_POST['brand_id'] ?? 0);
-    $brand = $db->fetchOne("SELECT name FROM brands WHERE id = ?", [$brandId]);
+    $brand = $db->fetchOne("SELECT name, image FROM brands WHERE id = ?", [$brandId]);
     if ($brand) {
+        $paths = [];
+        $cfg = $db->fetchAll("SELECT c.file_path FROM config_files c JOIN device_models dm ON c.device_model_id = dm.id WHERE dm.brand_id = ?", [$brandId]);
+        $other = $db->fetchAll(
+            "SELECT file_path FROM firmware_files WHERE brand_id = ?
+             UNION ALL SELECT file_path FROM manuals WHERE brand_id = ?
+             UNION ALL SELECT file_path FROM software_files WHERE brand_id = ?",
+            [$brandId, $brandId, $brandId]
+        );
+        foreach (array_merge($cfg, $other) as $row) {
+            if (!empty($row['file_path'])) $paths[] = $row['file_path'];
+        }
         $db->query("DELETE FROM brands WHERE id = ?", [$brandId]);
+        if (!empty($brand['image'])) removeUploadedFileByRelativePath($brand['image']);
+        foreach (array_unique($paths) as $path) removeUploadedFileByRelativePath($path);
         setFlash("Deleted brand " . $brand['name']);
     }
@@
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['delete_model'])) {
     if (!canManageModels() || !canDelete()) { setFlash('You don\'t have permission to delete models', 'error'); header('Location: dashboard.php#brands-models'); exit; }
     $modelId = (int)($_POST['model_id'] ?? 0);
     $model = $db->fetchOne("SELECT name FROM device_models WHERE id = ?", [$modelId]);
     if ($model) {
+        $paths = [];
+        $rows = $db->fetchAll(
+            "SELECT file_path FROM config_files WHERE device_model_id = ?
+             UNION ALL SELECT file_path FROM firmware_files WHERE device_model_id = ?
+             UNION ALL SELECT file_path FROM manuals WHERE device_model_id = ?
+             UNION ALL SELECT file_path FROM software_files WHERE device_model_id = ?",
+            [$modelId, $modelId, $modelId, $modelId]
+        );
+        foreach ($rows as $row) {
+            if (!empty($row['file_path'])) $paths[] = $row['file_path'];
+        }
         $db->query("DELETE FROM device_models WHERE id = ?", [$modelId]);
+        foreach (array_unique($paths) as $path) removeUploadedFileByRelativePath($path);
         setFlash("Deleted model " . $model['name']);
     }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 function removeUploadedFileByRelativePath($relativePath) {
     if (!$relativePath) return;
@@
-    $absolute = __DIR__ . '/../' . $relative;
-    $absolute = str_replace(['\\', '/'], DIRECTORY_SEPARATOR, $absolute);
+    $absolute = __DIR__ . '/../' . $relative;
+    $absolute = str_replace(['\\', '/'], DIRECTORY_SEPARATOR, $absolute);
+    $resolvedAbsolute = realpath($absolute);
+    if ($resolvedAbsolute !== false) {
+        $absolute = $resolvedAbsolute;
+    } else {
+        $resolvedDir = realpath(dirname($absolute));
+        if ($resolvedDir !== false) {
+            $absolute = $resolvedDir . DIRECTORY_SEPARATOR . basename($absolute);
+        }
+    }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             } elseif ($uploadType === 'software') {
                 $db->insert("INSERT INTO software_files (category, status, brand_id, device_model_id, name, system_type, file_path, file_size, version, description) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
                     ['software', 'active', $brandId, $modelId, $dbName, $systemType, $relativePath, $fileSize, $version, $description]);
             }
             setFlash("$displayName uploaded successfully");
-            $tab = $uploadType . 's';
+            $tabMap = ['config' => 'configs', 'firmware' => 'firmware', 'manual' => 'manuals', 'software' => 'software'];
+            $tab = $tabMap[$uploadType] ?? 'add-file';
             header("Location: dashboard.php#$tab");
             exit;
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 require_once __DIR__ . '/../config.php';
 require_once __DIR__ . '/../includes/database.php';
 require_once __DIR__ . '/../includes/functions.php';
 
-if (!canManageFiles()) {
-    header('Location: login.php');
-    exit;
-}
-
-$db = Database::getInstance();
-
 // Auto-migrate new permission columns on first admin visit
 if (isAdmin()) {
@@
 }
 
 // Handle logout
 if (isset($_GET['logout'])) {
     session_destroy();
-    header('Location: login.php');
+    header('Location: ../login.php');
+    exit;
+}
+
+if (!isLoggedIn()) {
+    header('Location: ../login.php?redirect=' . urlencode('/admin/dashboard.php'));
+    exit;
+}
+
+if (!canManageFiles()) {
+    header('Location: ../index.php');
     exit;
 }
 
+$db = Database::getInstance();
+
 // Handle user edit (admin only)
 if (isAdmin() && isset($_POST['edit_user'])) {
@@
             $newId = $db->insert("INSERT INTO users (username, password_hash, email, image, role, is_active) VALUES (?, ?, ?, ?, ?, 1)",
                 [$newUsername, $hash, $newEmail, $imagePath, $newRole]);
@@
             }
             setFlash("User '$newUsername' created");
+            header('Location: dashboard.php#users');
+            exit;
         }
     } else {
         $userError = 'Username and password are required';
@@
-                <form method="POST" enctype="multipart/form-data">
+                <form method="POST" action="dashboard.php" enctype="multipart/form-data">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 require_once __DIR__ . '/../config.php';
 require_once __DIR__ . '/../includes/database.php';
 require_once __DIR__ . '/../includes/functions.php';
 
+$db = Database::getInstance();
+
 // Auto-migrate new permission columns on first admin visit
 if (isAdmin()) {
@@
 if (!canManageFiles()) {
     header('Location: ../index.php');
     exit;
 }
-
-$db = Database::getInstance();
 
 // Handle user edit (admin only)
 if (isAdmin() && isset($_POST['edit_user'])) {
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if (!canManageFiles()) {
     header('Location: ../index.php');
     exit;
 }
 
+// Admin impersonation: sign in as any active user from Users tab
+if (isAdmin() && isset($_GET['impersonate_user'])) {
+    $targetUserId = (int)$_GET['impersonate_user'];
+    $targetUser = $targetUserId > 0
+        ? $db->fetchOne("SELECT id, username, role, image, is_active FROM users WHERE id = ?", [$targetUserId])
+        : null;
+
+    if ($targetUser && (int)($targetUser['is_active'] ?? 0) === 1) {
+        if (empty($_SESSION['_impersonator_id'])) {
+            $_SESSION['_impersonator_id'] = (int)($_SESSION['user_id'] ?? 0);
+            $_SESSION['_impersonator_username'] = $_SESSION['user_username'] ?? '';
+            $_SESSION['_impersonator_role'] = $_SESSION['user_role'] ?? '';
+            $_SESSION['_impersonator_image'] = $_SESSION['user_image'] ?? null;
+        }
+
+        $_SESSION['user_id'] = (int)$targetUser['id'];
+        $_SESSION['user_username'] = $targetUser['username'];
+        $_SESSION['user_role'] = $targetUser['role'];
+        $_SESSION['user_image'] = $targetUser['image'];
+        unset($_SESSION['_perms'], $_SESSION['_img_refreshed']);
+
+        $redirectTo = in_array($targetUser['role'], ['admin', 'editor'], true)
+            ? 'dashboard.php'
+            : '../index.php';
+        header('Location: ' . $redirectTo);
+        exit;
+    }
+
+    setFlash('Unable to sign in as selected user account', 'error');
+    header('Location: dashboard.php#users');
+    exit;
+}
+
 // Handle user edit (admin only)
 if (isAdmin() && isset($_POST['edit_user'])) {
@@
         <?php if (isAdmin()): ?><a href="#" onclick="return showTab('users')"><i class="fas fa-users"></i> Users</a><?php endif; ?>
         <span class="nav-spacer"></span>
         <a href="../index.php"><i class="fas fa-arrow-left"></i> View Site</a>
+        <?php if (!empty($_SESSION['_impersonator_id'])): ?>
+        <a href="../logout.php?switch_back=1"><i class="fas fa-rotate-left"></i> Back to Admin</a>
+        <?php endif; ?>
         <a href="?logout=1" class="logout"><i class="fas fa-sign-out-alt"></i> Sign Out</a>
     </div>
@@
                         <?php foreach ($allUsers as $u): ?>
                         <?php $roleColor = $u['role']==='admin'?'#005aa0':($u['role']==='editor'?'#00a86b':'#6c757d'); ?>
                         <?php $up = $permMap[$u['id']] ?? []; ?>
                         <?php $editPerms = ['can_upload'=>$up['can_upload']??0,'can_delete'=>$up['can_delete']??0,'can_manage_brands'=>$up['can_manage_brands']??0,'can_manage_models'=>$up['can_manage_models']??0,'can_view_configs'=>$up['can_view_configs']??0,'can_view_firmware'=>$up['can_view_firmware']??0,'can_view_manuals'=>$up['can_view_manuals']??0,'can_view_software'=>$up['can_view_software']??0,'can_view_brands_models'=>$up['can_view_brands_models']??0,'can_edit_files'=>$up['can_edit_files']??0]; ?>
                         <?php $editJson = htmlspecialchars(json_encode(['id'=>$u['id'],'username'=>$u['username'],'email'=>$u['email'] ?? '','role'=>$u['role'],'perms'=>$editPerms]), ENT_QUOTES, 'UTF-8'); ?>
+                        <?php $impersonateConfirm = json_encode('Sign in as ' . $u['username'] . '?'); ?>
                         <tr>
@@
                                 <button class="btn btn-sm btn-primary" onclick="openEditModalFromJson('<?= $editJson ?>')" title="Edit">
                                     <i class="fas fa-edit"></i>
                                 </button>
+                                <?php if ((int)$u['id'] !== (int)($_SESSION['user_id'] ?? 0) && (int)$u['is_active'] === 1): ?>
+                                <a href="?impersonate_user=<?= $u['id'] ?>" class="btn btn-sm btn-success" onclick='return confirm(<?= $impersonateConfirm ?>)' title="Sign in as this user">
+                                    <i class="fas fa-user-secret"></i> Login As
+                                </a>
+                                <?php endif; ?>
                                 <a href="?toggle_user=<?= $u['id'] ?>" class="btn btn-sm <?= $u['is_active'] ? 'btn-danger' : 'btn-primary' ?>" title="<?= $u['is_active'] ? 'Deactivate' : 'Activate' ?>">
                                     <i class="fas fa-<?= $u['is_active'] ? 'ban' : 'check' ?>"></i>
                                 </a>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if (isAdmin() && isset($_GET['delete_user'])) {
     $uid = (int)$_GET['delete_user'];
     $u = $db->fetchOne("SELECT role FROM users WHERE id = ?", [$uid]);
     if ($u && $u['role'] !== 'admin') {
         $db->query("DELETE FROM users WHERE id = ?", [$uid]);
@@
     header('Location: dashboard.php#users');
     exit;
 }
 
+// Handle bulk user management (admin only)
+if (isAdmin() && isset($_POST['bulk_user_manage'])) {
+    $action = $_POST['bulk_user_action'] ?? '';
+    $ids = array_values(array_unique(array_filter(array_map('intval', (array)($_POST['user_ids'] ?? [])))));
+    $allowedActions = ['activate', 'deactivate', 'delete'];
+
+    if (!in_array($action, $allowedActions, true)) {
+        setFlash('Select a valid bulk action', 'error');
+        header('Location: dashboard.php#users');
+        exit;
+    }
+
+    if (empty($ids)) {
+        setFlash('Select at least one user', 'error');
+        header('Location: dashboard.php#users');
+        exit;
+    }
+
+    $currentUserId = (int)($_SESSION['user_id'] ?? 0);
+    $changed = 0;
+    $skipped = 0;
+
+    foreach ($ids as $uid) {
+        if ($uid <= 0) {
+            $skipped++;
+            continue;
+        }
+
+        $u = $db->fetchOne("SELECT id, role, is_active FROM users WHERE id = ?", [$uid]);
+        if (!$u) {
+            $skipped++;
+            continue;
+        }
+
+        if ($uid === $currentUserId) {
+            $skipped++;
+            continue;
+        }
+
+        if ($action === 'activate') {
+            if (!(int)$u['is_active']) {
+                $db->query("UPDATE users SET is_active = 1 WHERE id = ?", [$uid]);
+                $changed++;
+            }
+            continue;
+        }
+
+        if ($action === 'deactivate') {
+            if ((int)$u['is_active']) {
+                $db->query("UPDATE users SET is_active = 0 WHERE id = ?", [$uid]);
+                $changed++;
+            }
+            continue;
+        }
+
+        if ($action === 'delete') {
+            if (($u['role'] ?? '') === 'admin') {
+                $skipped++;
+                continue;
+            }
+            $db->query("DELETE FROM users WHERE id = ?", [$uid]);
+            $changed++;
+        }
+    }
+
+    $labels = [
+        'activate' => 'activated',
+        'deactivate' => 'deactivated',
+        'delete' => 'deleted',
+    ];
+    $message = "Bulk action complete: $changed user(s) " . $labels[$action];
+    if ($skipped > 0) {
+        $message .= ", $skipped skipped";
+    }
+    setFlash($message, $changed > 0 ? 'success' : 'error');
+    header('Location: dashboard.php#users');
+    exit;
+}
+
+// Export users CSV (admin only)
+if (isAdmin() && isset($_GET['export_users'])) {
+    $rows = $db->fetchAll("SELECT id, username, email, role, is_active, created_at FROM users ORDER BY id ASC");
+    header('Content-Type: text/csv; charset=utf-8');
+    header('Content-Disposition: attachment; filename="users_export_' . date('Ymd_His') . '.csv"');
+    $out = fopen('php://output', 'w');
+    fputcsv($out, ['ID', 'Username', 'Email', 'Role', 'Status', 'Created At']);
+    foreach ($rows as $r) {
+        fputcsv($out, [
+            (int)$r['id'],
+            $r['username'] ?? '',
+            $r['email'] ?? '',
+            $r['role'] ?? '',
+            ((int)($r['is_active'] ?? 0) === 1 ? 'Active' : 'Inactive'),
+            $r['created_at'] ?? '',
+        ]);
+    }
+    fclose($out);
+    exit;
+}
+
 // Flash message helper
 function setFlash($msg, $type = 'success') {
     $_SESSION['flash'] = ['msg' => $msg, 'type' => $type];
 }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .btn-success {
             background: linear-gradient(135deg, var(--success), #008f5a);
             color: #fff; box-shadow: 0 4px 14px rgba(0,168,107,0.25);
         }
         .btn-success:hover { box-shadow: 0 6px 20px rgba(0,168,107,0.35); }
+
+        .users-toolbar {
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            gap: 10px;
+            margin: 10px 0 12px;
+            flex-wrap: wrap;
+        }
+        .users-toolbar-filters {
+            display: flex;
+            align-items: center;
+            gap: 8px;
+            flex-wrap: wrap;
+        }
+        .users-toolbar-filters label {
+            font-size: 0.78rem;
+            color: var(--muted);
+            font-weight: 600;
+        }
+        .users-toolbar-filters select {
+            padding: 7px 10px;
+            border: 1.5px solid var(--line);
+            border-radius: 8px;
+            font-size: 0.82rem;
+            min-width: 140px;
+            background: #fff;
+        }
+        .bulk-user-bar {
+            display: flex;
+            align-items: center;
+            gap: 8px;
+            margin: 0 0 12px;
+            padding: 10px 12px;
+            border-radius: 8px;
+            border: 1px solid var(--line);
+            background: #f8fafc;
+            flex-wrap: wrap;
+        }
+        .bulk-user-bar select {
+            padding: 7px 10px;
+            border: 1.5px solid var(--line);
+            border-radius: 8px;
+            font-size: 0.82rem;
+            min-width: 210px;
+            background: #fff;
+        }
+        .bulk-user-count {
+            font-size: 0.82rem;
+            font-weight: 700;
+            color: var(--muted);
+        }
 
         /* ===== FORMS ===== */
         .form-group { margin-bottom: 14px; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                         <div class="form-group">
                             <label>Password<span class="required">*</span></label>
                             <div style="display:flex;gap:8px">
                                 <input type="text" name="new_password" id="createPassword" required style="flex:1">
+                                <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="generateCreatePassword()" title="Generate password">
+                                    <i class="fas fa-key"></i>
+                                </button>
+                                <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="copyCreateCredentials()" title="Copy username and password">
+                                    <i class="fas fa-copy"></i>
+                                </button>
                             </div>
                         </div>
                     </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             <div class="section">
                 <h2><i class="fas fa-users"></i> All Users</h2>
+                <div class="users-toolbar">
+                    <div class="users-toolbar-filters">
+                        <label for="userRoleFilter">Role</label>
+                        <select id="userRoleFilter" onchange="applyAdminSearch()">
+                            <option value="">All Roles</option>
+                            <option value="admin">Admin</option>
+                            <option value="editor">Editor</option>
+                            <option value="user">User</option>
+                            <option value="viewer">Viewer</option>
+                        </select>
+                        <label for="userStatusFilter">Status</label>
+                        <select id="userStatusFilter" onchange="applyAdminSearch()">
+                            <option value="">All Status</option>
+                            <option value="active">Active</option>
+                            <option value="inactive">Inactive</option>
+                        </select>
+                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="resetUserFilters()">
+                            <i class="fas fa-rotate-left"></i> Reset
+                        </button>
+                    </div>
+                    <a href="dashboard.php?export_users=1" class="btn btn-sm btn-primary" title="Download users list as CSV">
+                        <i class="fas fa-file-csv"></i> Export CSV
+                    </a>
+                </div>
+                <form method="POST" action="dashboard.php#users" id="bulkUsersForm" onsubmit="return submitBulkUsersAction()">
+                    <input type="hidden" name="bulk_user_manage" value="1">
+                    <div id="bulkUserBar" class="bulk-user-bar" style="display:none">
+                        <span class="bulk-user-count" id="bulkUserCount">0 selected</span>
+                        <select name="bulk_user_action" id="bulkUserAction" required>
+                            <option value="">Choose action...</option>
+                            <option value="activate">Activate selected</option>
+                            <option value="deactivate">Deactivate selected</option>
+                            <option value="delete">Delete selected (non-admin only)</option>
+                        </select>
+                        <button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-bolt"></i> Apply</button>
+                    </div>
+                </form>
                 <?php
                 $allUsers = $db->fetchAll("SELECT id, username, email, image, role, is_active, created_at FROM users ORDER BY id ASC");
                 $allPerms = $db->fetchAll("SELECT * FROM user_permissions");
                 $permMap = [];
                 foreach ($allPerms as $p) $permMap[$p['user_id']] = $p;
                 if (empty($allUsers)): echo '<div class="empty">No users found.</div>';
                 else: ?>
                 <table>
-                    <thead><tr><th>ID</th><th>Image</th><th>Username</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead>
+                    <thead><tr><th style="width:34px"><input type="checkbox" id="selAllUsers" onchange="toggleAllUsers(this.checked)" title="Select all visible users"></th><th>ID</th><th>Image</th><th>Username</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead>
                     <tbody>
                         <?php foreach ($allUsers as $u): ?>
                         <?php $roleColor = $u['role']==='admin'?'#005aa0':($u['role']==='editor'?'#00a86b':'#6c757d'); ?>
                         <?php $up = $permMap[$u['id']] ?? []; ?>
                         <?php $editPerms = ['can_upload'=>$up['can_upload']??0,'can_delete'=>$up['can_delete']??0,'can_manage_brands'=>$up['can_manage_brands']??0,'can_manage_models'=>$up['can_manage_models']??0,'can_view_configs'=>$up['can_view_configs']??0,'can_view_firmware'=>$up['can_view_firmware']??0,'can_view_manuals'=>$up['can_view_manuals']??0,'can_view_software'=>$up['can_view_software']??0,'can_view_brands_models'=>$up['can_view_brands_models']??0,'can_edit_files'=>$up['can_edit_files']??0]; ?>
                         <?php $editJson = htmlspecialchars(json_encode(['id'=>$u['id'],'username'=>$u['username'],'email'=>$u['email'] ?? '','role'=>$u['role'],'perms'=>$editPerms]), ENT_QUOTES, 'UTF-8'); ?>
                         <?php $impersonateConfirm = json_encode('Sign in as ' . $u['username'] . '?'); ?>
-                        <tr>
+                        <?php $isCurrentUser = (int)$u['id'] === (int)($_SESSION['user_id'] ?? 0); ?>
+                        <tr class="user-row" data-user-role="<?= escape($u['role']) ?>" data-user-status="<?= $u['is_active'] ? 'active' : 'inactive' ?>">
+                            <td>
+                                <input
+                                    type="checkbox"
+                                    class="sel-user"
+                                    name="user_ids[]"
+                                    value="<?= (int)$u['id'] ?>"
+                                    form="bulkUsersForm"
+                                    onchange="checkBulkUsersBar()"
+                                    <?= $isCurrentUser ? 'disabled title="Current session user is excluded from bulk actions"' : '' ?>
+                                >
+                            </td>
                             <td><?= $u['id'] ?></td>
                             <td><?php if ($u['image']): ?><img src="../<?= escape($u['image']) ?>" alt="" style="width:36px;height:36px;border-radius:50%;object-fit:cover"><?php else: ?><span style="display:inline-flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:50%;background:#e0e0e0;color:#999;font-size:0.8rem"><i class="fas fa-user"></i></span><?php endif; ?></td>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function toggleEditPerms() {
             var sel = document.getElementById('editRole');
             var perms = document.getElementById('editPerms');
             perms.style.display = sel.value === 'editor' ? 'block' : 'none';
         }
+        function generateCreatePassword() {
+            var chars = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnpqrstuvwxyz23456789!@#$%';
+            var pwd = '';
+            for (var i = 0; i < 14; i++) {
+                pwd += chars.charAt(Math.floor(Math.random() * chars.length));
+            }
+            var input = document.getElementById('createPassword');
+            if (input) input.value = pwd;
+        }
+        function copyCreateCredentials() {
+            var user = document.querySelector('input[name=\"new_username\"]');
+            var pass = document.getElementById('createPassword');
+            var username = user ? user.value.trim() : '';
+            var password = pass ? pass.value.trim() : '';
+            if (!username || !password) {
+                alert('Enter username and password first.');
+                return;
+            }
+            var text = 'Username: ' + username + '\\nPassword: ' + password;
+            if (navigator.clipboard && navigator.clipboard.writeText) {
+                navigator.clipboard.writeText(text).then(function() {
+                    alert('Credentials copied.');
+                }).catch(function() {
+                    window.prompt('Copy credentials:', text);
+                });
+            } else {
+                window.prompt('Copy credentials:', text);
+            }
+        }
+        function toggleAllUsers(checked) {
+            var rows = document.querySelectorAll('#tab-users .user-row');
+            rows.forEach(function(row) {
+                var cb = row.querySelector('.sel-user');
+                if (!cb || cb.disabled) return;
+                if (row.style.display === 'none') return;
+                cb.checked = checked;
+            });
+            checkBulkUsersBar();
+        }
+        function checkBulkUsersBar() {
+            var bar = document.getElementById('bulkUserBar');
+            if (!bar) return;
+            var checked = document.querySelectorAll('.sel-user:checked');
+            var count = checked.length;
+            var countText = document.getElementById('bulkUserCount');
+            if (countText) countText.textContent = count + ' selected';
+            bar.style.display = count > 0 ? 'flex' : 'none';
+            var master = document.getElementById('selAllUsers');
+            if (master) master.checked = false;
+        }
+        function submitBulkUsersAction() {
+            var action = document.getElementById('bulkUserAction');
+            var checked = document.querySelectorAll('.sel-user:checked');
+            if (!action || !action.value) {
+                alert('Choose a bulk action.');
+                return false;
+            }
+            if (checked.length === 0) {
+                alert('Select at least one user.');
+                return false;
+            }
+            var msg = 'Apply "' + action.options[action.selectedIndex].text + '" to ' + checked.length + ' user(s)?';
+            return confirm(msg);
+        }
+        function resetUserFilters() {
+            var role = document.getElementById('userRoleFilter');
+            var status = document.getElementById('userStatusFilter');
+            if (role) role.value = '';
+            if (status) status.value = '';
+            applyAdminSearch();
+        }
         // Close modal on backdrop click
         document.getElementById('editUserModal').addEventListener('click', function(e) {
             if (e.target === this) closeEditModal();
         });
     </script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function adminTabNameFromControl(control) {
             var onclick = control.getAttribute('onclick') || '';
             var match = onclick.match(/showTab\('([^']+)'\)/);
             return match ? match[1] : '';
         }
+
+        function hasActiveUserFilters() {
+            var role = document.getElementById('userRoleFilter');
+            var status = document.getElementById('userStatusFilter');
+            return !!((role && role.value) || (status && status.value));
+        }
+
+        function userRowMatchesFilters(row) {
+            var role = document.getElementById('userRoleFilter');
+            var status = document.getElementById('userStatusFilter');
+            var roleFilter = role ? role.value : '';
+            var statusFilter = status ? status.value : '';
+            var rowRole = row.getAttribute('data-user-role') || '';
+            var rowStatus = row.getAttribute('data-user-status') || '';
+            if (roleFilter && rowRole !== roleFilter) return false;
+            if (statusFilter && rowStatus !== statusFilter) return false;
+            return true;
+        }
@@
                     var visible = 0;
 
                     rows.forEach(function(row) {
-                        var match = query === '' || adminRowSearchText(row).indexOf(query) !== -1;
+                        var textMatch = query === '' || adminRowSearchText(row).indexOf(query) !== -1;
+                        var userMatch = true;
+                        if (tabName === 'users' && row.classList.contains('user-row')) {
+                            userMatch = userRowMatchesFilters(row);
+                        }
+                        var match = textMatch && userMatch;
                         row.style.display = match ? '' : 'none';
                         if (match) visible++;
                     });
 
                     var emptyRow = ensureAdminSearchEmptyRow(table);
-                    if (emptyRow) emptyRow.style.display = query !== '' && visible === 0 ? '' : 'none';
+                    var shouldShowEmpty = visible === 0 && (query !== '' || (tabName === 'users' && hasActiveUserFilters()));
+                    if (emptyRow) emptyRow.style.display = shouldShowEmpty ? '' : 'none';
@@
             if (meta) {
-                meta.textContent = query ? activeVisible + '/' + activeTotal : '';
+                var usersTabActive = activeTab && activeTab.id === 'tab-users';
+                meta.textContent = (query || (usersTabActive && hasActiveUserFilters())) ? activeVisible + '/' + activeTotal : '';
             }
             if (clear) {
                 clear.hidden = query === '';
             }
+            checkBulkUsersBar();
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                             <div class="upload-field">
                                 <label><i class="fas fa-building"></i> Brand <span class="required">*</span></label>
                                 <div class="upload-field-action">
                                     <select name="brand_id" id="brandSelect" required>
                                         <option value="">â€” Select â€”</option>
@@
                                         <?php endforeach; ?>
                                     </select>
                                     <?php if (canManageBrands()): ?><button type="button" class="btn-field-action" onclick="showInlineForm('brandInlineForm')" title="New Brand"><i class="fas fa-plus"></i></button><?php endif; ?>
                                 </div>
+                                <?php if (canManageBrands()): ?>
+                                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
+                                    <form method="POST">
+                                        <input type="hidden" name="create_brand" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <input type="text" name="brand_name" placeholder="Brand name" required>
+                                            <input type="color" name="brand_color" value="#1a73e8">
+                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
+                                </div>
+                                <?php endif; ?>
                             </div>
                             <div class="upload-field">
                                 <label id="modelFieldLabel"><i class="fas fa-microchip"></i> Device Model <span class="required">*</span></label>
                                 <div class="upload-field-action">
                                     <select name="model_id" id="modelSelect">
@@
                                         <?php endforeach; endforeach; ?>
                                     </select>
                                     <?php if (canManageModels()): ?><button type="button" class="btn-field-action" onclick="showInlineForm('modelInlineForm')" title="New Model"><i class="fas fa-plus"></i></button><?php endif; ?>
                                 </div>
+                                <?php if (canManageModels()): ?>
+                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
+                                    <form method="POST">
+                                        <input type="hidden" name="create_model" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <select name="model_brand_id" required>
+                                                <option value="">â€” Brand â€”</option>
+                                                <?php foreach ($brands as $b): ?>
+                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                                <?php endforeach; ?>
+                                            </select>
+                                            <input type="text" name="model_name" placeholder="Model name" required>
+                                            <select name="model_system_type">
+                                                <option value="">System</option>
+                                                <option value="advanced">Advanced</option>
+                                                <option value="standard">Standard</option>
+                                            </select>
+                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
+                                </div>
+                                <?php endif; ?>
                             </div>
                         </div>
@@
             </form>
-
-                <?php if (canManageBrands()): ?>
-                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_brand" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <input type="text" name="brand_name" placeholder="Brand name" required>
-                            <input type="color" name="brand_color" value="#1a73e8">
-                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
-
-                <?php if (canManageModels()): ?>
-                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_model" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <select name="model_brand_id" required>
-                                <option value="">â€” Brand â€”</option>
-                                <?php foreach ($brands as $b): ?>
-                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                <?php endforeach; ?>
-                            </select>
-                            <input type="text" name="model_name" placeholder="Model name" required>
-                            <select name="model_system_type">
-                                <option value="">System</option>
-                                <option value="advanced">Advanced</option>
-                                <option value="standard">Standard</option>
-                            </select>
-                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
             <script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                 <div class="upload-field-action">
                                     <select name="brand_id" id="brandSelect" required>
                                         <option value="">â€” Select â€”</option>
                                         <?php foreach ($brands as $b): ?>
                                         <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                         <?php endforeach; ?>
                                     </select>
                                     <?php if (canManageBrands()): ?><button type="button" class="btn-field-action" onclick="showInlineForm('brandInlineForm')" title="New Brand"><i class="fas fa-plus"></i></button><?php endif; ?>
                                 </div>
+                                <?php if (canManageBrands()): ?>
+                                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
+                                    <form method="POST">
+                                        <input type="hidden" name="create_brand" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <input type="text" name="brand_name" placeholder="Brand name" required>
+                                            <input type="color" name="brand_color" value="#1a73e8">
+                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
+                                </div>
+                                <?php endif; ?>
                             </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                     <?php if (canManageBrands()): ?><button type="button" class="btn-field-action" onclick="showInlineForm('brandInlineForm')" title="New Brand"><i class="fas fa-plus"></i></button><?php endif; ?>
                                 </div>
+                                <?php if (canManageBrands()): ?>
+                                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
+                                    <form method="POST">
+                                        <input type="hidden" name="create_brand" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <input type="text" name="brand_name" placeholder="Brand name" required>
+                                            <input type="color" name="brand_color" value="#1a73e8">
+                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
+                                </div>
+                                <?php endif; ?>
                             </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                     <?php if (canManageModels()): ?><button type="button" class="btn-field-action" onclick="showInlineForm('modelInlineForm')" title="New Model"><i class="fas fa-plus"></i></button><?php endif; ?>
                                 </div>
+                                <?php if (canManageModels()): ?>
+                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
+                                    <form method="POST">
+                                        <input type="hidden" name="create_model" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <select name="model_brand_id" required>
+                                                <option value="">â€” Brand â€”</option>
+                                                <?php foreach ($brands as $b): ?>
+                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                                <?php endforeach; ?>
+                                            </select>
+                                            <input type="text" name="model_name" placeholder="Model name" required>
+                                            <select name="model_system_type">
+                                                <option value="">System</option>
+                                                <option value="advanced">Advanced</option>
+                                                <option value="standard">Standard</option>
+                                            </select>
+                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
+                                </div>
+                                <?php endif; ?>
                             </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             </form>
-
-                <?php if (canManageBrands()): ?>
-                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_brand" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <input type="text" name="brand_name" placeholder="Brand name" required>
-                            <input type="color" name="brand_color" value="#1a73e8">
-                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
-
-                <?php if (canManageModels()): ?>
-                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_model" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <select name="model_brand_id" required>
-                                <option value="">â€” Brand â€”</option>
-                                <?php foreach ($brands as $b): ?>
-                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                <?php endforeach; ?>
-                            </select>
-                            <input type="text" name="model_name" placeholder="Model name" required>
-                            <select name="model_system_type">
-                                <option value="">System</option>
-                                <option value="advanced">Advanced</option>
-                                <option value="standard">Standard</option>
-                            </select>
-                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
             <script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                <?php if (canManageBrands()): ?>
-                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_brand" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <input type="text" name="brand_name" placeholder="Brand name" required>
-                            <input type="color" name="brand_color" value="#1a73e8">
-                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
-
-                <?php if (canManageModels()): ?>
-                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_model" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <select name="model_brand_id" required>
-                                <option value="">â€” Brand â€”</option>
-                                <?php foreach ($brands as $b): ?>
-                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                <?php endforeach; ?>
-                            </select>
-                            <input type="text" name="model_name" placeholder="Model name" required>
-                            <select name="model_system_type">
-                                <option value="">System</option>
-                                <option value="advanced">Advanced</option>
-                                <option value="standard">Standard</option>
-                            </select>
-                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
             <script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
+                                                <option value="">- Brand -</option>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function showInlineForm(id) {
             var el = document.getElementById(id);
             if (!el) return;
+            document.querySelectorAll('.upload-inline-form').forEach(function(formBox) {
+                formBox.style.display = 'none';
+            });
             el.style.display = 'block';
             if (id === 'modelInlineForm') {
                 var brandSelect = document.getElementById('brandSelect');
                 var inlineBrand = el.querySelector('select[name="model_brand_id"]');
                 if (brandSelect && inlineBrand && brandSelect.value) {
                     inlineBrand.value = brandSelect.value;
                 }
             }
+            var firstInput = el.querySelector('input[type="text"], select');
+            if (firstInput) firstInput.focus();
         }
         function hideInlineForm(id) {
-            document.getElementById(id).style.display = 'none';
+            var el = document.getElementById(id);
+            if (!el) return;
+            el.style.display = 'none';
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                <?php if (canManageBrands()): ?>
+                <?php if (false && canManageBrands()): ?>
@@
-                <?php if (canManageModels()): ?>
+                <?php if (false && canManageModels()): ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                             <select name="model_brand_id" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
+                                                <option value="">- Brand -</option>
                                                 <?php foreach ($brands as $b): ?>
                                                 <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                                 <?php endforeach; ?>
                                             </select>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['create_brand']) && isset($_POST['brand_name']) && $_POST['brand_name'] !== '') {
     if (!canManageBrands()) { setFlash('You don\'t have permission to create brands', 'error'); header('Location: dashboard.php#add-file'); exit; }
     $brandName = trim($_POST['brand_name']);
     $brandColor = trim($_POST['brand_color'] ?? '#005aa0');
     if ($brandName) {
         $slug = slugify($brandName);
         $existing = $db->fetchOne("SELECT id FROM brands WHERE slug = ?", [$slug]);
-        if (!$existing) {
+        $return = $_POST['return_to'] ?? 'dashboard.php#add-file';
+        $sep = strpos($return, '?') === false ? '?' : '&';
+        if (!$existing) {
             $newId = $db->insert("INSERT INTO brands (name, slug, color) VALUES (?, ?, ?)", [$brandName, $slug, $brandColor]);
-            $return = $_POST['return_to'] ?? 'dashboard.php#add-file';
-            $sep = strpos($return, '?') === false ? '?' : '&';
             header("Location: $return{$sep}created_brand=$newId");
             exit;
         }
+        setFlash("Brand '$brandName' already exists; selected existing brand", 'error');
+        header("Location: $return{$sep}created_brand=" . (int)$existing['id']);
+        exit;
     }
     header('Location: dashboard.php#add-file');
     exit;
 }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                            <select name="model_brand_id" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
+                                            <select name="model_brand_id" required>
+                                                <option value="">- Brand -</option>
                                                 <?php foreach ($brands as $b): ?>
                                                 <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
                                                 <?php endforeach; ?>
                                             </select>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['create_brand']) && isset($_POST['brand_name']) && $_POST['brand_name'] !== '') {
     if (!canManageBrands()) { setFlash('You don\'t have permission to create brands', 'error'); header('Location: dashboard.php#add-file'); exit; }
     $brandName = trim($_POST['brand_name']);
     $brandColor = trim($_POST['brand_color'] ?? '#005aa0');
     if ($brandName) {
         $slug = slugify($brandName);
         $existing = $db->fetchOne("SELECT id FROM brands WHERE slug = ?", [$slug]);
-        $return = $_POST['return_to'] ?? 'dashboard.php#add-file';
-        $sep = strpos($return, '?') === false ? '?' : '&';
+        $return = $_POST['return_to'] ?? 'dashboard.php#add-file';
+        $anchor = '';
+        $hashPos = strpos($return, '#');
+        if ($hashPos !== false) {
+            $anchor = substr($return, $hashPos);
+            $return = substr($return, 0, $hashPos);
+        }
+        $sep = strpos($return, '?') === false ? '?' : '&';
         if (!$existing) {
             $newId = $db->insert("INSERT INTO brands (name, slug, color) VALUES (?, ?, ?)", [$brandName, $slug, $brandColor]);
-            header("Location: $return{$sep}created_brand=$newId");
+            header("Location: $return{$sep}created_brand=$newId$anchor");
             exit;
         }
         setFlash("Brand '$brandName' already exists; selected existing brand", 'error');
-        header("Location: $return{$sep}created_brand=" . (int)$existing['id']);
+        header("Location: $return{$sep}created_brand=" . (int)$existing['id'] . $anchor);
         exit;
     }
     header('Location: dashboard.php#add-file');
     exit;
 }
@@
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['create_model']) && isset($_POST['model_name']) && $_POST['model_name'] !== '') {
@@
         if (!$existing) {
             $newId = $db->insert("INSERT INTO device_models (brand_id, name, slug, system_type) VALUES (?, ?, ?, ?)", [$modelBrandId, $modelName, $slug, $modelSystemType]);
             $return = $_POST['return_to'] ?? 'dashboard.php#add-file';
+            $anchor = '';
+            $hashPos = strpos($return, '#');
+            if ($hashPos !== false) {
+                $anchor = substr($return, $hashPos);
+                $return = substr($return, 0, $hashPos);
+            }
             $sep = strpos($return, '?') === false ? '?' : '&';
-            header("Location: $return{$sep}created_model=$newId");
+            header("Location: $return{$sep}created_model=$newId$anchor");
             exit;
         }
     }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 function syncUploadBrandModel() {
                     var brandSelect = document.getElementById('brandSelect');
                     var modelSelect = document.getElementById('modelSelect');
                     if (!brandSelect || !modelSelect) return;
@@
                     if (!selectedValid) {
                         modelSelect.value = '';
                     }
                 }
+
+                function normalizeInlineBrandPlaceholders() {
+                    document.querySelectorAll('select[name="model_brand_id"]').forEach(function(sel) {
+                        if (!sel.options || !sel.options.length) return;
+                        var first = sel.options[0];
+                        first.value = '';
+                        first.text = '- Brand -';
+                        first.disabled = false;
+                    });
+                }
@@
                 toggleUploadFields();
+                normalizeInlineBrandPlaceholders();
 
                 document.getElementById('brandSelect').addEventListener('change', syncUploadBrandModel);
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             if (id === 'modelInlineForm') {
                 var brandSelect = document.getElementById('brandSelect');
                 var inlineBrand = el.querySelector('select[name="model_brand_id"]');
+                if (inlineBrand && inlineBrand.options && inlineBrand.options.length) {
+                    inlineBrand.options[0].value = '';
+                    inlineBrand.options[0].text = '- Brand -';
+                }
                 if (brandSelect && inlineBrand && brandSelect.value) {
                     inlineBrand.value = brandSelect.value;
                 }
             }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         /* ===== DELETE IMAGE CHECKBOX LABEL ===== */
         label.btn-danger.active { background: #c53030; }
 
+        /* ===== PREMIUM UI PASS ===== */
+        body {
+            background:
+                radial-gradient(1200px 420px at 80% -60px, rgba(0,90,160,0.16), transparent 65%),
+                radial-gradient(900px 340px at -12% -90px, rgba(0,168,107,0.1), transparent 68%),
+                var(--page);
+        }
+        .main {
+            position: relative;
+        }
+        .header {
+            border: 1px solid #dbe7f3;
+            border-radius: 14px;
+            padding: 16px 18px;
+            margin-bottom: 14px;
+            background: linear-gradient(135deg, #ffffff 0%, #f8fbff 48%, #f3fbf8 100%);
+            box-shadow: 0 10px 28px rgba(15, 23, 42, 0.08);
+        }
+        .header-left {
+            display: flex;
+            flex-direction: column;
+            gap: 4px;
+        }
+        .header-subtitle {
+            font-size: 0.84rem;
+            color: #64748b;
+        }
+        .header .user {
+            background: #fff;
+            border: 1px solid #dbe7f3;
+            border-radius: 999px;
+            padding: 5px 10px 5px 5px;
+            color: #334155;
+            font-weight: 600;
+            box-shadow: 0 4px 14px rgba(15,23,42,0.08);
+        }
+        .header-avatar {
+            width: 32px;
+            height: 32px;
+            border-radius: 50%;
+            object-fit: cover;
+            border: 1px solid #cfddee;
+            background: #e2e8f0;
+            color: #64748b;
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            flex-shrink: 0;
+        }
+        .dashboard-hero {
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            gap: 16px;
+            flex-wrap: wrap;
+            border: 1px solid #dbe6f3;
+            border-radius: 14px;
+            padding: 16px 18px;
+            margin-bottom: 16px;
+            background: linear-gradient(130deg, #0f1b2f 0%, #1a3555 52%, #0d7b61 100%);
+            color: #fff;
+            box-shadow: 0 16px 34px rgba(15,23,42,0.3);
+            position: relative;
+            overflow: hidden;
+        }
+        .dashboard-hero::after {
+            content: '';
+            position: absolute;
+            inset: 0;
+            background:
+                linear-gradient(115deg, rgba(255,255,255,0.13), transparent 34%),
+                repeating-linear-gradient(90deg, rgba(255,255,255,0.04) 0 1px, transparent 1px 36px);
+            pointer-events: none;
+        }
+        .dashboard-hero-copy, .dashboard-hero-actions { position: relative; z-index: 1; }
+        .dashboard-hero-copy h2 {
+            font-size: 1.28rem;
+            line-height: 1.2;
+            margin-bottom: 5px;
+        }
+        .dashboard-hero-copy p {
+            font-size: 0.87rem;
+            color: rgba(255,255,255,0.82);
+            max-width: 620px;
+        }
+        .dashboard-hero-chips {
+            margin-top: 10px;
+            display: flex;
+            align-items: center;
+            gap: 8px;
+            flex-wrap: wrap;
+        }
+        .dashboard-hero-chips span {
+            display: inline-flex;
+            align-items: center;
+            gap: 6px;
+            border: 1px solid rgba(255,255,255,0.23);
+            background: rgba(255,255,255,0.1);
+            padding: 6px 10px;
+            border-radius: 999px;
+            font-size: 0.75rem;
+            font-weight: 600;
+        }
+        .dashboard-hero-actions {
+            display: flex;
+            gap: 8px;
+            flex-wrap: wrap;
+            justify-content: flex-end;
+        }
+        .hero-btn {
+            display: inline-flex;
+            align-items: center;
+            gap: 6px;
+            border: 1px solid rgba(255,255,255,0.28);
+            border-radius: 9px;
+            padding: 8px 12px;
+            color: #fff;
+            background: rgba(255,255,255,0.09);
+            text-decoration: none;
+            font-size: 0.8rem;
+            font-weight: 700;
+            transition: all 0.2s;
+            cursor: pointer;
+        }
+        .hero-btn:hover {
+            background: rgba(255,255,255,0.18);
+            transform: translateY(-1px);
+        }
+        .stats-grid {
+            margin-bottom: 16px;
+        }
+        .stat-card {
+            border: 1px solid #dce7f4;
+            border-radius: 13px;
+            background: linear-gradient(155deg, #ffffff, #f8fbff);
+            box-shadow: 0 10px 24px rgba(15,23,42,0.08);
+        }
+        .stat-card::before {
+            height: 4px;
+            opacity: 0.75;
+        }
+        .stat-top {
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            gap: 8px;
+        }
+        .stat-icon {
+            width: 34px;
+            height: 34px;
+            border-radius: 10px;
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            background: #e9f2ff;
+            color: #005aa0;
+            font-size: 0.95rem;
+            box-shadow: inset 0 0 0 1px #d2e4fa;
+        }
+        .stat-card .label {
+            margin-top: 8px;
+        }
+        .tab-bar {
+            background: #ffffff;
+            border: 1px solid #dce7f4;
+            border-radius: 11px;
+            padding: 6px;
+            box-shadow: 0 8px 20px rgba(15,23,42,0.06);
+        }
+        .tab-btn {
+            border-color: transparent;
+        }
+        .tab-btn:not(.active) {
+            background: transparent;
+        }
+        .section {
+            border-color: #dce7f4;
+            border-radius: 13px;
+            box-shadow: 0 10px 24px rgba(15,23,42,0.08);
+        }
+
         /* ===== RESPONSIVE ===== */
         @media (max-width: 900px) {
             .sidebar {
                 position: static; width: auto; display: block;
                 padding: 0; flex-direction: row; flex-wrap: wrap;
             }
             .sidebar-brand { display: none; }
             .sidebar h2 { display: none; }
             .sidebar a { display: inline-flex; margin: 4px; }
             .sidebar .nav-spacer { display: none; }
             .main { margin-left: 0; padding: 16px; }
             .form-row { grid-template-columns: 1fr; gap: 0; }
             .header { align-items: flex-start; gap: 10px; flex-direction: column; }
+            .dashboard-hero { padding: 14px; }
+            .dashboard-hero-actions { justify-content: flex-start; width: 100%; }
             .admin-search { position: static; flex-wrap: nowrap; padding: 10px 14px; }
             .admin-search-meta { display: none; }
             .stat-card { padding: 14px; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .tab-btn {
             padding: 8px 16px;
             border: 1.5px solid var(--line);
             background: var(--panel);
             color: #4b5563;
             border-radius: 8px; cursor: pointer;
             font-size: 0.84rem; font-weight: 600;
             transition: all 0.2s;
         }
+        .tab-btn i {
+            margin-right: 6px;
+            font-size: 0.8rem;
+            opacity: 0.9;
+        }
         .tab-btn:hover {
             border-color: rgba(0,90,160,0.3);
             background: #f8fafc;
             transform: translateY(-1px);
         }
@@
-        .tab-content { display: none; }
-        .tab-content.active { display: block; }
+        .tab-content { display: none; }
+        .tab-content.active { display: block; animation: tabFadeIn 0.24s ease; }
+        @keyframes tabFadeIn {
+            from { opacity: 0; transform: translateY(4px); }
+            to { opacity: 1; transform: translateY(0); }
+        }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-    <div class="main">
-            <div class="header">
-                <h1><?= isAdmin() ? 'Dashboard' : 'File Manager' ?></h1>
-                <div class="user">
-                    <?php if (!empty($_SESSION['user_image'])): ?>
-                    <img src="../<?= escape($_SESSION['user_image']) ?>" alt="" style="width:32px;height:32px;border-radius:50%;object-fit:cover">
-                    <?php else: ?>
-                    <span style="display:inline-flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:50%;background:#e0e0e0;color:#999"><i class="fas fa-user"></i></span>
-                    <?php endif; ?>
-                    <?= escape($_SESSION['user_username']) ?> <span class="role-badge"><?= escape($_SESSION['user_role'] ?? 'unknown') ?></span>
-                </div>
-            </div>
-	            <?php flash(); ?>
-
-	        <?php if (isAdmin()): ?>
-	        <div class="stats-grid">
-	            <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
-	            <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
-	            <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
-	            <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
-	            <div class="stat-card"><div class="number"><?= $stats['manuals'] ?></div><div class="label">Manuals</div></div>
-	            <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
-	            <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
-	        </div>
-	        <?php endif; ?>
+    <div class="main">
+        <div class="header">
+            <div class="header-left">
+                <h1><?= isAdmin() ? 'Dashboard' : 'File Manager' ?></h1>
+                <div class="header-subtitle">Modern control center for uploads, access, and content lifecycle.</div>
+            </div>
+            <div class="user">
+                <?php if (!empty($_SESSION['user_image'])): ?>
+                <img src="../<?= escape($_SESSION['user_image']) ?>" alt="" class="header-avatar">
+                <?php else: ?>
+                <span class="header-avatar"><i class="fas fa-user"></i></span>
+                <?php endif; ?>
+                <?= escape($_SESSION['user_username']) ?> <span class="role-badge"><?= escape($_SESSION['user_role'] ?? 'unknown') ?></span>
+            </div>
+        </div>
+        <?php flash(); ?>
+
+        <div class="dashboard-hero">
+            <div class="dashboard-hero-copy">
+                <h2>Enterprise Operations Hub</h2>
+                <p>Manage fleet library content with clean workflows for uploads, approvals, search, and access control.</p>
+                <div class="dashboard-hero-chips">
+                    <?php if (isAdmin()): ?>
+                    <span><i class="fas fa-users"></i> <?= (int)$stats['users'] ?> Users</span>
+                    <span><i class="fas fa-database"></i> <?= (int)$stats['configs'] + (int)$stats['firmware'] + (int)$stats['manuals'] + (int)$stats['software'] ?> Files</span>
+                    <span><i class="fas fa-shield-alt"></i> Role Governance</span>
+                    <?php else: ?>
+                    <span><i class="fas fa-user-check"></i> Editor Workspace</span>
+                    <span><i class="fas fa-layer-group"></i> Multi-Tab Workflow</span>
+                    <?php endif; ?>
+                </div>
+            </div>
+            <div class="dashboard-hero-actions">
+                <?php if (canUpload()): ?><button type="button" class="hero-btn" onclick="return showTab('add-file')"><i class="fas fa-cloud-upload-alt"></i> Upload</button><?php endif; ?>
+                <?php if (canViewTab('brands-models')): ?><button type="button" class="hero-btn" onclick="return showTab('brands-models')"><i class="fas fa-sitemap"></i> Brands &amp; Models</button><?php endif; ?>
+                <?php if (isAdmin()): ?><button type="button" class="hero-btn" onclick="return showTab('users')"><i class="fas fa-users-cog"></i> Users</button><?php endif; ?>
+                <a href="../index.php" class="hero-btn"><i class="fas fa-globe"></i> View Site</a>
+            </div>
+        </div>
+
+        <?php if (isAdmin()): ?>
+        <div class="stats-grid">
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['brands'] ?></div><span class="stat-icon"><i class="fas fa-building"></i></span></div>
+                <div class="label">Brands</div>
+            </div>
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['models'] ?></div><span class="stat-icon"><i class="fas fa-microchip"></i></span></div>
+                <div class="label">Models</div>
+            </div>
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['configs'] ?></div><span class="stat-icon"><i class="fas fa-file-code"></i></span></div>
+                <div class="label">Configs</div>
+            </div>
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['firmware'] ?></div><span class="stat-icon"><i class="fas fa-microchip"></i></span></div>
+                <div class="label">Firmware</div>
+            </div>
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['manuals'] ?></div><span class="stat-icon"><i class="fas fa-book"></i></span></div>
+                <div class="label">Manuals</div>
+            </div>
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['software'] ?></div><span class="stat-icon"><i class="fas fa-cubes"></i></span></div>
+                <div class="label">Software</div>
+            </div>
+            <div class="stat-card">
+                <div class="stat-top"><div class="number"><?= $stats['users'] ?></div><span class="stat-icon"><i class="fas fa-users"></i></span></div>
+                <div class="label">Users</div>
+            </div>
+        </div>
+        <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         </div>
 
         <div class="tab-bar">
-            <button class="tab-btn active" onclick="showTab('overview')">Dashboard</button>
-            <?php if (canUpload()): ?><button class="tab-btn" onclick="showTab('add-file')">Upload File</button><?php endif; ?>
-            <?php if (canViewTab('configs')): ?><button class="tab-btn" onclick="showTab('configs')">Configs</button><?php endif; ?>
-            <?php if (canViewTab('firmware')): ?><button class="tab-btn" onclick="showTab('firmware')">Firmware</button><?php endif; ?>
-            <?php if (canViewTab('manuals')): ?><button class="tab-btn" onclick="showTab('manuals')">Manuals</button><?php endif; ?>
-            <?php if (canViewTab('software')): ?><button class="tab-btn" onclick="showTab('software')">Software</button><?php endif; ?>
-            <?php if (canViewTab('brands-models')): ?><button class="tab-btn" onclick="showTab('brands-models')">Brands &amp; Models</button><?php endif; ?>
-            <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')">Users</button><?php endif; ?>
+            <button class="tab-btn active" onclick="showTab('overview')"><i class="fas fa-chart-pie"></i>Dashboard</button>
+            <?php if (canUpload()): ?><button class="tab-btn" onclick="showTab('add-file')"><i class="fas fa-cloud-upload-alt"></i>Upload File</button><?php endif; ?>
+            <?php if (canViewTab('configs')): ?><button class="tab-btn" onclick="showTab('configs')"><i class="fas fa-file-code"></i>Configs</button><?php endif; ?>
+            <?php if (canViewTab('firmware')): ?><button class="tab-btn" onclick="showTab('firmware')"><i class="fas fa-microchip"></i>Firmware</button><?php endif; ?>
+            <?php if (canViewTab('manuals')): ?><button class="tab-btn" onclick="showTab('manuals')"><i class="fas fa-book-open"></i>Manuals</button><?php endif; ?>
+            <?php if (canViewTab('software')): ?><button class="tab-btn" onclick="showTab('software')"><i class="fas fa-cubes"></i>Software</button><?php endif; ?>
+            <?php if (canViewTab('brands-models')): ?><button class="tab-btn" onclick="showTab('brands-models')"><i class="fas fa-sitemap"></i>Brands &amp; Models</button><?php endif; ?>
+            <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')"><i class="fas fa-users-cog"></i>Users</button><?php endif; ?>
         </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .tab-btn {
             padding: 8px 16px;
             border: 1.5px solid var(--line);
             background: var(--panel);
             color: #4b5563;
             border-radius: 8px; cursor: pointer;
             font-size: 0.84rem; font-weight: 600;
             transition: all 0.2s;
         }
-        .tab-btn i {
-            margin-right: 6px;
-            font-size: 0.8rem;
-            opacity: 0.9;
-        }
         .tab-btn:hover {
             border-color: rgba(0,90,160,0.3);
             background: #f8fafc;
             transform: translateY(-1px);
         }
@@
 
         .tab-content { display: none; }
-        .tab-content.active { display: block; animation: tabFadeIn 0.24s ease; }
-        @keyframes tabFadeIn {
-            from { opacity: 0; transform: translateY(4px); }
-            to { opacity: 1; transform: translateY(0); }
-        }
+        .tab-content.active { display: block; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-    <div class="main">
-        <div class="header">
-            <div class="header-left">
-                <h1><?= isAdmin() ? 'Dashboard' : 'File Manager' ?></h1>
-                <div class="header-subtitle">Modern control center for uploads, access, and content lifecycle.</div>
-            </div>
-            <div class="user">
-                <?php if (!empty($_SESSION['user_image'])): ?>
-                <img src="../<?= escape($_SESSION['user_image']) ?>" alt="" class="header-avatar">
-                <?php else: ?>
-                <span class="header-avatar"><i class="fas fa-user"></i></span>
-                <?php endif; ?>
-                <?= escape($_SESSION['user_username']) ?> <span class="role-badge"><?= escape($_SESSION['user_role'] ?? 'unknown') ?></span>
-            </div>
-        </div>
-        <?php flash(); ?>
-
-        <div class="dashboard-hero">
-            <div class="dashboard-hero-copy">
-                <h2>Enterprise Operations Hub</h2>
-                <p>Manage fleet library content with clean workflows for uploads, approvals, search, and access control.</p>
-                <div class="dashboard-hero-chips">
-                    <?php if (isAdmin()): ?>
-                    <span><i class="fas fa-users"></i> <?= (int)$stats['users'] ?> Users</span>
-                    <span><i class="fas fa-database"></i> <?= (int)$stats['configs'] + (int)$stats['firmware'] + (int)$stats['manuals'] + (int)$stats['software'] ?> Files</span>
-                    <span><i class="fas fa-shield-alt"></i> Role Governance</span>
-                    <?php else: ?>
-                    <span><i class="fas fa-user-check"></i> Editor Workspace</span>
-                    <span><i class="fas fa-layer-group"></i> Multi-Tab Workflow</span>
-                    <?php endif; ?>
-                </div>
-            </div>
-            <div class="dashboard-hero-actions">
-                <?php if (canUpload()): ?><button type="button" class="hero-btn" onclick="return showTab('add-file')"><i class="fas fa-cloud-upload-alt"></i> Upload</button><?php endif; ?>
-                <?php if (canViewTab('brands-models')): ?><button type="button" class="hero-btn" onclick="return showTab('brands-models')"><i class="fas fa-sitemap"></i> Brands &amp; Models</button><?php endif; ?>
-                <?php if (isAdmin()): ?><button type="button" class="hero-btn" onclick="return showTab('users')"><i class="fas fa-users-cog"></i> Users</button><?php endif; ?>
-                <a href="../index.php" class="hero-btn"><i class="fas fa-globe"></i> View Site</a>
-            </div>
-        </div>
-
-        <?php if (isAdmin()): ?>
-        <div class="stats-grid">
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['brands'] ?></div><span class="stat-icon"><i class="fas fa-building"></i></span></div>
-                <div class="label">Brands</div>
-            </div>
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['models'] ?></div><span class="stat-icon"><i class="fas fa-microchip"></i></span></div>
-                <div class="label">Models</div>
-            </div>
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['configs'] ?></div><span class="stat-icon"><i class="fas fa-file-code"></i></span></div>
-                <div class="label">Configs</div>
-            </div>
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['firmware'] ?></div><span class="stat-icon"><i class="fas fa-microchip"></i></span></div>
-                <div class="label">Firmware</div>
-            </div>
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['manuals'] ?></div><span class="stat-icon"><i class="fas fa-book"></i></span></div>
-                <div class="label">Manuals</div>
-            </div>
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['software'] ?></div><span class="stat-icon"><i class="fas fa-cubes"></i></span></div>
-                <div class="label">Software</div>
-            </div>
-            <div class="stat-card">
-                <div class="stat-top"><div class="number"><?= $stats['users'] ?></div><span class="stat-icon"><i class="fas fa-users"></i></span></div>
-                <div class="label">Users</div>
-            </div>
-        </div>
-        <?php endif; ?>
+    <div class="main">
+            <div class="header">
+                <h1><?= isAdmin() ? 'Dashboard' : 'File Manager' ?></h1>
+                <div class="user">
+                    <?php if (!empty($_SESSION['user_image'])): ?>
+                    <img src="../<?= escape($_SESSION['user_image']) ?>" alt="" style="width:32px;height:32px;border-radius:50%;object-fit:cover">
+                    <?php else: ?>
+                    <span style="display:inline-flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:50%;background:#e0e0e0;color:#999"><i class="fas fa-user"></i></span>
+                    <?php endif; ?>
+                    <?= escape($_SESSION['user_username']) ?> <span class="role-badge"><?= escape($_SESSION['user_role'] ?? 'unknown') ?></span>
+                </div>
+            </div>
+	            <?php flash(); ?>
+
+	        <?php if (isAdmin()): ?>
+	        <div class="stats-grid">
+	            <div class="stat-card"><div class="number"><?= $stats['brands'] ?></div><div class="label">Brands</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['models'] ?></div><div class="label">Models</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['configs'] ?></div><div class="label">Configs</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['firmware'] ?></div><div class="label">Firmware</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['manuals'] ?></div><div class="label">Manuals</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['software'] ?></div><div class="label">Software</div></div>
+	            <div class="stat-card"><div class="number"><?= $stats['users'] ?></div><div class="label">Users</div></div>
+	        </div>
+	        <?php endif; ?>
@@
-        <div class="tab-bar">
-            <button class="tab-btn active" onclick="showTab('overview')"><i class="fas fa-chart-pie"></i>Dashboard</button>
-            <?php if (canUpload()): ?><button class="tab-btn" onclick="showTab('add-file')"><i class="fas fa-cloud-upload-alt"></i>Upload File</button><?php endif; ?>
-            <?php if (canViewTab('configs')): ?><button class="tab-btn" onclick="showTab('configs')"><i class="fas fa-file-code"></i>Configs</button><?php endif; ?>
-            <?php if (canViewTab('firmware')): ?><button class="tab-btn" onclick="showTab('firmware')"><i class="fas fa-microchip"></i>Firmware</button><?php endif; ?>
-            <?php if (canViewTab('manuals')): ?><button class="tab-btn" onclick="showTab('manuals')"><i class="fas fa-book-open"></i>Manuals</button><?php endif; ?>
-            <?php if (canViewTab('software')): ?><button class="tab-btn" onclick="showTab('software')"><i class="fas fa-cubes"></i>Software</button><?php endif; ?>
-            <?php if (canViewTab('brands-models')): ?><button class="tab-btn" onclick="showTab('brands-models')"><i class="fas fa-sitemap"></i>Brands &amp; Models</button><?php endif; ?>
-            <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')"><i class="fas fa-users-cog"></i>Users</button><?php endif; ?>
-        </div>
+        <div class="tab-bar">
+            <button class="tab-btn active" onclick="showTab('overview')">Dashboard</button>
+            <?php if (canUpload()): ?><button class="tab-btn" onclick="showTab('add-file')">Upload File</button><?php endif; ?>
+            <?php if (canViewTab('configs')): ?><button class="tab-btn" onclick="showTab('configs')">Configs</button><?php endif; ?>
+            <?php if (canViewTab('firmware')): ?><button class="tab-btn" onclick="showTab('firmware')">Firmware</button><?php endif; ?>
+            <?php if (canViewTab('manuals')): ?><button class="tab-btn" onclick="showTab('manuals')">Manuals</button><?php endif; ?>
+            <?php if (canViewTab('software')): ?><button class="tab-btn" onclick="showTab('software')">Software</button><?php endif; ?>
+            <?php if (canViewTab('brands-models')): ?><button class="tab-btn" onclick="showTab('brands-models')">Brands &amp; Models</button><?php endif; ?>
+            <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')">Users</button><?php endif; ?>
+        </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        /* ===== PREMIUM UI PASS ===== */
-        body {
-            background:
-                radial-gradient(1200px 420px at 80% -60px, rgba(0,90,160,0.16), transparent 65%),
-                radial-gradient(900px 340px at -12% -90px, rgba(0,168,107,0.1), transparent 68%),
-                var(--page);
-        }
-        .main {
-            position: relative;
-        }
-        .header {
-            border: 1px solid #dbe7f3;
-            border-radius: 14px;
-            padding: 16px 18px;
-            margin-bottom: 14px;
-            background: linear-gradient(135deg, #ffffff 0%, #f8fbff 48%, #f3fbf8 100%);
-            box-shadow: 0 10px 28px rgba(15, 23, 42, 0.08);
-        }
-        .header-left {
-            display: flex;
-            flex-direction: column;
-            gap: 4px;
-        }
-        .header-subtitle {
-            font-size: 0.84rem;
-            color: #64748b;
-        }
-        .header .user {
-            background: #fff;
-            border: 1px solid #dbe7f3;
-            border-radius: 999px;
-            padding: 5px 10px 5px 5px;
-            color: #334155;
-            font-weight: 600;
-            box-shadow: 0 4px 14px rgba(15,23,42,0.08);
-        }
-        .header-avatar {
-            width: 32px;
-            height: 32px;
-            border-radius: 50%;
-            object-fit: cover;
-            border: 1px solid #cfddee;
-            background: #e2e8f0;
-            color: #64748b;
-            display: inline-flex;
-            align-items: center;
-            justify-content: center;
-            flex-shrink: 0;
-        }
-        .dashboard-hero {
-            display: flex;
-            align-items: center;
-            justify-content: space-between;
-            gap: 16px;
-            flex-wrap: wrap;
-            border: 1px solid #dbe6f3;
-            border-radius: 14px;
-            padding: 16px 18px;
-            margin-bottom: 16px;
-            background: linear-gradient(130deg, #0f1b2f 0%, #1a3555 52%, #0d7b61 100%);
-            color: #fff;
-            box-shadow: 0 16px 34px rgba(15,23,42,0.3);
-            position: relative;
-            overflow: hidden;
-        }
-        .dashboard-hero::after {
-            content: '';
-            position: absolute;
-            inset: 0;
-            background:
-                linear-gradient(115deg, rgba(255,255,255,0.13), transparent 34%),
-                repeating-linear-gradient(90deg, rgba(255,255,255,0.04) 0 1px, transparent 1px 36px);
-            pointer-events: none;
-        }
-        .dashboard-hero-copy, .dashboard-hero-actions { position: relative; z-index: 1; }
-        .dashboard-hero-copy h2 {
-            font-size: 1.28rem;
-            line-height: 1.2;
-            margin-bottom: 5px;
-        }
-        .dashboard-hero-copy p {
-            font-size: 0.87rem;
-            color: rgba(255,255,255,0.82);
-            max-width: 620px;
-        }
-        .dashboard-hero-chips {
-            margin-top: 10px;
-            display: flex;
-            align-items: center;
-            gap: 8px;
-            flex-wrap: wrap;
-        }
-        .dashboard-hero-chips span {
-            display: inline-flex;
-            align-items: center;
-            gap: 6px;
-            border: 1px solid rgba(255,255,255,0.23);
-            background: rgba(255,255,255,0.1);
-            padding: 6px 10px;
-            border-radius: 999px;
-            font-size: 0.75rem;
-            font-weight: 600;
-        }
-        .dashboard-hero-actions {
-            display: flex;
-            gap: 8px;
-            flex-wrap: wrap;
-            justify-content: flex-end;
-        }
-        .hero-btn {
-            display: inline-flex;
-            align-items: center;
-            gap: 6px;
-            border: 1px solid rgba(255,255,255,0.28);
-            border-radius: 9px;
-            padding: 8px 12px;
-            color: #fff;
-            background: rgba(255,255,255,0.09);
-            text-decoration: none;
-            font-size: 0.8rem;
-            font-weight: 700;
-            transition: all 0.2s;
-            cursor: pointer;
-        }
-        .hero-btn:hover {
-            background: rgba(255,255,255,0.18);
-            transform: translateY(-1px);
-        }
-        .stats-grid {
-            margin-bottom: 16px;
-        }
-        .stat-card {
-            border: 1px solid #dce7f4;
-            border-radius: 13px;
-            background: linear-gradient(155deg, #ffffff, #f8fbff);
-            box-shadow: 0 10px 24px rgba(15,23,42,0.08);
-        }
-        .stat-card::before {
-            height: 4px;
-            opacity: 0.75;
-        }
-        .stat-top {
-            display: flex;
-            align-items: center;
-            justify-content: space-between;
-            gap: 8px;
-        }
-        .stat-icon {
-            width: 34px;
-            height: 34px;
-            border-radius: 10px;
-            display: inline-flex;
-            align-items: center;
-            justify-content: center;
-            background: #e9f2ff;
-            color: #005aa0;
-            font-size: 0.95rem;
-            box-shadow: inset 0 0 0 1px #d2e4fa;
-        }
-        .stat-card .label {
-            margin-top: 8px;
-        }
-        .tab-bar {
-            background: #ffffff;
-            border: 1px solid #dce7f4;
-            border-radius: 11px;
-            padding: 6px;
-            box-shadow: 0 8px 20px rgba(15,23,42,0.06);
-        }
-        .tab-btn {
-            border-color: transparent;
-        }
-        .tab-btn:not(.active) {
-            background: transparent;
-        }
-        .section {
-            border-color: #dce7f4;
-            border-radius: 13px;
-            box-shadow: 0 10px 24px rgba(15,23,42,0.08);
-        }
+        /* ===== POLISH PASS (existing UI only) ===== */
+        body {
+            background:
+                radial-gradient(1100px 380px at 82% -80px, rgba(0,90,160,0.12), transparent 62%),
+                radial-gradient(900px 340px at -10% -120px, rgba(0,168,107,0.09), transparent 66%),
+                var(--page);
+        }
+        .sidebar {
+            background: linear-gradient(180deg, #0f1a30 0%, #172746 55%, #10253f 100%);
+            border-right: 1px solid rgba(255,255,255,0.07);
+            box-shadow: 6px 0 30px rgba(15,23,42,0.24);
+        }
+        .sidebar a {
+            border: 1px solid transparent;
+            font-weight: 600;
+        }
+        .sidebar a:hover {
+            border-color: rgba(255,255,255,0.1);
+            transform: translateX(2px);
+        }
+        .sidebar a.active {
+            border-color: rgba(142,197,255,0.3);
+            background: linear-gradient(135deg, rgba(0,90,160,0.35), rgba(0,90,160,0.2));
+        }
+
+        .main { position: relative; }
+        .header {
+            border: 1px solid #dbe7f3;
+            border-radius: 14px;
+            padding: 14px 18px;
+            margin-bottom: 14px;
+            background: linear-gradient(135deg, #ffffff 0%, #f7fbff 52%, #f3fbf8 100%);
+            box-shadow: 0 10px 28px rgba(15, 23, 42, 0.08);
+        }
+        .header .user {
+            background: #fff;
+            border: 1px solid #dbe7f3;
+            border-radius: 999px;
+            padding: 5px 10px 5px 5px;
+            color: #334155;
+            font-weight: 600;
+            box-shadow: 0 4px 14px rgba(15,23,42,0.08);
+        }
+        .header .user img,
+        .header .user span {
+            border: 1px solid #d2dfef !important;
+        }
+
+        .stats-grid { margin-bottom: 16px; gap: 12px; }
+        .stat-card {
+            border: 1px solid #dce7f4;
+            border-radius: 13px;
+            background: linear-gradient(160deg, #ffffff, #f8fbff);
+            box-shadow: 0 10px 24px rgba(15,23,42,0.08);
+            padding: 16px 18px;
+        }
+        .stat-card::before { height: 4px; opacity: 0.78; }
+        .stat-card .number { font-size: 1.7rem; }
+        .stat-card .label { margin-top: 8px; letter-spacing: 0.06em; }
+
+        .admin-search {
+            background: #ffffff;
+            border-color: #d8e5f3;
+            border-radius: 11px;
+            box-shadow: 0 8px 20px rgba(15,23,42,0.06);
+        }
+        .admin-search input::placeholder { color: #94a3b8; }
+
+        .tab-bar {
+            background: #ffffff;
+            border: 1px solid #dce7f4;
+            border-radius: 11px;
+            padding: 6px;
+            box-shadow: 0 8px 20px rgba(15,23,42,0.06);
+        }
+        .tab-btn {
+            border-color: transparent;
+            border-radius: 9px;
+            font-weight: 700;
+        }
+        .tab-btn:not(.active) { background: transparent; }
+
+        .section {
+            border-color: #dce7f4;
+            border-radius: 13px;
+            box-shadow: 0 10px 24px rgba(15,23,42,0.08);
+        }
+        .section h2 {
+            font-size: 1.02rem;
+            margin-bottom: 12px;
+        }
+
+        thead th {
+            background: #f7fbff;
+            border-bottom-color: #dbe7f4;
+        }
+        tbody td { border-bottom-color: #edf2f8; }
+        tbody tr:hover td { background: #f8fbff; }
+
+        .btn { border-radius: 9px; font-weight: 700; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             .main { margin-left: 0; padding: 16px; }
             .form-row { grid-template-columns: 1fr; gap: 0; }
             .header { align-items: flex-start; gap: 10px; flex-direction: column; }
-            .dashboard-hero { padding: 14px; }
-            .dashboard-hero-actions { justify-content: flex-start; width: 100%; }
             .admin-search { position: static; flex-wrap: nowrap; padding: 10px 14px; }
             .admin-search-meta { display: none; }
             .stat-card { padding: 14px; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        <div class="tab-bar">
-            <button class="tab-btn active" onclick="showTab('overview')">Dashboard</button>
-            <?php if (canUpload()): ?><button class="tab-btn" onclick="showTab('add-file')">Upload File</button><?php endif; ?>
-            <?php if (canViewTab('configs')): ?><button class="tab-btn" onclick="showTab('configs')">Configs</button><?php endif; ?>
-            <?php if (canViewTab('firmware')): ?><button class="tab-btn" onclick="showTab('firmware')">Firmware</button><?php endif; ?>
-            <?php if (canViewTab('manuals')): ?><button class="tab-btn" onclick="showTab('manuals')">Manuals</button><?php endif; ?>
-            <?php if (canViewTab('software')): ?><button class="tab-btn" onclick="showTab('software')">Software</button><?php endif; ?>
-            <?php if (canViewTab('brands-models')): ?><button class="tab-btn" onclick="showTab('brands-models')">Brands &amp; Models</button><?php endif; ?>
-            <?php if (isAdmin()): ?><button class="tab-btn" onclick="showTab('users')">Users</button><?php endif; ?>
-        </div>
-
         <div id="tab-overview" class="tab-content active">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 function systemTypeBadge($type) {
     if (!$type) return '<span class="badge" style="background:#adb5bd;color:#fff;padding:2px 8px;border-radius:4px;font-size:0.75rem">None</span>';
     $color = $type === 'advanced' ? '#005aa0' : '#6c757d';
     $label = $type === 'advanced' ? 'Advanced' : 'Standard';
     return '<span class="badge" style="background:' . $color . ';color:#fff;padding:2px 8px;border-radius:4px;font-size:0.75rem">' . $label . '</span>';
 }
 
+function resolveAdminBrandImageSrc(array $brandRow) {
+    $stored = trim((string)($brandRow['image'] ?? ''));
+    if ($stored !== '') {
+        $relative = ltrim(str_replace('\\', '/', $stored), '/');
+        if (file_exists(__DIR__ . '/../' . $relative)) {
+            return '../' . $relative;
+        }
+    }
+
+    $slug = strtolower(trim((string)($brandRow['slug'] ?? '')));
+    if ($slug === '') return null;
+
+    $candidates = [
+        "assets/images/{$slug}_logo.svg",
+        "assets/images/{$slug}_logo.png",
+        "assets/images/{$slug}.png",
+        "assets/images/{$slug}.webp",
+        "assets/images/{$slug}.jpg",
+        "assets/images/{$slug}.jpeg",
+    ];
+
+    foreach ($candidates as $relative) {
+        if (file_exists(__DIR__ . '/../' . $relative)) {
+            return '../' . $relative;
+        }
+    }
+
+    return null;
+}
+
 $brands = $db->getBrands();
 $managedBrands = $db->fetchAll(
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                     <?php foreach ($managedBrands as $brandRow): ?>
                         <?php
                         $brandFormId = 'brandForm' . (int)$brandRow['id'];
                         $brandColor = preg_match('/^#[0-9a-fA-F]{6}$/', $brandRow['color'] ?? '') ? $brandRow['color'] : '#005aa0';
                         $brandConfirm = htmlspecialchars(json_encode('Delete brand "' . $brandRow['name'] . '"? This also removes related models and linked files.'), ENT_QUOTES, 'UTF-8');
-                        $brandImagePath = $brandRow['image'] ?? null;
+                        $brandStoredImage = trim((string)($brandRow['image'] ?? ''));
+                        $hasUploadedBrandImage = $brandStoredImage !== '' && file_exists(__DIR__ . '/../' . ltrim(str_replace('\\', '/', $brandStoredImage), '/'));
+                        $brandImagePath = resolveAdminBrandImageSrc($brandRow);
                         ?>
                         <tr>
                             <td style="text-align:center">
                                 <?php if ($brandImagePath): ?>
-                                <img src="../<?= escape($brandImagePath) ?>" alt="" class="brand-thumb" id="brandThumb<?= (int)$brandRow['id'] ?>">
+                                <img src="<?= escape($brandImagePath) ?>" alt="" class="brand-thumb" id="brandThumb<?= (int)$brandRow['id'] ?>">
                                 <?php else: ?>
                                 <div class="brand-thumb brand-thumb-placeholder" id="brandThumb<?= (int)$brandRow['id'] ?>">
                                     <i class="fas fa-satellite-dish"></i>
                                 </div>
                                 <?php endif; ?>
                                 <div style="margin-top:4px;display:flex;gap:4px;justify-content:center">
                                     <input type="file" name="brand_image" form="<?= $brandFormId ?>" accept="image/jpeg,image/png,image/gif,image/webp" style="width:70px;font-size:0.7rem" onchange="previewBrandImage(this, <?= (int)$brandRow['id'] ?>)">
-                                    <?php if ($brandImagePath): ?>
+                                    <?php if ($hasUploadedBrandImage): ?>
                                     <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Remove image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
                                         <input type="checkbox" name="delete_brand_image" value="1" form="<?= $brandFormId ?>" onchange="toggleRemoveImage(<?= (int)$brandRow['id'] ?>, this.checked)" style="display:none">
                                         <i class="fas fa-times"></i>
                                     </label>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 function resolveAdminBrandImageSrc(array $brandRow) {
     $stored = trim((string)($brandRow['image'] ?? ''));
     if ($stored !== '') {
         $relative = ltrim(str_replace('\\', '/', $stored), '/');
         if (file_exists(__DIR__ . '/../' . $relative)) {
             return '../' . $relative;
         }
     }
 
     $slug = strtolower(trim((string)($brandRow['slug'] ?? '')));
     if ($slug === '') return null;
+
+    // Built-in visual fallback for Dash Cam if no uploaded/admin image exists.
+    if ($slug === 'dash-cam') {
+        return '../assets/images/teltonika_device.jpg';
+    }
 
     $candidates = [
         "assets/images/{$slug}_logo.svg",
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 function removeUploadedFileByRelativePath($relativePath) {
@@
     if (file_exists($absolute) && is_file($absolute)) {
         @unlink($absolute);
     }
 }
+
+function clearBrandImagePath($relativePath) {
+    if (!$relativePath) return;
+    removeUploadedFileByRelativePath($relativePath);
+}
@@
-                if (isset($_POST['delete_brand_image']) && $currentImage) {
-                    $imgPath = __DIR__ . '/../' . $currentImage;
-                    if (file_exists($imgPath)) unlink($imgPath);
+                if (isset($_POST['delete_brand_image']) && $currentImage) {
+                    clearBrandImagePath($currentImage);
                     $currentImage = null;
                 }
@@
-                        if ($currentImage && file_exists(__DIR__ . '/../' . $currentImage)) {
-                            unlink(__DIR__ . '/../' . $currentImage);
-                        }
+                        if ($currentImage) clearBrandImagePath($currentImage);
                     }
                 }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                     <input type="file" name="brand_image" form="<?= $brandFormId ?>" accept="image/jpeg,image/png,image/gif,image/webp" style="width:70px;font-size:0.7rem" onchange="previewBrandImage(this, <?= (int)$brandRow['id'] ?>)">
                                     <?php if ($hasUploadedBrandImage): ?>
-                                    <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Remove image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
+                                    <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Delete uploaded image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
                                         <input type="checkbox" name="delete_brand_image" value="1" form="<?= $brandFormId ?>" onchange="toggleRemoveImage(<?= (int)$brandRow['id'] ?>, this.checked)" style="display:none">
-                                        <i class="fas fa-times"></i>
+                                        <i class="fas fa-trash"></i> Delete
                                     </label>
                                     <?php endif; ?>
                                 </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 function resolveAdminBrandImageSrc(array $brandRow) {
     $stored = trim((string)($brandRow['image'] ?? ''));
+    if ($stored === '__none__') {
+        return null;
+    }
     if ($stored !== '') {
         $relative = ltrim(str_replace('\\', '/', $stored), '/');
         if (file_exists(__DIR__ . '/../' . $relative)) {
             return '../' . $relative;
         }
     }
 
     $slug = strtolower(trim((string)($brandRow['slug'] ?? '')));
     if ($slug === '') return null;
-
-    // Built-in visual fallback for Dash Cam if no uploaded/admin image exists.
-    if ($slug === 'dash-cam') {
-        return '../assets/images/teltonika_device.jpg';
-    }
 
     $candidates = [
         "assets/images/{$slug}_logo.svg",
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 $current = $db->fetchOne("SELECT image FROM brands WHERE id = ?", [$brandId]);
                 $currentImage = $current['image'] ?? null;
 
                 // Handle image delete
-                if (isset($_POST['delete_brand_image']) && $currentImage) {
-                    clearBrandImagePath($currentImage);
-                    $currentImage = null;
+                if (isset($_POST['delete_brand_image'])) {
+                    if ($currentImage && $currentImage !== '__none__') {
+                        clearBrandImagePath($currentImage);
+                    }
+                    // Hide any fallback/default image until admin uploads a new one.
+                    $currentImage = '__none__';
                 }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                         $brandFormId = 'brandForm' . (int)$brandRow['id'];
                         $brandColor = preg_match('/^#[0-9a-fA-F]{6}$/', $brandRow['color'] ?? '') ? $brandRow['color'] : '#005aa0';
                         $brandConfirm = htmlspecialchars(json_encode('Delete brand "' . $brandRow['name'] . '"? This also removes related models and linked files.'), ENT_QUOTES, 'UTF-8');
                         $brandStoredImage = trim((string)($brandRow['image'] ?? ''));
                         $hasUploadedBrandImage = $brandStoredImage !== '' && file_exists(__DIR__ . '/../' . ltrim(str_replace('\\', '/', $brandStoredImage), '/'));
                         $brandImagePath = resolveAdminBrandImageSrc($brandRow);
+                        $hasVisibleBrandImage = !empty($brandImagePath);
                         ?>
@@
                                     <input type="file" name="brand_image" form="<?= $brandFormId ?>" accept="image/jpeg,image/png,image/gif,image/webp" style="width:70px;font-size:0.7rem" onchange="previewBrandImage(this, <?= (int)$brandRow['id'] ?>)">
-                                    <?php if ($hasUploadedBrandImage): ?>
+                                    <?php if ($hasVisibleBrandImage): ?>
                                     <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Delete uploaded image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
                                         <input type="checkbox" name="delete_brand_image" value="1" form="<?= $brandFormId ?>" onchange="toggleRemoveImage(<?= (int)$brandRow['id'] ?>, this.checked)" style="display:none">
                                         <i class="fas fa-trash"></i> Delete
                                     </label>
                                     <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                    <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Delete uploaded image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
+                                    <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Delete image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                         $brandFormId = 'brandForm' . (int)$brandRow['id'];
                         $brandColor = preg_match('/^#[0-9a-fA-F]{6}$/', $brandRow['color'] ?? '') ? $brandRow['color'] : '#005aa0';
                         $brandConfirm = htmlspecialchars(json_encode('Delete brand "' . $brandRow['name'] . '"? This also removes related models and linked files.'), ENT_QUOTES, 'UTF-8');
-                        $brandStoredImage = trim((string)($brandRow['image'] ?? ''));
-                        $hasUploadedBrandImage = $brandStoredImage !== '' && file_exists(__DIR__ . '/../' . ltrim(str_replace('\\', '/', $brandStoredImage), '/'));
                         $brandImagePath = resolveAdminBrandImageSrc($brandRow);
                         $hasVisibleBrandImage = !empty($brandImagePath);
                         ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                    <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Delete image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
+                                    <label class="btn btn-sm btn-danger" style="font-size:0.65rem;padding:2px 6px;cursor:pointer;transition:all 0.2s" title="Remove image" id="delImgLabel<?= (int)$brandRow['id'] ?>">
                                         <input type="checkbox" name="delete_brand_image" value="1" form="<?= $brandFormId ?>" onchange="toggleRemoveImage(<?= (int)$brandRow['id'] ?>, this.checked)" style="display:none">
-                                        <i class="fas fa-trash"></i> Delete
+                                        <i class="fas fa-times"></i>
                                     </label>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                <label><i class="fas fa-tag"></i> File Type</label>
+                                <label><i class="fas fa-file-lines"></i> File Type</label>
@@
-                                <label><i class="fas fa-layer-group"></i> System Type</label>
+                                <label><i class="fas fa-server"></i> System Type</label>
@@
-                            <label><i class="fas fa-heading"></i> Display Name</label>
+                            <label><i class="fas fa-pen-to-square"></i> Display Name</label>
@@
-                                <label><i class="fas fa-building"></i> Brand <span class="required">*</span></label>
+                                <label><i class="fas fa-industry"></i> Brand <span class="required">*</span></label>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-    <div class="main">
-            <div class="header">
-                <h1><?= isAdmin() ? 'Dashboard' : 'File Manager' ?></h1>
-                <div class="user">
-                    <?php if (!empty($_SESSION['user_image'])): ?>
-                    <img src="../<?= escape($_SESSION['user_image']) ?>" alt="" style="width:32px;height:32px;border-radius:50%;object-fit:cover">
-                    <?php else: ?>
-                    <span style="display:inline-flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:50%;background:#e0e0e0;color:#999"><i class="fas fa-user"></i></span>
-                    <?php endif; ?>
-                    <?= escape($_SESSION['user_username']) ?> <span class="role-badge"><?= escape($_SESSION['user_role'] ?? 'unknown') ?></span>
-                </div>
-            </div>
-	            <?php flash(); ?>
+    <div class="main">
+            <?php if (isAdmin()): ?>
+            <div class="header">
+                <h1><?= isAdmin() ? 'Dashboard' : 'File Manager' ?></h1>
+                <div class="user">
+                    <?php if (!empty($_SESSION['user_image'])): ?>
+                    <img src="../<?= escape($_SESSION['user_image']) ?>" alt="" style="width:32px;height:32px;border-radius:50%;object-fit:cover">
+                    <?php else: ?>
+                    <span style="display:inline-flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:50%;background:#e0e0e0;color:#999"><i class="fas fa-user"></i></span>
+                    <?php endif; ?>
+                    <?= escape($_SESSION['user_username']) ?> <span class="role-badge"><?= escape($_SESSION['user_role'] ?? 'unknown') ?></span>
+                </div>
+            </div>
+            <?php endif; ?>
+	            <?php flash(); ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .btn { border-radius: 9px; font-weight: 700; }
+
+        /* ===== ENTERPRISE MODERN BOOST ===== */
+        .main {
+            isolation: isolate;
+        }
+        .main::before {
+            content: '';
+            position: fixed;
+            right: -120px;
+            top: -90px;
+            width: 380px;
+            height: 380px;
+            border-radius: 50%;
+            background: radial-gradient(circle, rgba(0, 90, 160, 0.13), transparent 66%);
+            pointer-events: none;
+            z-index: -1;
+        }
+        .main::after {
+            content: '';
+            position: fixed;
+            left: 220px;
+            bottom: -170px;
+            width: 420px;
+            height: 420px;
+            border-radius: 50%;
+            background: radial-gradient(circle, rgba(0, 168, 107, 0.1), transparent 68%);
+            pointer-events: none;
+            z-index: -1;
+        }
+
+        .sidebar::before {
+            content: '';
+            position: sticky;
+            top: 0;
+            height: 1px;
+            width: 100%;
+            display: block;
+            background: linear-gradient(90deg, transparent, rgba(142,197,255,0.65), transparent);
+            z-index: 1;
+        }
+        .sidebar a {
+            position: relative;
+            overflow: hidden;
+        }
+        .sidebar a::after {
+            content: '';
+            position: absolute;
+            left: 0;
+            top: 7px;
+            bottom: 7px;
+            width: 2px;
+            border-radius: 999px;
+            background: transparent;
+            transition: all 0.2s ease;
+        }
+        .sidebar a.active::after {
+            background: #8ec5ff;
+        }
+
+        .header {
+            position: sticky;
+            top: 10px;
+            z-index: 8;
+            backdrop-filter: blur(10px);
+            -webkit-backdrop-filter: blur(10px);
+            background: linear-gradient(135deg, rgba(255,255,255,0.92) 0%, rgba(247,251,255,0.94) 52%, rgba(243,251,248,0.92) 100%);
+        }
+        .header h1 {
+            letter-spacing: 0;
+        }
+
+        .stat-card {
+            position: relative;
+            overflow: hidden;
+        }
+        .stat-card .number {
+            color: var(--primary);
+            background: linear-gradient(135deg, #0b3a66, #005aa0 45%, #1788d7);
+            -webkit-background-clip: text;
+            -webkit-text-fill-color: transparent;
+            background-clip: text;
+        }
+        .stat-card:hover {
+            transform: translateY(-4px) scale(1.01);
+        }
+
+        .admin-search {
+            top: 78px;
+            backdrop-filter: blur(8px);
+            -webkit-backdrop-filter: blur(8px);
+            background: rgba(255,255,255,0.92);
+        }
+        .admin-search input {
+            font-size: 0.93rem;
+        }
+
+        .tab-content.active {
+            animation: tabFadeIn 0.2s ease;
+        }
+        @keyframes tabFadeIn {
+            from { opacity: 0; transform: translateY(6px); }
+            to { opacity: 1; transform: translateY(0); }
+        }
+
+        .section {
+            position: relative;
+            overflow: hidden;
+        }
+        .section::before {
+            content: '';
+            position: absolute;
+            left: 0;
+            right: 0;
+            top: 0;
+            height: 2px;
+            background: linear-gradient(90deg, rgba(0,90,160,0.7), rgba(59,130,246,0.42), transparent);
+            pointer-events: none;
+        }
+
+        table {
+            border-radius: 10px;
+            overflow: hidden;
+        }
+        tbody tr:nth-child(even) td {
+            background: #fbfdff;
+        }
+        tbody tr:hover td {
+            background: #f1f7ff;
+        }
+        tbody td {
+            font-size: 0.83rem;
+        }
+
+        .btn {
+            border: 1px solid transparent;
+            transition: transform 0.2s ease, box-shadow 0.2s ease, filter 0.2s ease, border-color 0.2s ease;
+        }
+        .btn:active {
+            transform: translateY(0);
+        }
+        .btn-primary {
+            border-color: rgba(255,255,255,0.22);
+        }
+        .btn-danger {
+            border-color: rgba(255,255,255,0.22);
+        }
+        .btn-success {
+            border-color: rgba(255,255,255,0.22);
+        }
+        .btn:hover {
+            filter: saturate(1.06);
+        }
+
+        .users-toolbar,
+        .bulk-user-bar {
+            border-radius: 10px;
+            border-color: #d8e5f3;
+            background: linear-gradient(180deg, #ffffff, #f8fbff);
+            box-shadow: 0 6px 16px rgba(15,23,42,0.05);
+        }
+        .users-toolbar-filters select,
+        .bulk-user-bar select {
+            border-color: #cfdeee;
+        }
+
+        input:focus-visible,
+        select:focus-visible,
+        textarea:focus-visible,
+        .btn:focus-visible,
+        a:focus-visible {
+            outline: none;
+            box-shadow: 0 0 0 3px rgba(0,90,160,0.2);
+        }
 
         /* ===== RESPONSIVE ===== */
         @media (max-width: 900px) {
@@
             .sidebar a { display: inline-flex; margin: 4px; }
             .sidebar .nav-spacer { display: none; }
             .main { margin-left: 0; padding: 16px; }
+            .main::before,
+            .main::after { display: none; }
             .form-row { grid-template-columns: 1fr; gap: 0; }
+            .header { position: static; }
             .header { align-items: flex-start; gap: 10px; flex-direction: column; }
-            .admin-search { position: static; flex-wrap: nowrap; padding: 10px 14px; }
+            .admin-search { position: static; top: auto; flex-wrap: nowrap; padding: 10px 14px; }
             .admin-search-meta { display: none; }
             .stat-card { padding: 14px; }
             .stat-card .number { font-size: 1.4rem; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-<body>
+<body class="<?= isAdmin() ? 'role-admin' : 'role-editor' ?>">
@@
         .admin-search {
             top: 78px;
             backdrop-filter: blur(8px);
             -webkit-backdrop-filter: blur(8px);
             background: rgba(255,255,255,0.92);
         }
+        .role-editor .admin-search {
+            top: 12px;
+        }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .admin-search {
             top: 78px;
             backdrop-filter: blur(8px);
             -webkit-backdrop-filter: blur(8px);
             background: rgba(255,255,255,0.92);
         }
+        .role-editor .admin-search {
+            top: 12px;
+        }
         .admin-search input {
             font-size: 0.93rem;
         }
@@
-<body>
+<body class="<?= isAdmin() ? 'role-admin' : 'role-editor' ?>">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
 // File upload handling
 if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['upload'])) {
-    if (!canUpload()) { setFlash('You don\'t have upload permission', 'error'); header('Location: dashboard.php#add-file'); exit; }
+    $isAjaxUpload = isset($_POST['ajax']) && (string)$_POST['ajax'] === '1';
+    $uploadRespond = function(bool $success, string $message, string $tab = 'add-file') use ($isAjaxUpload) {
+        if ($isAjaxUpload) {
+            header('Content-Type: application/json');
+            echo json_encode(['success' => $success, 'message' => $message, 'tab' => $tab]);
+            exit;
+        }
+        setFlash($message, $success ? 'success' : 'error');
+        header('Location: dashboard.php#' . $tab);
+        exit;
+    };
+
+    if (!canUpload()) { $uploadRespond(false, 'You don\'t have upload permission'); }
     $uploadType  = $_POST['upload_type'] ?? '';
     $displayName = trim($_POST['display_name'] ?? '');
     $brandId     = (int)($_POST['brand_id'] ?? 0);
@@
-    if (empty($displayName)) { setFlash('Display name is required', 'error'); header('Location: dashboard.php#add-file'); exit; }
+    if (empty($displayName)) { $uploadRespond(false, 'Display name is required'); }
@@
         if (!in_array($ext, $allowedExts, true)) {
-            setFlash("File type .$ext not allowed for " . $uploadType, 'error');
-            header('Location: dashboard.php#add-file');
-            exit;
+            $uploadRespond(false, "File type .$ext not allowed for " . $uploadType);
         }
-        if ($file['size'] > MAX_FILE_SIZE) { setFlash('File exceeds max size', 'error'); header('Location: dashboard.php#add-file'); exit; }
+        if ($file['size'] > MAX_FILE_SIZE) { $uploadRespond(false, 'File exceeds max size'); }
@@
         // Validate required fields per type
-        if (!$brandId) { setFlash('Brand is required', 'error'); header('Location: dashboard.php#add-file'); exit; }
-        if ($uploadType === 'config' && !$modelId) { setFlash('Model is required for config files', 'error'); header('Location: dashboard.php#add-file'); exit; }
+        if (!$brandId) { $uploadRespond(false, 'Brand is required'); }
+        if ($uploadType === 'config' && !$modelId) { $uploadRespond(false, 'Model is required for config files'); }
         if ($uploadType !== 'config' && !$modelId) {
             $modelId = null;
         }
         if ($modelId) {
             $modelRow = $db->fetchOne("SELECT id, brand_id FROM device_models WHERE id = ?", [$modelId]);
             if (!$modelRow) {
-                setFlash('Selected model does not exist', 'error');
-                header('Location: dashboard.php#add-file');
-                exit;
+                $uploadRespond(false, 'Selected model does not exist');
             }
             if ((int)$modelRow['brand_id'] !== (int)$brandId) {
-                setFlash('Selected model does not belong to selected brand', 'error');
-                header('Location: dashboard.php#add-file');
-                exit;
+                $uploadRespond(false, 'Selected model does not belong to selected brand');
             }
         }
@@
         $uniqueName = uniqid() . '_' . $safeName;
         $destPath = $destDir . '/' . $uniqueName;
 
         if (!move_uploaded_file($file['tmp_name'], $destPath)) {
-            setFlash('Failed to save file', 'error');
-            header('Location: dashboard.php#add-file');
-            exit;
+            $uploadRespond(false, 'Failed to save file');
         }
@@
             }
-            setFlash("$displayName uploaded successfully");
             $tabMap = ['config' => 'configs', 'firmware' => 'firmware', 'manual' => 'manuals', 'software' => 'software'];
             $tab = $tabMap[$uploadType] ?? 'add-file';
-            header("Location: dashboard.php#$tab");
-            exit;
+            $uploadRespond(true, "$displayName uploaded successfully and is now visible on user side.", $tab);
         } catch (Exception $e) {
             if (file_exists($destPath)) unlink($destPath);
-            setFlash('Upload failed: ' . $e->getMessage(), 'error');
-            header('Location: dashboard.php#add-file');
-            exit;
+            $uploadRespond(false, 'Upload failed: ' . $e->getMessage());
         }
     } else {
-        setFlash('No file selected or upload error', 'error');
+        $uploadRespond(false, 'No file selected or upload error');
     }
-    header('Location: dashboard.php#add-file');
-    exit;
 }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         .btn-upload-submit:disabled {
             opacity: 0.7;
             cursor: not-allowed;
         }
+        .upload-progress-wrap {
+            margin-top: 8px;
+            display: none;
+            gap: 6px;
+            flex-direction: column;
+        }
+        .upload-progress-track {
+            width: 100%;
+            height: 8px;
+            border-radius: 999px;
+            background: #dbe8f6;
+            overflow: hidden;
+        }
+        .upload-progress-fill {
+            width: 0%;
+            height: 100%;
+            border-radius: 999px;
+            background: linear-gradient(90deg, #005aa0, #00a86b);
+            transition: width 0.2s ease;
+        }
+        .upload-progress-meta {
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            gap: 8px;
+            font-size: 0.76rem;
+            color: #475569;
+            font-weight: 600;
+        }
         .upload-inline-form {
             margin-top: 12px;
             border: 1px solid #dbe5f0;
             background: #f8fbff;
             border-radius: 10px;
@@
         .flash-success { background: #d1fae5; color: #065f46; }
         .flash-error { background: #fee2e2; color: #991b1b; }
         @keyframes slideDown {
             from { opacity: 0; transform: translateY(-10px); }
             to { opacity: 1; transform: translateY(0); }
         }
+        .toast-stack {
+            position: fixed;
+            right: 14px;
+            top: 14px;
+            z-index: 99999;
+            display: flex;
+            flex-direction: column;
+            gap: 8px;
+            pointer-events: none;
+            max-width: min(92vw, 380px);
+        }
+        .app-toast {
+            display: flex;
+            align-items: flex-start;
+            gap: 10px;
+            border-radius: 10px;
+            padding: 11px 12px;
+            border: 1px solid #d7e4f2;
+            box-shadow: 0 14px 26px rgba(15,23,42,0.15);
+            background: #fff;
+            color: #0f172a;
+            font-size: 0.84rem;
+            font-weight: 600;
+            pointer-events: auto;
+            animation: toastIn 0.2s ease;
+        }
+        .app-toast.success {
+            border-color: #bce7d4;
+            background: linear-gradient(160deg, #f4fff9, #ecfff5);
+            color: #0b6b44;
+        }
+        .app-toast.error {
+            border-color: #f6c7c7;
+            background: linear-gradient(160deg, #fff7f7, #fff1f1);
+            color: #9f1d1d;
+        }
+        .app-toast i { margin-top: 1px; }
+        .app-toast.hide {
+            opacity: 0;
+            transform: translateY(-6px);
+            transition: opacity 0.2s ease, transform 0.2s ease;
+        }
+        @keyframes toastIn {
+            from { opacity: 0; transform: translateY(-8px); }
+            to { opacity: 1; transform: translateY(0); }
+        }
 
         /* ===== DELETE IMAGE CHECKBOX LABEL ===== */
         label.btn-danger.active { background: #c53030; }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                         <button type="submit" class="btn-upload-submit" id="uploadSubmitBtn">
                             <span class="btn-upload-text"><i class="fas fa-upload"></i> Upload File</span>
                             <span class="btn-upload-loader" style="display:none"><i class="fas fa-spinner fa-spin"></i> Uploading...</span>
                         </button>
+                        <div class="upload-progress-wrap" id="uploadProgressWrap">
+                            <div class="upload-progress-track"><div class="upload-progress-fill" id="uploadProgressFill"></div></div>
+                            <div class="upload-progress-meta">
+                                <span id="uploadProgressText">Preparing upload...</span>
+                                <span id="uploadProgressPct">0%</span>
+                            </div>
+                        </div>
                     </div>
                 </div>
             </form>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
             </div>
             <?php endif; ?>
 	            <?php flash(); ?>
+                <div id="toastStack" class="toast-stack" aria-live="polite" aria-atomic="true"></div>
 
 	        <?php if (isAdmin()): ?>
 	        <div class="stats-grid">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                // Upload spinner + pre-submit validation
-                document.getElementById('uploadForm').addEventListener('submit', function(e) {
-                    var type = document.getElementById('uploadType').value;
-                    var brand = document.getElementById('brandSelect').value;
-                    var model = document.getElementById('modelSelect').value;
-                    if (!brand) {
-                        e.preventDefault();
-                        alert('Please select a brand.');
-                        return;
-                    }
-                    if (type === 'config' && !model) {
-                        e.preventDefault();
-                        alert('Please select a device model for configuration files.');
-                        return;
-                    }
-
-                    document.getElementById('uploadSubmitBtn').disabled = true;
-                    document.querySelector('.btn-upload-text').style.display = 'none';
-                    document.querySelector('.btn-upload-loader').style.display = 'inline';
-                });
+                // Upload with progress + toast feedback
+                (function() {
+                    var uploadForm = document.getElementById('uploadForm');
+                    var uploadBtn = document.getElementById('uploadSubmitBtn');
+                    var uploadText = document.querySelector('.btn-upload-text');
+                    var uploadLoader = document.querySelector('.btn-upload-loader');
+                    var progressWrap = document.getElementById('uploadProgressWrap');
+                    var progressFill = document.getElementById('uploadProgressFill');
+                    var progressPct = document.getElementById('uploadProgressPct');
+                    var progressText = document.getElementById('uploadProgressText');
+
+                    if (!uploadForm) return;
+
+                    function setUploadBusy(isBusy) {
+                        uploadBtn.disabled = !!isBusy;
+                        if (uploadText) uploadText.style.display = isBusy ? 'none' : 'inline';
+                        if (uploadLoader) uploadLoader.style.display = isBusy ? 'inline' : 'none';
+                        if (progressWrap) progressWrap.style.display = isBusy ? 'flex' : 'none';
+                    }
+
+                    function setUploadProgress(percent, label) {
+                        var safe = Math.max(0, Math.min(100, percent || 0));
+                        if (progressFill) progressFill.style.width = safe + '%';
+                        if (progressPct) progressPct.textContent = Math.round(safe) + '%';
+                        if (progressText && label) progressText.textContent = label;
+                    }
+
+                    uploadForm.addEventListener('submit', function(e) {
+                        e.preventDefault();
+                        var type = document.getElementById('uploadType').value;
+                        var brand = document.getElementById('brandSelect').value;
+                        var model = document.getElementById('modelSelect').value;
+                        if (!brand) {
+                            if (typeof showToast === 'function') showToast('Please select a brand.', 'error');
+                            return;
+                        }
+                        if (type === 'config' && !model) {
+                            if (typeof showToast === 'function') showToast('Please select a device model for configuration files.', 'error');
+                            return;
+                        }
+
+                        var formData = new FormData(uploadForm);
+                        formData.append('ajax', '1');
+                        var xhr = new XMLHttpRequest();
+                        setUploadBusy(true);
+                        setUploadProgress(0, 'Preparing upload...');
+                        xhr.open('POST', 'dashboard.php', true);
+
+                        xhr.upload.onprogress = function(ev) {
+                            if (!ev.lengthComputable) return;
+                            var p = Math.min(98, Math.round((ev.loaded / ev.total) * 100));
+                            setUploadProgress(p, p < 95 ? 'Uploading file...' : 'Finalizing...');
+                        };
+
+                        xhr.onerror = function() {
+                            setUploadBusy(false);
+                            setUploadProgress(0, 'Preparing upload...');
+                            if (typeof showToast === 'function') showToast('Upload failed. Please try again.', 'error');
+                        };
+
+                        xhr.onload = function() {
+                            var data = null;
+                            try { data = JSON.parse(xhr.responseText || '{}'); } catch (err) { data = null; }
+
+                            if (xhr.status >= 200 && xhr.status < 300 && data && data.success) {
+                                setUploadProgress(100, 'Published to user library');
+                                if (typeof showToast === 'function') showToast(data.message || 'Upload complete.', 'success', 3200);
+                                var nextTab = data.tab || 'add-file';
+                                setTimeout(function() {
+                                    window.location.href = 'dashboard.php#' + nextTab;
+                                }, 900);
+                                return;
+                            }
+
+                            setUploadBusy(false);
+                            setUploadProgress(0, 'Preparing upload...');
+                            var msg = (data && data.message) ? data.message : 'Upload failed. Please try again.';
+                            if (typeof showToast === 'function') showToast(msg, 'error', 4200);
+                        };
+
+                        xhr.send(formData);
+                    });
+                })();
             </script>
         </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         function togglePw() {
             var p = document.getElementById('editPassword');
             var t = document.getElementById('pwToggle');
             if (p.type === 'password') {
                 p.type = 'text';
@@
                 p.type = 'password';
                 t.innerHTML = '<i class="fas fa-eye"></i>';
             }
         }
+
+        function showToast(message, type, timeoutMs) {
+            var stack = document.getElementById('toastStack');
+            if (!stack) return;
+            var toast = document.createElement('div');
+            var toastType = type === 'error' ? 'error' : 'success';
+            toast.className = 'app-toast ' + toastType;
+            var icon = toastType === 'error' ? 'fa-circle-exclamation' : 'fa-circle-check';
+            toast.innerHTML = '<i class="fas ' + icon + '"></i><span>' + String(message || '') + '</span>';
+            stack.appendChild(toast);
+            var ttl = typeof timeoutMs === 'number' ? timeoutMs : 3200;
+            setTimeout(function() {
+                toast.classList.add('hide');
+                setTimeout(function() {
+                    if (toast.parentNode) toast.parentNode.removeChild(toast);
+                }, 220);
+            }, ttl);
+        }
+
         function deleteFile(id, type) {
             if (!confirm('Delete this file?')) return false;
             fetch('dashboard.php?delete=' + id + '&type=' + type + '&ajax=1')
                 .then(function(r) {
                     if (!r.ok) throw new Error('Request failed');
@@
                 .then(function(d) {
                     if (d.success) {
                         var btn = document.querySelector('[data-del-id="' + id + '"][data-del-type="' + type + '"]');
                         if (btn) { var tr = btn.closest('tr'); if (tr) tr.remove(); }
                         applyAdminSearch();
+                        showToast('File deleted from admin and user library.', 'success');
                     } else {
-                        alert('Delete failed');
+                        showToast('Delete failed.', 'error');
                     }
                 })
-                .catch(function() { alert('Delete failed. Please refresh and try again.'); });
+                .catch(function() { showToast('Delete failed. Please refresh and try again.', 'error', 4200); });
             return false;
         }
@@
             checked.forEach(function(cb) { ids.push(cb.value); });
-            if (ids.length === 0) { alert('Select files to delete'); return; }
+            if (ids.length === 0) { showToast('Select files to delete.', 'error'); return; }
             if (!confirm('Delete ' + ids.length + ' selected file(s)?')) return;
             var form = new FormData();
             form.append('bulk_delete', '1');
             form.append('type', type);
             ids.forEach(function(id) { form.append('ids[]', id); });
@@
                 .then(function(d) {
                     if (d.success) {
                         checked.forEach(function(cb) { var tr = cb.closest('tr'); if (tr) tr.remove(); });
                         applyAdminSearch();
+                        showToast((d.deleted || ids.length) + ' file(s) deleted and removed from user library.', 'success');
                     } else {
-                        alert('Delete failed: ' + (d.message || 'Unknown error'));
+                        showToast('Delete failed: ' + (d.message || 'Unknown error'), 'error', 4200);
                     }
                 })
-                .catch(function() { alert('Bulk delete failed. Please refresh and try again.'); });
+                .catch(function() { showToast('Bulk delete failed. Please refresh and try again.', 'error', 4200); });
         }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
         // Initial tab on page load
         (function() {
+            document.querySelectorAll('.flash-msg').forEach(function(msg) {
+                var text = (msg.innerText || msg.textContent || '').trim();
+                if (!text) return;
+                var type = msg.classList.contains('flash-error') ? 'error' : 'success';
+                showToast(text, type, 3400);
+                msg.remove();
+            });
+
             var adminSearchInput = document.getElementById('adminSearchInput');
             if (adminSearchInput) {
                 adminSearchInput.addEventListener('input', applyAdminSearch);
                 adminSearchInput.addEventListener('keydown', function(e) {
                     if (e.key === 'Escape') clearAdminSearch();
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 function setUploadProgress(percent, label) {
                     var safe = Math.max(0, Math.min(100, percent || 0));
                     if (progressFill) progressFill.style.width = safe + '%';
                     if (progressPct) progressPct.textContent = Math.round(safe) + '%';
                     if (progressText && label) progressText.textContent = label;
                 }
+
+                    function fallbackSubmit() {
+                        if (progressText) progressText.textContent = 'Switching to standard upload...';
+                        var ajaxFlag = uploadForm.querySelector('input[name="ajax"]');
+                        if (ajaxFlag) ajaxFlag.remove();
+                        uploadForm.submit();
+                    }
 
                     uploadForm.addEventListener('submit', function(e) {
                         e.preventDefault();
                         var type = document.getElementById('uploadType').value;
                         var brand = document.getElementById('brandSelect').value;
@@
                         if (type === 'config' && !model) {
                             if (typeof showToast === 'function') showToast('Please select a device model for configuration files.', 'error');
                             return;
                         }
 
+                        if (!fileInput || !fileInput.files || !fileInput.files[0]) {
+                            if (typeof showToast === 'function') showToast('Please choose a file first.', 'error');
+                            return;
+                        }
+
+                        var ext = (fileInput.files[0].name.split('.').pop() || '').toLowerCase();
+                        var allowedForType = uploadTypeExtMap[type] || [];
+                        if (allowedForType.length && allowedForType.indexOf(ext) === -1) {
+                            if (typeof showToast === 'function') {
+                                showToast('Selected file .' + ext + ' does not match "' + type + '" type. Please change File Type.', 'error', 4500);
+                            } else {
+                                alert('Selected file .' + ext + ' does not match "' + type + '" type. Please change File Type.');
+                            }
+                            return;
+                        }
+
                         var formData = new FormData(uploadForm);
                         formData.append('ajax', '1');
-                        var xhr = new XMLHttpRequest();
+                        var xhr = null;
+                        try {
+                            xhr = new XMLHttpRequest();
+                        } catch (err) {
+                            fallbackSubmit();
+                            return;
+                        }
                         setUploadBusy(true);
                         setUploadProgress(0, 'Preparing upload...');
                         xhr.open('POST', 'dashboard.php', true);
@@
                         xhr.onload = function() {
                             var data = null;
                             try { data = JSON.parse(xhr.responseText || '{}'); } catch (err) { data = null; }
 
                             if (xhr.status >= 200 && xhr.status < 300 && data && data.success) {
@@
                                 return;
                             }
+
+                            // If server responded with non-JSON (for example redirected HTML),
+                            // fallback to normal form post so upload still works.
+                            if (!data) {
+                                fallbackSubmit();
+                                return;
+                            }
 
                             setUploadBusy(false);
                             setUploadProgress(0, 'Preparing upload...');
                             var msg = (data && data.message) ? data.message : 'Upload failed. Please try again.';
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 function formatAcceptList(exts) {
                     return exts.map(function(ext) { return '.' + ext; }).join(', ');
                 }
+
+                function detectUploadTypeByExt(ext) {
+                    var lowered = (ext || '').toLowerCase();
+                    if (!lowered) return '';
+                    if ((uploadTypeExtMap.config || []).indexOf(lowered) !== -1) return 'config';
+                    if ((uploadTypeExtMap.firmware || []).indexOf(lowered) !== -1) return 'firmware';
+                    if ((uploadTypeExtMap.manual || []).indexOf(lowered) !== -1) return 'manual';
+                    if ((uploadTypeExtMap.software || []).indexOf(lowered) !== -1) return 'software';
+                    return '';
+                }
@@
                 function onFileSelect(input) {
                     if (input.files && input.files[0]) {
                         var f = input.files[0];
                         document.getElementById('previewName').textContent = f.name;
                         document.getElementById('previewSize').textContent = (f.size / 1024).toFixed(1) + ' KB';
                         var icon = document.getElementById('previewIcon');
                         var ext = f.name.split('.').pop().toLowerCase();
+                        var autoType = detectUploadTypeByExt(ext);
+                        if (autoType) {
+                            var typeSelect = document.getElementById('uploadType');
+                            if (typeSelect && typeSelect.value !== autoType) {
+                                typeSelect.value = autoType;
+                                toggleUploadFields();
+                                if (typeof showToast === 'function') {
+                                    showToast('File type changed to ' + autoType + ' based on .' + ext + ' file.', 'success', 2600);
+                                }
+                            }
+                        }
                         var icons = {cfg:'fa-file-code',conf:'fa-file-code',txt:'fa-file-lines',pdf:'fa-file-pdf',bin:'fa-microchip',fw:'fa-microchip',hex:'fa-microchip',zip:'fa-file-zipper',exe:'fa-gear',msi:'fa-gear'};
                         icon.innerHTML = '<i class="fas ' + (icons[ext]||'fa-file') + '"></i>';
                         document.getElementById('dropzoneContent').style.display = 'none';
                         document.getElementById('dropzonePreview').style.display = 'flex';
                         dropzone.classList.add('has-file');
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                     function setUploadProgress(percent, label) {
                         var safe = Math.max(0, Math.min(100, percent || 0));
                         if (progressFill) progressFill.style.width = safe + '%';
                         if (progressPct) progressPct.textContent = Math.round(safe) + '%';
                         if (progressText && label) progressText.textContent = label;
                     }
+
+                    function notify(message, type, timeoutMs) {
+                        if (typeof showToast === 'function') {
+                            showToast(message, type || 'success', timeoutMs);
+                        } else {
+                            alert(message);
+                        }
+                    }
 
                     function fallbackSubmit() {
                         if (progressText) progressText.textContent = 'Switching to standard upload...';
                         var ajaxFlag = uploadForm.querySelector('input[name="ajax"]');
                         if (ajaxFlag) ajaxFlag.remove();
@@
                         var type = document.getElementById('uploadType').value;
                         var brand = document.getElementById('brandSelect').value;
                         var model = document.getElementById('modelSelect').value;
                         if (!brand) {
-                            if (typeof showToast === 'function') showToast('Please select a brand.', 'error');
+                            notify('Please select a brand.', 'error');
                             return;
                         }
                         if (type === 'config' && !model) {
-                            if (typeof showToast === 'function') showToast('Please select a device model for configuration files.', 'error');
+                            notify('Please select a device model for configuration files.', 'error');
                             return;
                         }
 
                         if (!fileInput || !fileInput.files || !fileInput.files[0]) {
-                            if (typeof showToast === 'function') showToast('Please choose a file first.', 'error');
+                            notify('Please choose a file first.', 'error');
                             return;
                         }
 
                         var ext = (fileInput.files[0].name.split('.').pop() || '').toLowerCase();
                         var allowedForType = uploadTypeExtMap[type] || [];
                         if (allowedForType.length && allowedForType.indexOf(ext) === -1) {
-                            if (typeof showToast === 'function') {
-                                showToast('Selected file .' + ext + ' does not match "' + type + '" type. Please change File Type.', 'error', 4500);
-                            } else {
-                                alert('Selected file .' + ext + ' does not match "' + type + '" type. Please change File Type.');
+                            var detectedType = detectUploadTypeByExt(ext);
+                            if (detectedType) {
+                                var typeSelectFix = document.getElementById('uploadType');
+                                if (typeSelectFix) {
+                                    typeSelectFix.value = detectedType;
+                                    toggleUploadFields();
+                                    type = detectedType;
+                                    allowedForType = uploadTypeExtMap[type] || [];
+                                    notify('File type auto-corrected to "' + detectedType + '". Uploading now.', 'success', 2600);
+                                }
+                            } else {
+                                notify('Selected file .' + ext + ' is not supported.', 'error', 4500);
+                                return;
                             }
-                            return;
                         }
@@
                         xhr.onerror = function() {
                             setUploadBusy(false);
                             setUploadProgress(0, 'Preparing upload...');
-                            if (typeof showToast === 'function') showToast('Upload failed. Please try again.', 'error');
+                            notify('Upload failed. Please try again.', 'error');
                         };
@@
                             if (xhr.status >= 200 && xhr.status < 300 && data && data.success) {
                                 setUploadProgress(100, 'Published to user library');
-                                if (typeof showToast === 'function') showToast(data.message || 'Upload complete.', 'success', 3200);
+                                notify(data.message || 'Upload complete.', 'success', 3200);
                                 var nextTab = data.tab || 'add-file';
                                 setTimeout(function() {
                                     window.location.href = 'dashboard.php#' + nextTab;
                                 }, 900);
                                 return;
@@
                             setUploadBusy(false);
                             setUploadProgress(0, 'Preparing upload...');
                             var msg = (data && data.message) ? data.message : 'Upload failed. Please try again.';
-                            if (typeof showToast === 'function') showToast(msg, 'error', 4200);
+                            notify(msg, 'error', 4200);
                         };
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                     uploadForm.addEventListener('submit', function(e) {
                         e.preventDefault();
-                        var type = document.getElementById('uploadType').value;
-                        var brand = document.getElementById('brandSelect').value;
-                        var model = document.getElementById('modelSelect').value;
-                        if (!brand) {
-                            notify('Please select a brand.', 'error');
-                            return;
-                        }
-                        if (type === 'config' && !model) {
-                            notify('Please select a device model for configuration files.', 'error');
-                            return;
-                        }
-
-                        if (!fileInput || !fileInput.files || !fileInput.files[0]) {
-                            notify('Please choose a file first.', 'error');
-                            return;
-                        }
-
-                        var ext = (fileInput.files[0].name.split('.').pop() || '').toLowerCase();
-                        var allowedForType = uploadTypeExtMap[type] || [];
-                        if (allowedForType.length && allowedForType.indexOf(ext) === -1) {
-                            var detectedType = detectUploadTypeByExt(ext);
-                            if (detectedType) {
-                                var typeSelectFix = document.getElementById('uploadType');
-                                if (typeSelectFix) {
-                                    typeSelectFix.value = detectedType;
-                                    toggleUploadFields();
-                                    type = detectedType;
-                                    allowedForType = uploadTypeExtMap[type] || [];
-                                    notify('File type auto-corrected to "' + detectedType + '". Uploading now.', 'success', 2600);
-                                }
-                            } else {
-                                notify('Selected file .' + ext + ' is not supported.', 'error', 4500);
+                        try {
+                            var type = document.getElementById('uploadType').value;
+                            var brand = document.getElementById('brandSelect').value;
+                            var model = document.getElementById('modelSelect').value;
+                            if (!brand) {
+                                notify('Please select a brand.', 'error');
+                                return;
+                            }
+                            if (type === 'config' && !model) {
+                                notify('Please select a device model for configuration files.', 'error');
+                                return;
+                            }
+
+                            if (!fileInput || !fileInput.files || !fileInput.files[0]) {
+                                notify('Please choose a file first.', 'error');
                                 return;
                             }
-                        }
-
-                        var formData = new FormData(uploadForm);
-                        formData.append('ajax', '1');
-                        var xhr = null;
-                        try {
+
+                            var ext = (fileInput.files[0].name.split('.').pop() || '').toLowerCase();
+                            var allowedForType = uploadTypeExtMap[type] || [];
+                            if (allowedForType.length && allowedForType.indexOf(ext) === -1) {
+                                var detectedType = detectUploadTypeByExt(ext);
+                                if (detectedType) {
+                                    var typeSelectFix = document.getElementById('uploadType');
+                                    if (typeSelectFix) {
+                                        typeSelectFix.value = detectedType;
+                                        toggleUploadFields();
+                                        type = detectedType;
+                                        allowedForType = uploadTypeExtMap[type] || [];
+                                        notify('File type auto-corrected to "' + detectedType + '". Uploading now.', 'success', 2600);
+                                    }
+                                } else {
+                                    notify('Selected file .' + ext + ' is not supported.', 'error', 4500);
+                                    return;
+                                }
+                            }
+
+                            var formData = new FormData(uploadForm);
+                            formData.append('ajax', '1');
+                            var xhr = null;
                             xhr = new XMLHttpRequest();
-                        } catch (err) {
-                            fallbackSubmit();
-                            return;
-                        }
-                        setUploadBusy(true);
-                        setUploadProgress(0, 'Preparing upload...');
-                        xhr.open('POST', 'dashboard.php', true);
-
-                        xhr.upload.onprogress = function(ev) {
-                            if (!ev.lengthComputable) return;
-                            var p = Math.min(98, Math.round((ev.loaded / ev.total) * 100));
-                            setUploadProgress(p, p < 95 ? 'Uploading file...' : 'Finalizing...');
-                        };
-
-                        xhr.onerror = function() {
-                            setUploadBusy(false);
-                            setUploadProgress(0, 'Preparing upload...');
-                            notify('Upload failed. Please try again.', 'error');
-                        };
-
-                        xhr.onload = function() {
-                            var data = null;
-                            try { data = JSON.parse(xhr.responseText || '{}'); } catch (err) { data = null; }
-
-                            if (xhr.status >= 200 && xhr.status < 300 && data && data.success) {
-                                setUploadProgress(100, 'Published to user library');
-                                notify(data.message || 'Upload complete.', 'success', 3200);
-                                var nextTab = data.tab || 'add-file';
-                                setTimeout(function() {
-                                    window.location.href = 'dashboard.php#' + nextTab;
-                                }, 900);
+                            setUploadBusy(true);
+                            setUploadProgress(0, 'Preparing upload...');
+                            xhr.open('POST', 'dashboard.php', true);
+
+                            xhr.upload.onprogress = function(ev) {
+                                if (!ev.lengthComputable) return;
+                                var p = Math.min(98, Math.round((ev.loaded / ev.total) * 100));
+                                setUploadProgress(p, p < 95 ? 'Uploading file...' : 'Finalizing...');
+                            };
+
+                            xhr.onerror = function() {
+                                setUploadBusy(false);
+                                setUploadProgress(0, 'Preparing upload...');
+                                notify('Upload failed. Please try again.', 'error');
+                            };
+
+                            xhr.onload = function() {
+                                var data = null;
+                                try { data = JSON.parse(xhr.responseText || '{}'); } catch (err) { data = null; }
+
+                                if (xhr.status >= 200 && xhr.status < 300 && data && data.success) {
+                                    setUploadProgress(100, 'Published to user library');
+                                    notify(data.message || 'Upload complete.', 'success', 3200);
+                                    var nextTab = data.tab || 'add-file';
+                                    setTimeout(function() {
+                                        window.location.href = 'dashboard.php#' + nextTab;
+                                    }, 900);
+                                    return;
+                                }
+
+                                // If server responded with non-JSON (for example redirected HTML),
+                                // fallback to normal form post so upload still works.
+                                if (!data) {
+                                    fallbackSubmit();
+                                    return;
+                                }
+
+                                setUploadBusy(false);
+                                setUploadProgress(0, 'Preparing upload...');
+                                var msg = (data && data.message) ? data.message : 'Upload failed. Please try again.';
+                                notify(msg, 'error', 4200);
+                            };
+
+                            xhr.send(formData);
+                        } catch (err) {
+                            fallbackSubmit();
+                            return;
+                        }
+                    });
+                })();
+            </script>
-                                return;
-                            }
-
-                            // If server responded with non-JSON (for example redirected HTML),
-                            // fallback to normal form post so upload still works.
-                            if (!data) {
-                                fallbackSubmit();
-                                return;
-                            }
-
-                            setUploadBusy(false);
-                            setUploadProgress(0, 'Preparing upload...');
-                            var msg = (data && data.message) ? data.message : 'Upload failed. Please try again.';
-                            notify(msg, 'error', 4200);
-                        };
-
-                        xhr.send(formData);
-                    });
-                })();
-            </script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-        <a href="../index.php"><i class="fas fa-arrow-left"></i> View Site</a>
         <?php if (!empty($_SESSION['_impersonator_id'])): ?>
         <a href="../logout.php?switch_back=1"><i class="fas fa-rotate-left"></i> Back to Admin</a>
         <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                 <?php if (canManageBrands()): ?>
                                 <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                                    <form method="POST">
-                                        <input type="hidden" name="create_brand" value="1">
-                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                                        <div class="inline-form-row">
-                                            <input type="text" name="brand_name" placeholder="Brand name" required>
-                                            <input type="color" name="brand_color" value="#1a73e8">
-                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                                        </div>
-                                    </form>
+                                    <div class="inline-form-row">
+                                        <input type="text" id="inlineBrandName" placeholder="Brand name" required>
+                                        <input type="color" id="inlineBrandColor" value="#1a73e8">
+                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineBrand()"><i class="fas fa-plus"></i> Add</button>
+                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                    </div>
                                 </div>
                                 <?php endif; ?>
                             </div>
                             <div class="upload-field">
@@
                                 <?php if (canManageModels()): ?>
                                 <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <form method="POST">
-                                        <input type="hidden" name="create_model" value="1">
-                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                                        <div class="inline-form-row">
-                                            <select name="model_brand_id" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
-                                                <?php foreach ($brands as $b): ?>
-                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                                <?php endforeach; ?>
-                                            </select>
-                                            <input type="text" name="model_name" placeholder="Model name" required>
-                                            <select name="model_system_type">
-                                                <option value="">System</option>
-                                                <option value="advanced">Advanced</option>
-                                                <option value="standard">Standard</option>
-                                            </select>
-                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                                        </div>
-                                    </form>
+                                    <div class="inline-form-row">
+                                        <select id="inlineModelBrandId" required>
+                                            <option value="">- Brand -</option>
+                                            <?php foreach ($brands as $b): ?>
+                                            <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                            <?php endforeach; ?>
+                                        </select>
+                                        <input type="text" id="inlineModelName" placeholder="Model name" required>
+                                        <select id="inlineModelSystemType">
+                                            <option value="">System</option>
+                                            <option value="advanced">Advanced</option>
+                                            <option value="standard">Standard</option>
+                                        </select>
+                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
+                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                    </div>
                                 </div>
                                 <?php endif; ?>
                             </div>
                         </div>
@@
-                <?php if (false && canManageBrands()): ?>
-                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_brand" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <input type="text" name="brand_name" placeholder="Brand name" required>
-                            <input type="color" name="brand_color" value="#1a73e8">
-                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
-
-                <?php if (false && canManageModels()): ?>
-                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                    <form method="POST">
-                        <input type="hidden" name="create_model" value="1">
-                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                        <div class="inline-form-row">
-                            <select name="model_brand_id" required>
-                                <option value="">â€” Brand â€”</option>
-                                <?php foreach ($brands as $b): ?>
-                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                <?php endforeach; ?>
-                            </select>
-                            <input type="text" name="model_name" placeholder="Model name" required>
-                            <select name="model_system_type">
-                                <option value="">System</option>
-                                <option value="advanced">Advanced</option>
-                                <option value="standard">Standard</option>
-                            </select>
-                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                        </div>
-                    </form>
-                </div>
-                <?php endif; ?>
             <script>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                                    <form method="POST">
-                                        <input type="hidden" name="create_brand" value="1">
-                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                                        <div class="inline-form-row">
-                                            <input type="text" name="brand_name" placeholder="Brand name" required>
-                                            <input type="color" name="brand_color" value="#1a73e8">
-                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                                        </div>
-                                    </form>
-                                </div>
+                                <div id="brandInlineForm" class="upload-inline-form" style="display:none">
+                                    <div class="inline-form-row">
+                                        <input type="text" id="inlineBrandName" placeholder="Brand name" required>
+                                        <input type="color" id="inlineBrandColor" value="#1a73e8">
+                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineBrand()"><i class="fas fa-plus"></i> Add</button>
+                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                    </div>
+                                </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <form method="POST">
-                                        <input type="hidden" name="create_model" value="1">
-                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                                        <div class="inline-form-row">
-                                            <select name="model_brand_id" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
-                                                <?php foreach ($brands as $b): ?>
-                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                                <?php endforeach; ?>
-                                            </select>
-                                            <input type="text" name="model_name" placeholder="Model name" required>
-                                            <select name="model_system_type">
-                                                <option value="">System</option>
-                                                <option value="advanced">Advanced</option>
-                                                <option value="standard">Standard</option>
-                                            </select>
-                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                                        </div>
-                                    </form>
-                                </div>
+                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
+                                    <div class="inline-form-row">
+                                        <select id="inlineModelBrandId" required>
+                                            <option value="">- Brand -</option>
+                                            <?php foreach ($brands as $b): ?>
+                                            <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                            <?php endforeach; ?>
+                                        </select>
+                                        <input type="text" id="inlineModelName" placeholder="Model name" required>
+                                        <select id="inlineModelSystemType">
+                                            <option value="">System</option>
+                                            <option value="advanced">Advanced</option>
+                                            <option value="standard">Standard</option>
+                                        </select>
+                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
+                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                    </div>
+                                </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                <?php if (canManageModels()): ?>
-                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <form method="POST">
-                                        <input type="hidden" name="create_model" value="1">
-                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                                        <div class="inline-form-row">
-                                            <select name="model_brand_id" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
-                                                <?php foreach ($brands as $b): ?>
-                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                                <?php endforeach; ?>
-                                            </select>
-                                            <input type="text" name="model_name" placeholder="Model name" required>
-                                            <select name="model_system_type">
-                                                <option value="">System</option>
-                                                <option value="advanced">Advanced</option>
-                                                <option value="standard">Standard</option>
-                                            </select>
-                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                                        </div>
-                                    </form>
-                                </div>
-                                <?php endif; ?>
+                                <?php if (canManageModels()): ?>
+                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
+                                    <div class="inline-form-row">
+                                        <select id="inlineModelBrandId" required>
+                                            <option value="">- Brand -</option>
+                                            <?php foreach ($brands as $b): ?>
+                                            <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                            <?php endforeach; ?>
+                                        </select>
+                                        <input type="text" id="inlineModelName" placeholder="Model name" required>
+                                        <select id="inlineModelSystemType">
+                                            <option value="">System</option>
+                                            <option value="advanced">Advanced</option>
+                                            <option value="standard">Standard</option>
+                                        </select>
+                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
+                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                    </div>
+                                </div>
+                                <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <form method="POST">
-                                        <input type="hidden" name="create_model" value="1">
-                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
-                                        <div class="inline-form-row">
+                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
+                                    <div class="inline-form-row">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
@@
-                                    </form>
+                                    </div>
                                 </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                            <select name="model_brand_id" required>
+                                            <select id="inlineModelBrandId" required>
@@
-                                            <input type="text" name="model_name" placeholder="Model name" required>
-                                            <select name="model_system_type">
+                                            <input type="text" id="inlineModelName" placeholder="Model name" required>
+                                            <select id="inlineModelSystemType">
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 function normalizeInlineBrandPlaceholders() {
                     document.querySelectorAll('select[name="model_brand_id"]').forEach(function(sel) {
                         if (!sel.options || !sel.options.length) return;
                         var first = sel.options[0];
                         first.value = '';
@@
                         first.disabled = false;
                     });
                 }
+
+                function submitInlineForm(fields) {
+                    var form = document.createElement('form');
+                    form.method = 'POST';
+                    form.action = 'dashboard.php#add-file';
+                    fields.forEach(function(pair) {
+                        var input = document.createElement('input');
+                        input.type = 'hidden';
+                        input.name = pair[0];
+                        input.value = pair[1];
+                        form.appendChild(input);
+                    });
+                    document.body.appendChild(form);
+                    form.submit();
+                }
+
+                function inlineToast(message, type) {
+                    if (typeof showToast === 'function') {
+                        showToast(message, type || 'error', 3200);
+                    } else {
+                        alert(message);
+                    }
+                }
+
+                function submitInlineBrand() {
+                    var brandNameInput = document.getElementById('inlineBrandName');
+                    var brandColorInput = document.getElementById('inlineBrandColor');
+                    if (!brandNameInput) return;
+                    var brandName = (brandNameInput.value || '').trim();
+                    if (!brandName) {
+                        inlineToast('Brand name is required.', 'error');
+                        brandNameInput.focus();
+                        return;
+                    }
+                    var brandColor = brandColorInput ? (brandColorInput.value || '#1a73e8') : '#1a73e8';
+                    submitInlineForm([
+                        ['create_brand', '1'],
+                        ['brand_name', brandName],
+                        ['brand_color', brandColor],
+                        ['return_to', 'dashboard.php#add-file']
+                    ]);
+                }
+
+                function submitInlineModel() {
+                    var modelBrandInput = document.getElementById('inlineModelBrandId');
+                    var modelNameInput = document.getElementById('inlineModelName');
+                    var modelSystemInput = document.getElementById('inlineModelSystemType');
+                    var modelBrandId = modelBrandInput ? (modelBrandInput.value || '') : '';
+                    var modelName = modelNameInput ? (modelNameInput.value || '').trim() : '';
+                    if (!modelBrandId) {
+                        inlineToast('Please choose a brand for this model.', 'error');
+                        if (modelBrandInput) modelBrandInput.focus();
+                        return;
+                    }
+                    if (!modelName) {
+                        inlineToast('Model name is required.', 'error');
+                        if (modelNameInput) modelNameInput.focus();
+                        return;
+                    }
+                    submitInlineForm([
+                        ['create_model', '1'],
+                        ['model_brand_id', modelBrandId],
+                        ['model_name', modelName],
+                        ['model_system_type', modelSystemInput ? (modelSystemInput.value || '') : ''],
+                        ['return_to', 'dashboard.php#add-file']
+                    ]);
+                }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 function normalizeInlineBrandPlaceholders() {
                     document.querySelectorAll('select[name="model_brand_id"]').forEach(function(sel) {
                         if (!sel.options || !sel.options.length) return;
                         var first = sel.options[0];
                         first.value = '';
                         first.text = '- Brand -';
                         first.disabled = false;
                     });
+                    var inlineModelBrand = document.getElementById('inlineModelBrandId');
+                    if (inlineModelBrand && inlineModelBrand.options && inlineModelBrand.options.length) {
+                        inlineModelBrand.options[0].value = '';
+                        inlineModelBrand.options[0].text = '- Brand -';
+                        inlineModelBrand.options[0].disabled = false;
+                    }
                 }
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                 <?php if (canManageBrands()): ?>
                                 <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                                    <div class="inline-form-row">
-                                        <input type="text" id="inlineBrandName" placeholder="Brand name" required>
-                                        <input type="color" id="inlineBrandColor" value="#1a73e8">
-                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineBrand()"><i class="fas fa-plus"></i> Add</button>
-                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                                    </div>
+                                    <form method="POST">
+                                        <input type="hidden" name="create_brand" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <input type="text" name="brand_name" placeholder="Brand name" required>
+                                            <input type="color" name="brand_color" value="#1a73e8">
+                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
                                 </div>
                                 <?php endif; ?>
@@
                                 <?php if (canManageModels()): ?>
                                 <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <div class="inline-form-row">
-                                            <select id="inlineModelBrandId" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
-                                                <?php foreach ($brands as $b): ?>
-                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                                <?php endforeach; ?>
-                                            </select>
-                                            <input type="text" id="inlineModelName" placeholder="Model name" required>
-                                            <select id="inlineModelSystemType">
-                                                <option value="">System</option>
-                                                <option value="advanced">Advanced</option>
-                                                <option value="standard">Standard</option>
-                                            </select>
-                                            <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                                        </div>
-                                    </div>
+                                    <form method="POST">
+                                        <input type="hidden" name="create_model" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <select name="model_brand_id" required>
+                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
+                                                <?php foreach ($brands as $b): ?>
+                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                                <?php endforeach; ?>
+                                            </select>
+                                            <input type="text" name="model_name" placeholder="Model name" required>
+                                            <select name="model_system_type">
+                                                <option value="">System</option>
+                                                <option value="advanced">Advanced</option>
+                                                <option value="standard">Standard</option>
+                                            </select>
+                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
                                 </div>
                                 <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                 <?php if (canManageBrands()): ?>
                                 <div id="brandInlineForm" class="upload-inline-form" style="display:none">
-                                    <div class="inline-form-row">
-                                        <input type="text" id="inlineBrandName" placeholder="Brand name" required>
-                                        <input type="color" id="inlineBrandColor" value="#1a73e8">
-                                        <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineBrand()"><i class="fas fa-plus"></i> Add</button>
-                                        <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
-                                    </div>
+                                    <form method="POST">
+                                        <input type="hidden" name="create_brand" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <input type="text" name="brand_name" placeholder="Brand name" required>
+                                            <input type="color" name="brand_color" value="#1a73e8">
+                                            <button type="submit" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('brandInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
                                 </div>
                                 <?php endif; ?>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                                 <?php if (canManageModels()): ?>
                                 <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <div class="inline-form-row">
-                                            <select id="inlineModelBrandId" required>
-                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
-                                                <?php foreach ($brands as $b): ?>
-                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
-                                                <?php endforeach; ?>
-                                            </select>
-                                            <input type="text" id="inlineModelName" placeholder="Model name" required>
-                                            <select id="inlineModelSystemType">
-                                                <option value="">System</option>
-                                                <option value="advanced">Advanced</option>
-                                                <option value="standard">Standard</option>
-                                            </select>
-                                            <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
-                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
-                                        </div>
-                                    </div>
+                                    <form method="POST">
+                                        <input type="hidden" name="create_model" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <select name="model_brand_id" required>
+                                                <option value="">Ã¢â‚¬â€ Brand Ã¢â‚¬â€</option>
+                                                <?php foreach ($brands as $b): ?>
+                                                <option value="<?= $b['id'] ?>"><?= escape($b['name']) ?></option>
+                                                <?php endforeach; ?>
+                                            </select>
+                                            <input type="text" name="model_name" placeholder="Model name" required>
+                                            <select name="model_system_type">
+                                                <option value="">System</option>
+                                                <option value="advanced">Advanced</option>
+                                                <option value="standard">Standard</option>
+                                            </select>
+                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="button" class="btn btn-sm" style="background:#e0e0e0;color:#333" onclick="hideInlineForm('modelInlineForm')">Cancel</button>
+                                        </div>
+                                    </form>
                                 </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
-                                    <div class="inline-form-row">
-                                            <select id="inlineModelBrandId" required>
+                                <div id="modelInlineForm" class="upload-inline-form" style="display:none">
+                                    <form method="POST">
+                                        <input type="hidden" name="create_model" value="1">
+                                        <input type="hidden" name="return_to" value="dashboard.php#add-file">
+                                        <div class="inline-form-row">
+                                            <select name="model_brand_id" required>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                            <input type="text" id="inlineModelName" placeholder="Model name" required>
-                                            <select id="inlineModelSystemType">
+                                            <input type="text" name="model_name" placeholder="Model name" required>
+                                            <select name="model_system_type">
@@
-                                            <button type="button" class="btn btn-sm btn-primary" onclick="submitInlineModel()"><i class="fas fa-plus"></i> Add</button>
+                                            <button type="submit" name="create_model" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Add</button>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
-                                    </div>
+                                    </form>
                                 </div>
*** Update File: C:\Users\pc\Documents\Codex\2026-05-22\files-mentioned-by-the-user-translink\extracted\Translink file library\website\admin\dashboard.php
@@
                 function normalizeInlineBrandPlaceholders() {
                     document.querySelectorAll('select[name="model_brand_id"]').forEach(function(sel) {
                         if (!sel.options || !sel.options.length) return;
                         var first = sel.options[0];
                         first.value = '';
                         first.text = '- Brand -';
                         first.disabled = false;
                     });
-                    var inlineModelBrand = document.getElementById('inlineModelBrandId');
-                    if (inlineModelBrand && inlineModelBrand.options && inlineModelBrand.options.length) {
-                        inlineModelBrand.options[0].value = '';
-                        inlineModelBrand.options[0].text = '- Brand -';
-                        inlineModelBrand.options[0].disabled = false;
-                    }
-                }
-
-                function submitInlineForm(fields) {
-                    var form = document.createElement('form');
-                    form.method = 'POST';
-                    form.action = 'dashboard.php#add-file';
-                    fields.forEach(function(pair) {
-                        var input = document.createElement('input');
-                        input.type = 'hidden';
-                        input.name = pair[0];
-                        input.value = pair[1];
-                        form.appendChild(input);
-                    });
-                    document.body.appendChild(form);
-                    form.submit();
-                }
-
-                function inlineToast(message, type) {
-                    if (typeof showToast === 'function') {
-                        showToast(message, type || 'error', 3200);
-                    } else {
-                        alert(message);
-                    }
-                }
-
-                function submitInlineBrand() {
-                    var brandNameInput = document.getElementById('inlineBrandName');
-                    var brandColorInput = document.getElementById('inlineBrandColor');
-                    if (!brandNameInput) return;
-                    var brandName = (brandNameInput.value || '').trim();
-                    if (!brandName) {
-                        inlineToast('Brand name is required.', 'error');
-                        brandNameInput.focus();
-                        return;
-                    }
-                    var brandColor = brandColorInput ? (brandColorInput.value || '#1a73e8') : '#1a73e8';
-                    submitInlineForm([
-                        ['create_brand', '1'],
-                        ['brand_name', brandName],
-                        ['brand_color', brandColor],
-                        ['return_to', 'dashboard.php#add-file']
-                    ]);
-                }
-
-                function submitInlineModel() {
-                    var modelBrandInput = document.getElementById('inlineModelBrandId');
-                    var modelNameInput = document.getElementById('inlineModelName');
-                    var modelSystemInput = document.getElementById('inlineModelSystemType');
-                    var modelBrandId = modelBrandInput ? (modelBrandInput.value || '') : '';
-                    var modelName = modelNameInput ? (modelNameInput.value || '').trim() : '';
-                    if (!modelBrandId) {
-                        inlineToast('Please choose a brand for this model.', 'error');
-                        if (modelBrandInput) modelBrandInput.focus();
-                        return;
-                    }
-                    if (!modelName) {
-                        inlineToast('Model name is required.', 'error');
-                        if (modelNameInput) modelNameInput.focus();
-                        return;
-                    }
-                    submitInlineForm([
-                        ['create_model', '1'],
-                        ['model_brand_id', modelBrandId],
-                        ['model_name', modelName],
-                        ['model_system_type', modelSystemInput ? (modelSystemInput.value || '') : ''],
-                        ['return_to', 'dashboard.php#add-file']
-                    ]);
                 }
*** End Patch
